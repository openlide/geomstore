# G5-benchmark-medium-p2/p3 判定记录

#74 | FP | peakMemory 以 `initialMemory.heapUsed` 为起点、只在 `memoryAfter > peakMemory` 时上抬（runner.ts 现第 100/113-115 行，HEAD 版为 94/107-108），delta = peak - initial 结构上恒 >= 0；node 复刻该循环跑「GC 深回落」序列 `[900,800,750,1200,600,500,1300,100]` 得 peak=1300 / delta=300 / negative?=false，报告要求的「改用 peak 而非带符号差值」正是现状；已在赋值处补注为什么恒非负以免再被误读。
#63 | fix | 成立：`BenchmarkResult.datasetSize` 是 string，而两个产出方（runner 的 `scenario.datasetSize`、helpers `inferDatasetSize(): DatasetSize`）都给联合类型；改为 `DatasetSize` 并 `grep -rn datasetSize`（含 README/tests/scripts）确认无一处写入联合类型外的值。
#64 | reject | 漂移成立但报告改法方向错：`OperationType` 现有 11 个成员，报告建议的 `keyof ...operationTime` 只剩 6 个，会把 unsubscribe/compose/cacheGet/cacheSet/cacheInvalidate 全削掉并让 `OperationContext.type` 无法表达。改为「`keyof BenchmarkConfig['thresholds']['operationTime']` ∪ 其余 5 个显式成员」，共享部分单向派生、多余部分保留。
#65 | fix | 成立且比报告更具体：`general.iterations` 全仓无一处读取（`grep -rn "general\." src/` 只有 enableWarmup/warmupIterations/skipGC），是纯死字段，已从 BenchmarkConfig 与默认配置删除；场景级三字段保留并写明两层语义（general 只管整轮统一预热，场景级不继承不覆盖），不做「回落」以免再出一层隐式优先级。
#73 | fix（取报告第二方案） | 成立：`score` 无任何赋值点，实测 `results.map(r=>r.score)` 恒空 ⇒ avgScore 恒 0，而 markdown/HTML 两个渲染器都不显示它，只有 JSON 报告带一个假 0。包内没有评分口径，按报告建议删掉 `BenchmarkResult.score` 与 `summary.avgScore`（不新造归一化权重，那是无规格的发明）；types 的 summary 注释留下「要加回来先定义归一化口径」。
#68 | fix | 成立：箭头体确实反读自己正在定义的 `SAMPLING_CONFIG.MAX_SAMPLES`。抽出模块级 `MAX_MEMORY_SAMPLES = 100`，键值与函数都改读它；实跑编译产物 `getSampleInterval(1000)=10`、`(50)=1`、`MAX_SAMPLES=100`，行为零变化。
#69 | fix（两方案并用） | 成立：`SCENARIO_NAMES.SUBSCRIBE/SUBSCRIBE_THROUGHPUT` 在两张阈值表里都没有档，`REPLACE_STATE` 只有 AVG。补 `SUBSCRIBE_AVG/SUBSCRIBE_P99/REPLACE_STATE_P99`，`SUBSCRIBE_MIN` 按同事定下的 `deriveThroughputMin` 推导（实跑得 5000），四张吞吐档与四个 throughput 场景名一一对上；cache-*/-memory 系列按报告的第二方案在 TIME_THRESHOLDS 头注释写清「阈值按操作维度而非场景名组织，命中率归 CACHE_THRESHOLDS、字节数归 MEMORY_THRESHOLDS」，不再被当成漏配。
#77 | fix | 成立：`checkThresholds` 里的 1/3/10/50、5、10、0.1、0.67、20 全是裸字面量。提到模块级具名常量并逐条写为什么（档位次线性放宽、quick- 抖动、一轮迭代可能是 $patch/dispatch 而门限只有 setState 一档、吞吐由耗时反推更易被抖动放大、缓存场景刻意造未命中）。顺带把 `Record<string, number>` + `|| 1` 收成 `Record<DatasetSize, number>`（#63/#65 之后编译器保证四档全覆盖，回落值是死代码）；`MIN_PASS_RATIO` 注释里写明 `ceil` 的实际后果：非缓存场景 3 项需全过、缓存场景 4 项容 1 项不达标（判定口径未改，只是可交代）。
#92 | fix | 实测成立：编译产物跑 `calculatePercentile([1,2,3],0 / -10 / 101 / NaN)` 全部返回 `undefined`，直接违背 `: number`。按报告夹百分位再夹下标，并额外把 NaN 单独贴到 0 侧、±Infinity 由夹取自然走两端；改后实测 0/-10/NaN → 1、101/Infinity → 3、-Infinity → 1（全为 number），区间内 `p=50→2`、`95/99/100→3` 与改前一致。
#93 | fix | 实测成立：改前 `calculateTimeStats(300000 项)` 抛 `RangeError: Maximum call stack size exceeded`，二分探到首个失败长度 128000，正落在 xlarge 档迭代量级。改为单次循环取极值；改后 30 万项正常返回 min/max，小样本 `[4,2,9,7]` 统计逐项与改前一致。报告提到的第二处 `pairLog.push(...candidateLog)` 不在本包（全仓 grep 无 pairLog），故只改本处。
#94 | fix | 实测成立：改前 `repeat(...,5)` 第 2 轮抛错即整次 reject，`iterations run = 2`、前 1 轮数据全丢；`parallel(fn,2,6)` 同样（run = 2）。新增 `IterationOutcome<T>`（types/index.ts，随 `export type *` 已对外），`repeat`/`parallel` 共用私有 `measureIteration` 就地吸收异常，改后 repeat 返回 5 条、parallel 返回 6 条且 error 落在失败下标，其余轮次保留；失败轮的 duration 是「调用到抛出」耗时，已在类型注释写明参与统计前要先过滤 error。
#95 | fix | 实测成立：改前 `heapLimit = heapTotal*2 = 108937216`，而同一进程 `v8.getHeapStatistics().heap_size_limit = 4345298944`（差约 40 倍）。改为模块加载时取一次真实上限（堆上限进程内固定，而 getMemorySnapshot 每轮都调），改后两者相等。
#105 | fix（表达式已漂移，缺陷仍在） | 报告引的 `store.getCached ? (key) => store.getCached?.(key) : undefined` 已被同事改成解构式 `getCached ? (key) => getCached.call(store, key) : undefined`，但「构造期特性探测」的问题一模一样：A/B 实跑复刻表达式证明——事后挂上 `getCached` 时旧写法永远是 `undefined`（缓存读分支静默空转），新写法立刻可用；无缓存时 `store.getCached?.(k)` 返回 undefined 也不抛。`getCacheStats` 有意保持构造期判定并在注释写清理由：它的「有没有」就是 runner 上报「缓存未启用」的契约信号，改成恒有值会让无缓存库被当成有缓存统计。
