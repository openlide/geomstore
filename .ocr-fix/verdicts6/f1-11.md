# f1-11（wave f1）

条数：1｜FIXED 0 / FP 0 / REJECT 0 / NEEDS-MAIN 1

## R6-114 low · tsconfig.json:L32-L32

verdict: NEEDS-MAIN

改动: none —— 探针式改动已全部回退，`git diff -- tsconfig.json` 为空、`npx prettier --check tsconfig.json` → `All matched files use Prettier code style!`。基线第 32 行仍是 `"types": ["node", "jest"],`。

回归锁: none（未落地代码；本报告方向若被采纳，属配置收紧、不改对外行为，是否补锁由主会话定）

### 判据（实测明细）

事实层面报告成立，且「摘掉 jest」今天确实零成本：

1. src/examples 对 `@types/jest` 全局零依赖（两种独立测法互相印证）：
   - `grep -rnE "\b(jest|jasmine|expect|describe|test|it|beforeAll|afterAll|beforeEach|afterEach|fit|xit)\b" src examples --include=*.ts` → 命中全部在注释/字符串里（如 `storeName: 'test-store'`、指向 tests 文件名的说明），唯一的代码级 `\.test(` 是 `RegExp.test`（`src/core/compose/helpers.ts:38`、`src/core/store/StateProxy.ts:367`）。
   - 报告自带的取证命令 `npx tsc --noEmit -p tsconfig.json --types node` → exit 0、零诊断；并顺手验证该命令不是空跑：`npx tsc --noEmit -p tsconfig.json --types nope-not-a-pkg` → `error TS2688`（exit 2），说明 CLI `--types` 确实覆盖 `-p` 里的 `types`，证据有效。
2. 临时把基线改成 `"types": ["node"]` 后，四条管线 + 发布链 + 测试侧 tsconfig 全跑了一遍（同一份快照）：
   | 管线 | 命令 | 摘掉 jest 后 |
   | --- | --- | --- |
   | Typecheck (src) | `npx tsc --noEmit -p tsconfig.typecheck.json` | exit 0 |
   | typecheck:src | `npx tsc --noEmit`（基线） | exit 0 |
   | typecheck:examples | `npx tsc --noEmit -p tools/tsconfig.examples.json` | exit 0 |
   | typecheck:tests | `npx tsc --noEmit -p tsconfig.tests.json` | 仅 1 条 `tests/unit/r6-f1-06-clone.test.ts(49,22) TS2554`（f1-06 分片在飞的用例；`tsconfig.tests.json` 自带 `"types": ["jest","node"]`，与本改动无关） |
   | build（产 dist/.d.ts） | `npx tsc -p tsconfig.build.json --outDir $TEMP/probe-nojest` | exit 0，正常产出 index.d.ts/core/extras/... |
   | 测试侧继承链 | `npx tsc --noEmit -p tsconfig.jest.json` / `-p tests/tsconfig.json` | 各 exit 0 |
   也就是说：**没有任何一条管线因为摘掉 jest 而变红**，继承链也没断——`tsconfig.build.json` / `tsconfig.typecheck.json` / `tools/tsconfig.examples.json` 只吃 src+examples，`tsconfig.jest.json:11`、`tsconfig.tests.json:5`、`jest.config.js:26` 三处各自显式声明 `["jest","node"]`（且 `jest.config.js:15` 为 `diagnostics: false`，测试侧本来不做类型检查），测试面完全不受影响。
3. 因此这条的阻塞点**不是技术风险，而是权限/口径**：`fixbrief6.md` 硬约束 4 写明本轮分片 agent「零 lint/format/tsconfig 配置改动」，基线 `types` 属 tsconfig 配置面，落不落地须由统管配置与 CHANGELOG 的主会话拍板。
4. 一处需要纠正的前提交代：任务派单里说「`decisions5.md` 提到第五轮零配置改动是既定口径」——**与记录相反**。第五轮 root-config 分片实际 FIXED 18 条配置类改动（`eslint.config.js`/`jest.config.js`/`tsconfig.typecheck.json`/`package.json`/`.gitignore`/`ci.yml`），并在 `CHANGELOG.md:125` 以「配置清理」条目收口（那次就包含基线 `tsconfig.json` 的 `lib` 去 DOM）。所以本轮的冻结理由只能落在硬约束 4 本身，不能引第五轮判例；请主会话按「是否连收紧一起冻」明确一句，避免同类条目反复回炉。

给主会话的决策包（择一，都是一次到位、不留白名单）：

- 采纳：把 `tsconfig.json:32` 改为 `"types": ["node"]`，并在第 28–30 行那段注释后补一句「基线 `types` 语义＝库源码可见全局只有 node；测试侧的 jest 由 `tsconfig.jest.json`/`tsconfig.tests.json`/`jest.config.js` 三处显式声明」——收益是把「src 误用 `expect`/`jest.fn()` 能过 typecheck 与 build、到运行时才 ReferenceError」这条漏口关掉；代价实测为零。
- 拒采：那就把「基线 types 含 jest」当成有意为之（IDE 便利），并在 `tsconfig.json` 注释里写明它同时驱动 build/typecheck(src) 是已知取舍，免得第七轮又长出来。

文档: 采纳则属纯配置收紧、无对外行为变更 → `CHANGELOG.md` 走「配置清理」措辞一句（`docs/**` 不动，SKILL 不动）；拒采则 none。另：本轮若维持冻结，建议在 `fixbrief` 里把「配置改动」明确为「禁止放宽型改动，收紧型走主会话」，消除口径歧义。

### 自验

- `npx prettier --check tsconfig.json` → `All matched files use Prettier code style!`
- `git diff -- tsconfig.json` → 空（探针已回退，本分片对仓库净改动为 0）
- 未新增/改动测试文件，故不跑 `npx jest`；未改 src/tests/scripts，故不跑 `npx eslint`。

### 顺带回报（并发噪声，交主会话对账，非本分片职责）

同期其他分片在改 src，我最后复测基线时看到两条红：`src/core/compose/StoreRegistry.ts(10,1) TS6133 'isProduction' is declared but its value is never read`（typecheck 与 build 都报）与 `tests/types/tmp-probe-r6f110.typecheck.ts(13,45) TS2322`（f1-10 的临时探针文件，名字带 `tmp-probe`，全库门禁前记得删）。这两条与我无关，但会让「四条 typecheck 全绿」的收口对账踩到。
