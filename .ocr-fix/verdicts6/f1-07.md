# f1-07（wave f1）
条数：10｜FIXED 10 / FP 0 / REJECT 0 / NEEDS-MAIN 0（另附 2 条跨文件尾巴，见文末「NEEDS-MAIN 交接」）

涉及文件均已改完：`src/extras/action/decorators/timeout.ts`、`src/extras/error/ErrorAggregator.ts`、
`src/extras/error/ErrorMonitoring.ts`、`src/extras/error/reporters/HttpReporter.ts`、
`src/extras/index.ts`、`src/extras/selector.ts`、`src/extras/selector/createSelector.ts`。
新增回归锁：`tests/unit/r6-f1-07-error-accounting.test.ts`、`tests/unit/r6-f1-07-http-reporter.test.ts`、
`tests/unit/r6-f1-07-entry-and-docs.test.ts`。
未动 `src/extras/error/index.ts` 与任何子入口清单（本片不需要新增导出符号）。

## R6-092 low src/extras/action/decorators/timeout.ts:L43-L51
verdict: FIXED
改动: @example 的判据从 `error.message.includes('Timeout after')` 换成
`(error as { code?: unknown }).code === TIMEOUT_ERROR_CODE`（示例顶部补 `import { TIMEOUT_ERROR_CODE, withTimeout } from '@openlide/geomstore/extras/action'`）；
被弃用的文本写法降到示例后的「历史注记」里，并写明它双向失效的两个方向（误判底层自抛文本 / 漏判执行器入口的小写 `Action timeout after`）。
回归锁: tests/unit/r6-f1-07-entry-and-docs.test.ts > 「withTimeout 抛出的错误带 code，且两个入口的消息文本确实不同」
判据: 纯 JSDoc 修复，但示例口径单独锁了一条运行时用例（`caught.code === TIMEOUT_ERROR_CODE`、消息含 `Timeout after 5ms`）。
文档: 纯文档修复，非行为变更。dist 的 .d.ts 会带上新示例 → 主会话重跑 `pnpm skill:api` 刷新
`.codebuddy/skills/geomstore/references/api/extras-action.md` 的 `withTimeout` 一节即可，无需手工改写。

## R6-047 medium src/extras/error/ErrorAggregator.ts:L137-L145
verdict: FIXED
改动: **选定「单独维护总数」这一支**（作业说明给的二选一）：`getStats()` 的
`totalErrors` 改为按条累计的 `observedErrors`（每次 `addError` 加一，驱逐与 clear 之外永不回退），
`byCode` / `byStore` 同步改为按条累计的独立账目（不再从存活组现算）；另外**同时**补上驱逐留痕
`evictedGroups` / `evictedErrors`（`cleanupOldGroups` 里累加被删组的 count）+ 首次驱逐出声一次 warn。
不变量：`sum(byCode) === sum(byStore) === totalErrors` 恒成立且随时间单调不减。
分组视图（`totalGroups` / `getGroups()`）仍只反映存活组，差额由 evicted 两项说明。
`clear()` 把全部账目与留痕一起归零（口径＝「自上次 clear() 以来」，与 `droppedErrors` 一致）。
回归锁: tests/unit/r6-f1-07-error-accounting.test.ts > 「组被 maxGroups 驱逐后 totalErrors 单调不减，evictedErrors/evictedGroups 记账」、
「clear() 把驱逐留痕与全部账目一起归零」、「ErrorMonitoring.generateReport 的 totalErrors 跨驱逐保持单调」
判据: 探针（临时把 `totalErrors` 改回存活组求和后实跑）：`Expected: 4 / Received: 3`、
`Expected: 130 / Received: 100`，复现「倒退」；复原后 3 条全绿。既有 `sum(byStore) === totalErrors` 锁
（tests/unit/extras/error/error-followups.test.ts:82、r5-extras-error-recovery.test.ts:237、
ErrorMonitoring.test.ts:691）在新口径下继续成立（实跑 190 passed）。
文档: **行为变更（对外可读）**。`ErrorAggregator.getStats()` / `ErrorMonitoring.getAggregationStats()`
的 `totalErrors`/`byCode`/`byStore` 口径由「存活组求和」改为「自 clear() 起观测到的全部错误」，
并新增 `evictedGroups` / `evictedErrors` 两个字段；`generateReport().summary.totalErrors` 不再随驱逐变小。
CHANGELOG 需按「修复：聚合驱逐不再让错误总数倒退（口径统一为观测到的错误数）」写，并在文档里明确
`totalGroups`/`topErrors` 仍是存活组视图。

## R6-093 low src/extras/error/ErrorAggregator.ts:L107-L114
verdict: FIXED
改动: 单组 Store 登记从 `affectedStores.includes` 线性扫描改为 `GroupEntry.storeNames`（Set）O(1) 去重，
并给基数设上限 `MAX_STORES_PER_GROUP = 50`：达到上限后追加一次保留字 `__others__` 桶标记、此后不再逐个登记（每组只 warn 一次）。
随组 `hits` Map 整体删除（按 Store 的账目已在 R6-047 里改由 `observedByStore` 承担）；
全局 `byStore` 同样设 `MAX_TRACKED_STORE_KEYS = 200` 上限，超出并入 `__others__`，
所以「截断的只是列表/键基数，条数一条不丢」。`getGroupsByStore` 的口径限制写进 JSDoc
（溢出组「没返回」不等于「没报错」，要准确按 Store 计数请用 `getStats().byStore`）。
回归锁: tests/unit/r6-f1-07-error-accounting.test.ts > 「同一组超过 50 个 Store 后并入 __others__，组 count 与 byStore 不缺计」、
「全局 byStore 的键数有上限，超出部分并入 __others__ 且求和不变」
判据: 探针（临时把该分支换回旧的 includes/push 写法）：上限用例失败；复原后全绿。
文档: **行为变更（对外可读）**：`ErrorGroup.affectedStores` 与 `getStats().byStore` 的键集合里
可能出现保留字 `__others__`。文档需写明它是溢出桶（计数已并入其中，不是某个真实 Store）。

## R6-048 medium src/extras/error/ErrorMonitoring.ts:L106-L118
verdict: FIXED
改动: 构造期改走新增的 `normalizeReporters(config.reporters)`：非数组（含被显式 undefined 顶掉的值）
→ `[]` + 一条 console.warn（与本库「配置错不抛错但出声」口径一致）；
`createDefaultMonitoring` 把 `reporters` 这一项移到 `...config` **之后**，
`givenReporters === undefined` 时回到默认 `[new ConsoleReporter()]`，真给数组（含合法 `[]`）才尊重调用方。
回归锁: tests/unit/r6-f1-07-error-accounting.test.ts > 「createDefaultMonitoring 带显式 undefined 的 reporters 时仍保留默认 ConsoleReporter」、
「reporters 非数组时退回空数组并出声，report/addReporter 不再同步抛 TypeError」
判据: 探针（临时回退构造器与 createDefaultMonitoring 两处）：两条用例分别以
`TypeError: Cannot read properties of undefined (reading 'map')` 与
`TypeError: this.reporters.map is not a function` 失败——与 finding 描述的后果链一致；复原后全绿。
文档: 行为变更（异常路径变可控）：`reporters` 传非数组不再让 `report()` reject，而是退回无报告器并 warn。

## R6-094 low src/extras/error/ErrorMonitoring.ts:L428-L439
verdict: FIXED
改动: 与 R6-048 同一处收口——`normalizeReporters` 对数组返回 `[...reporters]` 私有副本，
`addReporter`/`removeReporter` 从此都只作用于实例自己那份数组（构造器注释、两个方法的 JSDoc 同步改写）。
回归锁: tests/unit/r6-f1-07-error-accounting.test.ts > 「addReporter 不改写调用方数组，也不跨实例生效」
（断言：同一 config 数组喂两个实例后 `shared` 长度仍为 1、m1 的注册不会被 m2 使用、remove 后方向一致）
判据: 旧实现 `this.reporters = config.reporters` + `push` 必然改写调用方数组（该断言方向即失败点）；
另 R6-048 探针运行中同一数组别名路径已实跑复现。
文档: 行为变更（副作用移除）：`new ErrorMonitoring({ reporters: arr })` 之后库不再修改 `arr`。

## R6-049 medium src/extras/error/reporters/HttpReporter.ts:L227-L255
verdict: FIXED
改动: 按「不改行为契约、把 body 产出收进兜底」的方向做三件事——
(1) 新增 `toSerializableScalar`，`storeName`/`operation`/`level`/`timestamp` 四个透传字段与 payload 同一口径
（string/number/boolean 原样、bigint/symbol 字符串化、对象/函数降级为 `[object]`/`[function]` 标记）；
(2) 每条上下文改为**各自序列化一次**（新增 `serializeContextItem`，内部 try/catch）：本条导致不可序列化时
只把它自己换成 `{"error":{"message":"[Unserializable error context: <原因>]"}}` 片段，原因仍写入消息供排查；
批量体由 `buildBatchBody` 用已序列化片段拼 `{"errors":[...]}`，**不再**做「验证一遍 + 外层重串」，
于是非幂等 getter/toJSON 只在第二次取值才抛的路径也被挡住（该条最多自我降级）；
(3) `reportBatch` 补 `Array.isArray` 兜底（与 ConsoleReporter.ts:90 同口径），非数组按空批次处理。
单条与批量仍共用同一投影（`report` 与批量条目都出自 `serializeContextItem`，R5-216 的键集合一致性用例继续通过）。
回归锁: tests/unit/r6-f1-07-http-reporter.test.ts > 「BigInt timestamp / Symbol level 收成字符串，整批不 reject」、
「对象值的标量字段降级为类型标记…」、「非幂等 toJSON（第二次取值才抛）只降级它自己那一条」、
「reportBatch 收到非数组时按空批次处理…」、「单条 body 与批量条目仍是同一投影…」
判据: 旧实现探针（把 `git show HEAD:src/extras/error/reporters/HttpReporter.ts` 复制成临时文件实跑）3 条全过，
逐条复现缺口：① `reportBatch` 含 BigInt timestamp → `rejects.toBeInstanceOf(TypeError)`（整批失败）；
② `reportBatch(null)` → TypeError；③ wx `fail({errMsg:'request:fail timeout', errno:7})` →
消息恒为 `'wx.request failed'` 且 `cause === undefined`。新实现下这三条改判为「整批 resolve + 标量字符串化 +
消息含 errMsg/errno 并挂 cause」，即本片锁的断言方向。探针目录 `tests/probe-f1-07-tmp/` 跑完已删除。
文档: 行为变更（对外可读的报文形态）：畸形标量会被字符串化/换成 `[object]` 标记；
一条彻底不可序列化的上下文以 `[Unserializable error context: …]` 片段留在批次里。
文档需补一句区分依据：**批次被丢弃**（`anyReporterSucceeded` 为假、按 maxFlushRetries 重入队）与
**单条上下文畸形**（批次仍成功，条目里出现该标记）现在是两件事。见 `.ocr-fix` 尾巴（docs/**）。

## R6-095 low src/extras/error/reporters/HttpReporter.ts:L132-L141
verdict: FIXED
改动: 新增 `describeWxFailure` + `toWxRequestError`：`fail` 回调不再恒走 `new Error('wx.request failed')`，
而是把平台的 `errMsg`（+ `errno`）拼成 `wx.request failed: <errMsg> (errno: <errno>)`，
并按本库既有口径把原始载荷挂到 `cause`（ES2020 无 cause 签名，用属性赋值补，同 `ErrorRecovery.ts` 的 `withCause`）；
`Error` 实例载荷仍原样透传，字符串载荷作为原因，空载荷保持 `'wx.request failed'`。
回归锁: tests/unit/r6-f1-07-http-reporter.test.ts > 「errMsg 与 errno 都进入 Error 文本，原始载荷挂在 cause 上」、
「超时类失败可按原因区分，不再全是同一句常量」、「字符串载荷原样作为原因；空载荷与 Error 载荷保持既有行为」
判据: 旧实现探针（HEAD 文件）实跑：`caught.message === 'wx.request failed'` 且 `cause === undefined`
→ 新断言（含 errMsg/errno + cause）在旧码上必失败。既有 `tests/unit/extras/error/error-boundaries.test.ts:142`
（`rejects.toThrow('wx.request failed')`）与 `ErrorMonitoring.test.ts:621` 仍通过（子串匹配）。
文档: 行为变更（错误文本更具体）：wx 上报失败抛出的 Error 消息由常量变为含 errMsg/errno。
若有文档写死「常量消息」需一并更新（未发现此类文档）。

## R6-096 low src/extras/index.ts:L74-L84
verdict: FIXED
改动: action 段补 `export type { LogSink, LogPhase } from './action.js'`（附「为何必须转发」的理由注释），
并把文件头「清单一致性由该测试兜底」收窄为「**值导出**一致性由该测试兜底；`Object.keys()` 拿不到 `export type`，
类型清单仍需人工与子入口对齐（R6-096 即该漂移的实例）」。
回归锁: tests/unit/r6-f1-07-entry-and-docs.test.ts > 「LogSink / LogPhase 在两个入口上都是同一类型」
（`import type` 自两个入口并互相赋值——任一侧缺项即 `tsc` 报错，即报告建议的「按名字列表比对的类型级用例」形态）
判据: 编译期锁：`import type { LogSink } from '@/extras/index.js'` 在补导出前是 TS2305；
`npx tsc --noEmit -p tsconfig.tests.json` 现 0 报错（本片文件）。
既有 `tests/unit/r5-extras-action-p2-entry-parity.test.ts` 未改（它锁值导出，仍通过）。
文档: 无（导出面纯增，`@openlide/geomstore/extras` 多两个类型名）。

## R6-097 low src/extras/selector.ts:L5-L22
verdict: FIXED
改动: 值清单补 `createRetrySelector` / `createRetrySelectorAsync`（与 `selector/index.ts:20` 对齐）；
`@example` 从 reselect 风格两参改为真实签名 `createSelector(selectorFn, options?)`，
并明写「这里没有输入选择器 + 组合函数的两参形态」。类型清单第 9-11 行与 `index.ts:21-22` 已逐名核对，无缺项。
回归锁: tests/unit/r6-f1-07-entry-and-docs.test.ts > 「文档列出的值导出全部在入口上（含两个重试工厂）」、
「文档示例的真实签名可运行：单参 selectorFn，第二个参数是 options」（后者同时固化「两参写法没有组合语义」）
判据: 文档修复但配了运行时锁：入口枚举 8 个文档列出的名字全部为 function；
示例 `createSelector((s) => s.count * 2)` 返回 42，两参写法返回 21（第二个实参被当 options 忽略）。
文档: 纯文档修复（对外行为不变）。

## R6-098 low src/extras/selector/createSelector.ts:L353-L362
verdict: FIXED
改动: 「纯对象/数组/Date/RegExp/Map/Set 会被正确深拷贝，不受影响」拆成两类看法：
纯对象/数组/Date/RegExp 在 `deepEqual` 下可分辨、快照路径正常；**Map/Set 的键也被深克隆**
而 `deepEqual` 的 Map 分支按键的 SameValueZero 匹配 → 状态里存在**对象键 Map** 时
`deepEqual(clone(state), state)` 恒 false，无版本号快照路径上**每次调用都判 miss**
（每次付整树克隆 + 整树深比较，比 `cache: false` 更贵且无诊断信息），并给出三条规避口径。
与 `core/utils/clone.ts:36-40`、`core/utils/equality.ts:184-198` 措辞对齐；行为未改（clone.ts 已裁定「该窄口径不改为行为」）。
回归锁: tests/unit/r6-f1-07-entry-and-docs.test.ts > 「无版本号快照路径：对象键 Map 让缓存永不命中」
（对象键 Map 三次调用 → 计算函数被调 3 次；原始值键 Map → 只调 1 次，对照证明文档口径的边界正好是「对象键」）
判据: 该用例先在当前实现上跑过（passed），断言方向与文档新增句子一一对应；
`src/extras/selector/parametricSelector.ts` 的「限制：与 createSelector 相同」一段**不含**被纠正的那句
（已核对全文，只描述不可克隆对象），故不需同步修改、也不在本片白名单内。
文档: 纯文档修复（行为不变）。

## NEEDS-MAIN 交接（跨白名单，本片未动）

1. `src/types/error.ts`（契约层，非本片白名单）需要两处扩展，才能把 R6-047 的两个「建议方向」补齐到对外报告面：
   - `MonitoringConfig` 增加 `maxGroups?: number`（聚合存活组上限，建议默认仍 100）。
     主会话加完字段后，`ErrorMonitoring` 构造器里改一行即可（本片已在 `new ErrorAggregator()` 处留注释指回这条）：
     `this.aggregator = new ErrorAggregator(normalizeCapacity(config.maxGroups, DEFAULT_MAX_GROUPS, 1))`
     （`DEFAULT_MAX_GROUPS` 需从 `ErrorAggregator.ts` 导出，或由主会话把该常量在契约层注释里锚定为 100）。
   - `ErrorReport.summary` 增加 `evictedGroups: number` / `evictedErrors: number`，
     并在 `totalErrors` 的注释里补「含被 maxGroups 驱逐者」这句。目前这两个值只能从
     `monitoring.getAggregationStats()` 取到，`generateReport()` 的快照里看不到（口径已正确、留痕可查，
     但报告快照是自足交付物，缺这两项就得额外调一次 stats）。改 `generateReport` 只需在 summary 里
     透传 `stats.evictedGroups` / `stats.evictedErrors`。
   阻塞点：本片白名单不含 `src/types/error.ts`，且 `ErrorReport` 是随包发布的公开类型（加字段属对外可见变更）。
2. `docs/**`、`CHANGELOG.md`、`.codebuddy/skills/**` 由主会话统一收尾，措辞见上面各条的「文档」行：
   真正的**行为变更**是 R6-047（错误总数口径 + 新字段）、R6-093（`__others__` 溢出桶）、
   R6-048/R6-094（reporters 归一化与私有副本）、R6-049（报文里出现标量降级与 `[Unserializable error context]` 片段）、
   R6-095（wx 失败消息含 errMsg/errno）；R6-092/R6-096/R6-097/R6-098 是文档/导出面修复。
   其中 skill 的 API 清单（含 `withTimeout` 示例）由 `pnpm skill:api` 从 dist 再生成，重跑即可。

## 自验（收工前实跑）

```
npx jest tests/unit/r6-f1-07-error-accounting.test.ts tests/unit/r6-f1-07-http-reporter.test.ts tests/unit/r6-f1-07-entry-and-docs.test.ts --ci
  → Test Suites: 3 passed, 3 total / Tests: 22 passed, 22 total

npx jest tests/unit/core/error/ErrorMonitoring.test.ts tests/unit/extras/error tests/unit/r5-extras-error-monitoring.test.ts tests/unit/r5-extras-error-recovery.test.ts tests/unit/r5-extras-error-reporters.test.ts tests/unit/hot-round5.test.ts --ci
  → Test Suites: 8 passed, 8 total / Tests: 190 passed, 190 total

npx jest tests/unit/extras tests/unit/r5-extras --ci
  → Test Suites: 1 failed, 53 passed, 54 total / Tests: 2 failed, 997 passed, 999 total
    两条失败在 tests/unit/r5-extras-snapshot-fixes.test.ts（clone/equality 的 SKIP_CLONE_NODE 与无序配对），
    属并行分片正在改的 src/core/utils/clone.ts、equality.ts、src/extras/snapshot/**（git status 可见其为在改状态），
    与本片文件无交集；本片负责的 54 个套件里其余全绿。

npx eslint --max-warnings 0 <本片 7 个 src 文件 + 3 个测试文件>   → 0 error 0 warning
npx prettier --check <同上 10 个文件>                             → All matched files use Prettier code style!
npx tsc --noEmit                                                  → 本片文件 0 报错
                                                                    （剩余报错在别的在改文件：
                                                                    src/core/store/ActionManager.ts、
                                                                    src/extras/action/decorators/cache.ts、
                                                                    src/integrations/enterprise/offline.ts）
npx tsc --noEmit -p tsconfig.tests.json                           → 本片测试文件 0 报错
```

未跑全量 jest / pnpm build / pnpm test（按作业说明交主会话统一门禁）。全程无 `git commit` / `stash` / `checkout`。
