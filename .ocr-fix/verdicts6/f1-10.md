# f1-10（wave f1）
条数：10｜FIXED 9 / FP 0 / REJECT 1 / NEEDS-MAIN 0（另有 4 条尾巴需主会话接力，见文末「跨片/NEEDS-MAIN 尾巴」）

涉及文件（全部已改）：src/types/compose.ts、src/types/integration.ts、src/types/store.ts、
tests/types/integration-types.typecheck.ts、tests/types/selector-equalityfn-variance.typecheck.ts、
tests/types/store-config-base.typecheck.ts、tests/utils/createTestStore.ts

## R6-061 medium maintainability src/types/compose.ts:L10-L41
verdict: REJECT
改动: 未按「删除公开 API」方向落地；改按报告的另一分支——把 `ComposeOptions.lazy` / `tree` / `NamespaceConfig`
逐个就地标注**未实现/未接线**（写清「编译通过、静默无效」+ 读取方证据 + docs/API.md 无条目），
并在接口头注释里说明「运行时只消费 namespace / strict 两项」。未删除任何导出。
判据: 事实成立且已复核——`src/types/compose.ts` 是唯一出现 `lazy`/`tree?`/`separator`/`autoPrefix` 的类型文件；
`src/core/compose/composeStore.ts:98-99` 构造函数只读 `options.namespace`/`options.strict`，
`createStoreTree`（同文件 :970-972）只读 `namespace`；`NamespaceConfig` 的三处再导出在
`src/core/index.ts:80`、`src/core/compose/index.ts:7`、`src/core/compose/composeStore.ts:958`（`grep -rn NamespaceConfig src/` 实测）。
不按删除改的理由：① 删 `ComposeOptions` 成员与 `NamespaceConfig` 导出都是对外破坏性变更，需主版本窗口；
② 收口点在 3 个不在本分片的文件；③ 第五轮判例（`.ocr-fix/decisions5.md` R5-241）已确立
「唯一有牙齿的改法是破坏公开契约时，不为一轮评审静默删导出」。
文档: docs/API.md 需要新增/明确「`lazy`、`tree`、`NamespaceConfig` 为已声明未实现项，写了不生效」的口径；
CHANGELOG 记为**纯文档修复**（无行为变更）。⚠️ 冲突提醒：`.codebuddy/skills/geomstore/SKILL.md` 现文已写成
「lazy/tree/NamespaceConfig 已从 ComposeOptions 移除，手写会被 TS 编译报错」「ComposedStore 已移除」——
与当前代码相反（成员/类型都还在，实测见上），需主会话二选一：要么排到 0.7.0 真删并同步本分片注释，
要么把 SKILL 文案改回「已声明未实现」。本分片禁改 skills/**，未动。

## R6-062 medium documentation src/types/integration.ts:L25-L30
verdict: FIXED
改动: 重写 `ConnectOptions.autoUpdateOnShow` 注释——挂载点改为「页面 onShow / 组件 pageLifetimes.show / App onShow」，
写明 `attached` 是首次注入点（等价 onLoad）且组件配置的 `onShow` 不是组件生命周期；补上生效三条件
`autoUpdateOnShow && autoInject && injectMapping 非空`，并点明「缺任一即无效且不告警」；`injectMapping` 注释补「`{}` 与未写等价」。
判据: 三处判定实测在 `src/integrations/with-store.ts:239`、`:447`、`src/integrations/with-app-store.ts:267`（同一条三条件与门）；
组件侧挂 `pageLifetimes.show`（with-store.ts:445-458 及其原注释）。纯注释修复，无行为变更，按说明书不加运行时锁
（该三条件行为已由既有用例锁住：`tests/integration/with-store.test.ts` 的 COV-002/COV-003、
`tests/unit/r5-integrations-p2.test.ts` 的「未开 autoUpdateOnShow 时不包装 onShow」）。
文档: none（docs/API.md:208 早已是本口径，本轮是把类型注释追平，不改对外措辞）

## R6-063 medium bug src/types/integration.ts:L148-L157
verdict: FIXED（`setData` 半已落地；`data` 半见尾巴）
改动: `InjectedDataShape` 收为**只服务实例视角**（保留必选 `setData`），新增内部基类型
`InjectedConfigDataShape`（只有 `data`，不声明 `setData`），`PageConfig` / `ComponentConfig` 改用它；
`PageConfig` / `ComponentConfig` 的文档同时写清「配置对象是用户字面量的浅拷贝、`data` 是实例 data 的口径、
不代表装饰器返回那一刻可读」。回归锁在 tests/types/integration-types.typecheck.ts（见 R6-063 锁段）。
判据: 运行时证据——`enhancedConfig = { ...PageConfig }`（with-store.ts:150 附近、`:470` 附近），
全链路只调 `this.setData`（grep setData：仅 with-store.ts:197/210/223/243/389/402/410/454 与内部类型
`:34/:48/:85/:96` 的配置侧可选、实例侧必选）。类型侧端到端锁做过「先失败后通过」实跑：
把 `InjectedConfigDataShape` 临时加回必选 `setData`（＝旧实现）后 `npx tsc --noEmit -p tsconfig.tests.json`
报 3 处（integration-types.typecheck.ts:362/363 TS2322 + 端到端 `pageOutput.setData({count:1})` 的 TS2578），
撤掉改动后归零。顺带纠正了原锁自身的问题：`const x: PgCfg['setData'] = undefined` 那种「取值式」反例
在旧实现上也报错（缺成员 vs undefined 不可赋），两态皆红＝空锁，已改为 `Equal<'setData' extends keyof T ? true : false, false>`。
文档: CHANGELOG 记为**类型面收紧**（非运行时行为变更）：`withPageStore/withComponentStore` 返回的配置对象类型不再有 `setData`；
调用方若曾依赖 `cfg.setData` 会转为编译报错（运行时本就是 TypeError）。docs/API.md 若描述过配置对象含 setData 需一并改。

## R6-064 medium documentation src/types/integration.ts:L386-L403
verdict: FIXED
改动: 按 R5-277/R5-285 后的实现重写 `AppThis` 撞名段与 `HostStoreApi` 注释：
不再是「无条件 `Object.assign` 覆写 / `exposedKeys` 循环 / 只在卸载时还原原值」，改为
「七个键逐个经 `canOwnKey` 判定 + `defineOwnValue`（defineProperty）写入，卸载按**原描述符整体回放**」，
并把「宿主该键不可重定义 ⇒ 两侧都留不下、只剩一条跳过告警（exposeStoreAPI 与 bindActions 各有）」写进撞名结论；
`globalData` 段同步补上「该键不可重定义时改为跳过并告警，顶掉不成立」的反向分支。别名规避手法原文保留。
判据: `src/integrations/utils.ts` 实测——`canOwnKey` :74-82、`defineOwnValue` :84-94、
`bindActions` 跳过分支 :296-303（含 `[bindActions] 宿主成员 … 不可重定义…已跳过` 告警）、
`members` 表 :418 与 `skipped` 汇总告警 :426-441；挂载顺序 `with-app-store.ts:242`(bindActions) → `:251`(exposeStoreAPI)。
纯注释修复，不加锁。
文档: docs/API.md 的三类告警口径（第五轮已记）需核对是否已含「跳过暴露」这一条；CHANGELOG 记为纯文档修复。

## R6-065 medium documentation src/types/store.ts:L300-L319
verdict: FIXED
改动: `StoreConfig.cacheKeys` 与 `StoreOptions.cacheKeys` 两处注释统一为
「未提供（undefined）时缓存所有键；**显式空数组＝一个键都不缓存**」，并给出实现判据、告警文案、
派生表达式踩雷样例（`Object.keys(state).filter(...)` 得 `[]`）与「要全缓存请不写这一项」。
以运行时实现为权威，**未改任何默认语义**。
判据: `src/core/store/StoreCache.ts:121-128`（`this._cacheKeys = keys ? new Set(keys) : undefined` + 空数组告警
「cacheKeys 为空数组：缓存不会命中任何键，enableCache 形同关闭」）；既有运行时锁
`tests/unit/regression/ocr-medium-wave.test.ts:434`「#153 cacheKeys 为空数组时告警并保持『不缓存任何键』语义」。
文档: docs/API.md / SKILL 里凡出现「cacheKeys 为空时缓存所有」的措辞都要按此改（行为未变，纯纠错）。

## R6-109 low bug src/types/store.ts:L88-L101
verdict: FIXED（注释已落地；形状反例锁见尾巴）
改动: 把「混合形状」残留口子写进 `ActionCollisionKeys` 的注释：`interface MyActions extends Actions { getState(): number }`
仍命中 `string extends keyof A` ⇒ 一个键都不剔除 ⇒ `ActionContext` 上 `getState()` 解析到基座签名（返回 S），
而运行时是用户 action 覆盖基座；并写清「类型层无法还原显式键名、真要关需在 StoreOptions/FactoryStoreConfig 拒收带索引签名的 A（破坏性收紧）」
+ 规避清单（action 名避开 name/state/setState/$patch/$replaceState/getState/dispatch）。
判据: 运行时侧已复核 `src/core/store/ActionManager.ts:134-146`（context 代理 `get` 陷阱优先返回
`boundActions[prop]`）+ `:151-156`，即「用户 action 覆盖基座」成立，故类型与运行时确实相反。
类型面无法按报告方向修（索引签名吸收显式键），故本轮只落注释，反例锁待跨片补。
文档: none（类型面未变）

## R6-110 low documentation tests/types/integration-types.typecheck.ts:L377-L379
verdict: FIXED
改动: 删掉「运行时目前只查 `app.__store__.getState()`、补断言记为 NEEDS-MAIN」的过期待办口吻，改为指向既有用例
（`tests/integration/with-app-store.test.ts` 的「action 名与调试 API 同名（getState）时，App 上的展平成员仍是基座 API」，
含 `expect(typeof app.getState).toBe('function')`、`expect(app.getState()).toEqual({ count: 3 })`、
`expect(app.__store__.dispatch('getState')).toBe('from-action')`），并顺手把「后者无条件覆写」改成
「逐个 canOwnKey + defineProperty；不可重定义则两侧都留不下」的新口径（与 R6-064 同口径）。
判据: 实读该用例（with-app-store.test.ts:233-253）确认断言查的就是展平成员本身；
`npx jest tests/integration/with-app-store.test.ts --ci` 2 suites/41 tests 全绿。
文档: none

## R6-111 low test tests/types/selector-equalityfn-variance.typecheck.ts:L33-L37
verdict: FIXED
改动: ① `equalityFn: (a, b) => a === b` 那条「正例」补上成对的 `snapshotState: false`，并注明「本文件是写法参照物，
按可抄的正确形态书写；默认快照 + 引用相等 ⇒ 永不命中」；② 新增形状锁
`Equal<NonNullable<SelectorOptions['snapshotState']>, boolean>` 与 `[undefined] extends [SelectorOptions['snapshotState']]` ⇒ 可选，
外加 `snapshotState: 'lazy'` 的 `@ts-expect-error` 反例；③ 头部第 3 条措辞收窄为「只锁**方差**口径」，
并明写两条入口在 `snapshotState` 上**刻意不一致**（`createMemoizedSelector` 不暴露该选项），
`createMemoizedSelector` 段注释同步补「在其上传 `a === b` 得到永不命中的缓存，要只比引用请改走 createSelector(…, snapshotState:false)」。
判据: 口径来自 `src/types/selector.ts`（`snapshotState` 默认 true + 「仅当同时置 false 才该用引用相等」）与
`src/extras/selector/createSelector.ts:391-394` 的成对示例、`:417-420` 的「不暴露 snapshotState」。
删除成员即空锁的问题此前成立（8 份类型测试无一断言该成员），现已断言。
文档: none；尾巴：`tests/types/selector-generic-defaults.typecheck.ts:84` 的 `equalityFn: (a, b) => a === b` 同样没配
`snapshotState: false`，该文件不在本分片。

## R6-112 low documentation tests/types/store-config-base.typecheck.ts:L89-L99
verdict: FIXED
改动: ① 小节标题改为「免泛型裸写法（S 取默认 `Record<string, unknown>`）」，解说改为「本节钉的是 S 的**默认值**」，
并显式撤掉旧注释的越权 claim（`bareForm` 走 `satisfies StoreConfig` ＝默认值，`ConfigState<Record<string, unknown>>` 归一成功，
根本不进 `types/store.ts:52` 的兜底分支）；② 新增一节 `ConfigState` 退化兜底锁：
`const _degenerateGetterReadsByKey: StoreConfig<unknown>['getters'] = { double: (state) => Number(state.count) * 2 }`
（兜底漂成 `object` 时当场报错）+ `Equal<Parameters<NonNullable<StoreConfig<unknown>['getters']>[string]>[0], Record<string, unknown>>`
与 `Equal<…, object> = false` 双向精确断言；③ 文件头「锁定行为」清单补上这两条的分工；④ 本文件补 `Equal` helper（原文件没有）。
判据: 实跑复核：`StoreConfig<S = Record<string, unknown>>`（src/types/store.ts:287），
首轮我写的 `DegenerateGetters[string]` 因 `getters?` 含 `undefined` 报 TS2537、并让两条 Equal 退化成 `any`（假绿），
已改 `NonNullable<…>[string]`；`npx tsc --noEmit -p tsconfig.tests.json` 对本文件零报错。
文档: none

## R6-113 low documentation tests/utils/createTestStore.ts:L10-L26
verdict: FIXED
改动: 把「让测试工厂与真实 API 在同一处对齐」改写为「对齐的**只是 `state` 的必填性**，不含形状接受面」，
并写清不对称的方向与后果：带形参的 state 工厂在本工厂报 TS2322（附错误文案形状），公开入口 `createStore` 今天不拒；
真正该收紧的是 `createStore` 侧（类型谎称 state 是函数、运行时 `_initializeState` 按无参调用工厂 ⇒ `{ count: undefined }`），
故**不得**把工厂放宽去「对齐」。
判据（命令级）: 临时探针 `tests/types/tmp-probe-r6f110.typecheck.ts`（tsconfig.tests.json 同一份 compilerOptions，跑完已删）——
`createStore({ state: (seed: number) => ({ count: seed }) })` 零报错；
`createTestStore({ state: (seed: number) => ({ count: seed }) })` 报
`TS2322: Type '(seed: number) => { count: number; }' is not assignable to type '({ count: number; } | (() => { count: number; }) | undefined) & ({ count: number; } | (() => { count: number; }))'`
⇒ 报告的两条偏差方向均复现，判 FIXED 而非 FP。运行时侧 `_initializeState` 无参调用工厂实测于 `src/core/store/Store.ts:949-951`。
文档: none（注释类，无对外行为；若将来收紧 `createStore`，属 src 行为变更，另记 CHANGELOG）

---

## 跨片/NEEDS-MAIN 尾巴（本分片权限外，需主会话接力）
1. **R6-063 的 `data` 半**：`withPageStore` / `withComponentStore` 的返回类型（`src/integrations/with-store.ts`，Page 在 :150 附近、
   Component 在 :470 附近）交叉了 `{ data: D & ExtractPageData<S,O,G> }`，而运行时那份 `data` 只是 `D`。
   建议改法：返回类型收成 `{ data: D }`（映射值只出现在 `PageThis`/`ComponentThis` 的实例 `this.data`），
   再在 `tests/types/integration-types.typecheck.ts` 把 `PgCfg['data']` 的断言按 `D` 收紧。`src/integrations/**` 不在本分片。
2. **R6-109 的形状反例锁**：`tests/types/action-context-collision.typecheck.ts` 不在本分片，需该分片补
   `interface MyActions extends Actions { getState(): number }` + `ActionContext<{count:number}, MyActions>` 下
   「`ctx.getState()` 返回 S 而运行时返回 action 值」的现状锁（注释已落地在 `src/types/store.ts`，可直接引用其措辞）。
3. **R6-111 的另一处**：`tests/types/selector-generic-defaults.typecheck.ts:84` 的 `equalityFn: (a,b)=>a===b` 需同样配
   `snapshotState: false`（或就地注明只断言可赋性）。
4. **R6-061 的删除决策**：若要在 0.7.0 真删 `ComposeOptions.lazy/tree` 与 `NamespaceConfig`，需同步
   `src/core/index.ts:80`、`src/core/compose/index.ts:7`、`src/core/compose/composeStore.ts:958` 三处再导出、
   `docs/API.md` 与 SKILL 文案（现在 SKILL 已按「已移除」写，与代码相反）。

## 自验（收工前实跑）
```
npx tsc -p tsconfig.typecheck.json --noEmit          # src 面：src/types/{compose,integration,store}.ts 零报错
                                                     # 但全库当前（并行分片在改）另有 4 处非本分片报错，见下
npx tsc --noEmit -p tsconfig.tests.json              # 过滤本分片 4 个文件：零报错
npx eslint --max-warnings 0 <7 个改动文件>            # EXIT=0
npx prettier --check <7 个改动文件>                   # 4 个 CRLF 文件被 warn；见下方判据＝行尾工件、非内容问题
npx jest tests/integration/auto-inject.test.ts tests/integration/with-app-store.test.ts --ci
                                                     # Test Suites: 2 passed / Tests: 41 passed
npx jest tests/unit/regression/ocr-medium-wave.test.ts --ci
                                                     # Test Suites: 1 passed / Tests: 45 passed
                                                     # （含 R6-065 引用的「#153 cacheKeys 为空数组…不缓存任何键」运行时锁，
                                                     #  证明本轮改注释没有把默认语义往注释旧文案上挪）
本分片无新增/改动的 jest 收集用例（7 个文件全是类型面/注释面），故未建 tests/unit/r6-f1-10-*.test.ts：
类型面修复的回归锁按仓库既有口径放 `tests/types/*.typecheck.ts`（见 R6-063 / R6-111 / R6-112 三条），
R6-063 的锁做过「旧实现先红」实跑；R6-061/062/064/065/109/110/113 为纯注释/文档修复，按说明书只需说明、不加锁。

```
- **行尾说明**：`git config core.autocrlf=true`，工作树 167 个 `.ts` 是 CRLF；未改动的
  `src/core/store/Store.ts`、`examples/basic/index.ts` 同样被 `prettier --check` warn。
  把 `npx prettier <file>` 的输出与本文件都做 LF 归一后 `diff` 为**空**（integration.ts / store.ts /
  integration-types / createTestStore 四份都验过）⇒ 本分片内容符合 prettier 口径，无需 `--write`（那会整文件换行、diff 膨胀）。
- **全库 src 类型门禁当前不干净（非本分片造成，供主会话对账）**：
  `src/core/performance/__r6old_probe.test.ts`（TS2307 ×2，别处的临时探针没删）、
  `src/core/store/ActionManager.ts:86`（TS6133 `_getMutationCount` 未使用）、
  `src/extras/action/decorators/cache.ts:172`（TS2352）、
  `tests/unit/r6-f1-04-error-shape.test.ts`（TS2339 ×2 + TS2345）、
  `tests/unit/extras/ocr-low-round4-p2.test.ts:300`（TS2741）、
  `tests/unit/r6-f1-06-clone.test.ts:49`（TS2345/TS2554，本分片开工前的基线上就有）；
  另有 `examples/weapp/app-integration.ts` 报 `Cannot find name 'App'`——我开工时 tsconfig.tests.json 的 include 不含 examples，
  现含，疑为并行分片改了 include，需主会话确认是否刻意。
