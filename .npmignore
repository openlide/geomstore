# ⚠ 发布面的唯一来源是 package.json 的 "files" 白名单：npm/pnpm 在 files 存在时完全不读本文件。
# 实测（npm pack --dry-run --ignore-scripts）当前发布面 = 213 文件：
#   dist/**（纯 ESM 产物 + .d.ts，sourcemap 已由 postbuild 剔除）
#   + CHANGELOG.md、README.md、package.json（后两者 npm 无条件附带）
#   + store/ hooks/ plugins/ integrations/ compose/ selectors/ snapshot/ performance/
#     actions/ cache/ error/ 等子路径 stub 目录（由 prepack 生成、postpack 清理，故 dry-run 时缺席）
# 所以「仅 dist」并不准确，本文件也不是发布清单。
# 下列规则只有一种情形下生效：files 字段被删除或放宽时的兜底排除（含类型解析说明：
# 类型完全由 dist 内 .d.ts 经 exports 映射提供，src 不随包发布）。
src/
tests/
docs/
examples/
scripts/

# Config files
tsconfig*.json
jest.config.js
eslint.config.js
.prettierrc*

# Development files
.gitignore
.github/

# Documentation
*.md
!README.md
!CHANGELOG.md

# Test files
**/*.test.ts
**/*.spec.ts

# Build artifacts
*.tsbuildinfo

# 兜底：凭据 / 本地数据 / 生成物（防 files 白名单被误删后整仓外泄）
.npmrc
.env*
coverage/
coverage-report.json
coverage-*.json
.vscode/
.idea/
.codebuddy/
.ocr-fix/
*.log
.DS_Store
