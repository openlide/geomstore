# f2-12（wave f2）

条数：11｜FIXED 11 / FP 0 / REJECT 0 / NEEDS-MAIN 0（另有 4 条跨分片配合项记在文末「需要主会话/他片配合」）

涉及文件（本片只动这四个）：`examples/cache/01-basic-cache.ts`、`packages/benchmark/src/runner.ts`、`src/core/compose/composeStore.ts`、`src/core/store/Store.ts`。
已按任务要求先读 `.ocr-fix/verdicts6/hot-p0.md`，hot 层（R6-001/003/004/005/007）判据一律沿用：`setState` 的 `PROTO_SENSITIVE_KEYS` 仍走 `defineOwnProperty`（本片只把它那段等值取读的三元式提成同判据的 `readOwnValue`，语义逐字不变，`hot-round6` R6-007 两例仍绿）；三条读路径仍经 `_readablePick`（本片把它的「死店 → 空视图 + 一次性告警」抽成 `_skipDestroyedChild(store, note)` 供缓存 API 复用，读路径文案一字未改）；benchmark 第 4 判定项仍只在 `scenario.cacheConfig !== undefined` 时计入。
回归锁新建三份，未触碰 `tests/unit/hot-round6.test.ts`。

## R6-069 low examples/cache/01-basic-cache.ts:L11-L12
verdict: FIXED
改动: 注释口径改正——`cacheConfig.enableStats` 默认就是 true（Store.ts:243 / LRUCache.ts:132 / types/store.ts 字段注释同一口径），显式传 `true` 不打开任何东西；真正「按需」的方向是性能敏感时传 `false`，并写明关掉后 `getCacheStats()` 的 hits/misses 恒为 0、键列表与体积统计不受影响。代码零变更（`enableStats: true` 保留为显式声明）。
回归锁: none —— 纯注释修正，库行为未变；默认值的运行时速测见判据。
判据: `grep -n "enableStats" src/core/cache/LRUCache.ts src/types/store.ts` → LRUCache:132 `config.enableStats ?? true`；`grep -n "options.enableStats" src/core/cache/LRUCache.ts` → 216/228/325 三处计数器都在这道门后。
文档: none（示例自身即文案）

## R6-024 medium packages/benchmark/src/runner.ts:L79-L81
verdict: FIXED
改动: runner 侧补上「成员存在却不读取」的识别。新增 `BenchmarkRunner.probeCacheRead(store)`：建店后（**不在**被计时的循环里）跑一次 `getCached(key)`，若 `getCacheStats()` 的 `hits+misses` 没有前进就判定为没有读取发生，`readKey` 的缓存读分支据此退化为真实状态读（新增第 4 参 `useCacheRead`），并在该场景结果上挂 `warnings` 说明退化原因与契约要求。store 不暴露 `getCacheStats` 时无从证伪，按契约信任该成员存在（返回 true，不产生噪声告警）。
回归锁: tests/unit/r6-f2-12-benchmark-runner.test.ts > R6-024 getCached 成员存在但不读取时退化为真实状态读（2 例：空转成员被识别并出告警；真缓存 store 不被误判）
判据: 同一份「假缓存 store」输入分别喂给 HEAD 版与工作树版 runner（`.cache/r6-f2-12-old/probe.mjs`，两包各 `tsc` 出产物后同脚本对跑）——`OLD: {"warningCount":0,"cacheTotal":0}`（空转被当成一次读取计时，报告里毫无痕迹）/ `NEW: {"warningCount":1,"cacheTotal":0}`。根因侧 `packages/benchmark/src/index.ts:78`（恒存在的 `getCached` 箭头函数）已由 f1-03 的 R6-022 修成按存在性动态解析，本片守的是外部 harness 与手写包装这条仍敞开的入口；`pnpm bench` 的 5b 无缓存用例仍绿（成员缺席 ⟹ 探针直接判 true，走原有退化路径）。
文档: 行为变更（报告面）——`BenchmarkResult.warnings` 在「store 有 getCached 却不产生可观测访问」时会多一条说明；CHANGELOG 记一句「基准不再把空转的缓存读当成真实读取」。

## R6-025 medium packages/benchmark/src/runner.ts:L122-L152
verdict: FIXED
改动: 三份累加状态（`results` / `thresholdIssues` / `startTime`）从实例字段改成**每次运行的局部对象** `BenchmarkRun`，由 `runAll` 创建、经 `runScenario(scenario, run)` / `generateReport(run)` / `generateRecommendations(run)` 串接；实例字段删除，`runAll` 不再有可被第二次调用抹掉的共享可变态，两次交叠的运行互不可见（报告只引用本次运行的样本）。
回归锁: tests/unit/r6-f2-12-benchmark-runner.test.ts > R6-025（2 例：同一实例并发 runAll 各自 2 条目、条目名不穿插；先后两次 runAll 都从空数组起步）
判据: HEAD/工作树对照——`OLD: a=["x-1","x-1","x-2","x-2"] (aTotal=3) / bTotal=4`（正是报告描述的「一份被抹、一份被灌进两轮混合值」）；`NEW: a=b=["x-1","x-2"]，aTotal=bTotal=2`。锁文件在旧实现上实测为红（`.cache/r6-f2-12-old` 对照跑）。
文档: 修复（无 API 变化，`runAll` 从此可重入/可并发）

## R6-026 medium packages/benchmark/src/runner.ts:L200-L248
verdict: FIXED
改动: ①每个场景取 `initialMemory` 之前先 `benchmarkUtils.forceGC()` 重新定基线（与 `measureMemory` 同一口径，且不受 `general.skipGC` 管辖——那个开关只管整轮开始前那次统一预热）；②内存增量改为 `peakMemory − 本轮样本下界`（新增 `lowWaterMemory`，逐轮取小），脏基线不再把 delta 夹成 0；③顺带去掉每轮重复的第二次 `getMemorySnapshot()`（原 `memSnapshots` 与 `memoryAfter` 各采一次），样本改为 `number[]`。注释按 ①②两个方向重写，并诚实记下残留：无 `--expose-gc` 时 `forceGC` 是 no-op、下界退化成基线，此时增量只能当方向参考（与 `measureMemory` 的注释同一结论）。
回归锁: tests/unit/r6-f2-12-benchmark-runner.test.ts > R6-026（2 例：`process.memoryUsage` 喂「基线 10MB、样本全低于基线」的脏基线序列 ⟹ delta = 2MB 而非 0；堆完全不动 ⟹ delta 仍为 0）
判据: 对照探针 `OLD: {"initial":10000000,"peak":10000000,"delta":0}`（假绿：真实摆动 2MB 被读成 0 字节，`delta <= perStore × 档位倍数` 无条件通过）/ `NEW: {...,"delta":2000000}`。`pnpm bench` 冒烟里 quick-slow-write 的内存档从此有牙齿（该场景报告打出「峰值内存增量 1.67 MB 超过门限 48.83 KB」）。
文档: 行为变更（口径）——`BenchmarkResult.results.memory.delta` 的定义由「峰值减基线」改为「峰值减本轮样本下界」，跨轮次的历史报告数值不可直接对比；CHANGELOG 需写明并说明 `initial/peak/final` 三列不变。

## R6-027 medium packages/benchmark/src/runner.ts:L295-L316
verdict: FIXED（含一处跨分片尾巴，见文末配合项 2）
改动: ①`datasets[size].actions` 真的建出动作注册表——新增 `createActionRegistry(count)`，不再采用工具类硬编码的 `Math.min(stateKeys, 20)`（那份与配置里的 small=5 / xlarge=100 都对不上，dispatch 档门限判的是一个没人配置过的动作集）；②`datasets[size].subscribers` 真的挂上监听器——`runScenario` 按配置数 `store.subscribe` 空回调，场景收尾在 `finally` 里先逐个退订再 `destroy()`（`thresholds.memory.perSubscriber` 从此有对照物）；③`createTestStore` 收整个 dataset 配置并把此前被直接丢弃的 `storeConfig.getters` 下发给 store 工厂（runner 的 `createStore` 签名新增可选 `getters`）；④两个计数在测量前校验（非非负整数 ⟹ 抛错计入 errors，与 `stateKeys`/`capacity` 同一口径）；⑤新增 `unmeasuredConfigNote()`：本轮确实没测量的维度（`scenario.concurrency`、`datasets[*].getters` 的读取档、`datasets[*].nestingDepth`，以及无产出物可比的 getter 门限）在 `report.recommendations` 里逐条点名，报告不再宣称它测过。
回归锁: tests/unit/r6-f2-12-benchmark-runner.test.ts > R6-027（2 例：actions=3/stateKeys=4 时建店动作数是 3、dispatch 只点到 action0..2、subscribe/unsubscribe 各 2 次；声明 concurrency 时报告点名且不含已测量的 subscribers）
判据: 对照探针 `OLD: {"actionCount":4,"subscribed":0,"unsubscribed":0,"dispatched":["action3"],"note":false}` / `NEW: {"actionCount":3,"subscribed":2,"unsubscribed":2,"dispatched":["action0","action1","action2"],"note":true}`。并发档 `concurrent-access` 已由 f1-02 的 R6-019 从默认场景集删除，本片不恢复。
文档: 行为变更（建店形状 + 报告面）——基准 store 的动作数/订阅数改由配置决定，历史基准数字与新的不可比；`report.recommendations` 可能多一条「声明值未测量」的点名行。CHANGELOG 记「基准 runner 接进 datasets.actions/subscribers 两个维度，并停止把未测量配置当成已测」。

## R6-028 medium packages/benchmark/src/runner.ts:L399-L406
verdict: FIXED
改动: `runBenchmarkIteration` 用新增的本地 `isThenable` 判 dispatch 返回值，是 thenable 时返回 `asyncTail: Promise<number>`（续段实测毫秒数），由 `runScenario` 的循环 `await` 后并入本轮 duration 并在续段之后补采一次内存。热循环对同步库**零额外成本**（同步路径不产生 promise、函数体仍是同步，方法注释同步改口径）。三条后果一并消掉：异步段计入被计时区间、reject 冒泡成场景错误而非 floating rejection、续段跑完才 `store.destroy()`（不再违背 `BenchmarkStore.destroy` 的生命周期契约）。
回归锁: tests/unit/r6-f2-12-benchmark-runner.test.ts > R6-028（2 例：30ms 异步动作 ⟹ `executionTime.avg >= 5ms` 且事件序为 resolve→destroy；reject ⟹ `errors` 带原因、`unhandledRejection` 计数为 0）
判据: 对照探针 `OLD: {"avgMs":0.051,"events":["destroy","resolve-after-destroy"]}` / `NEW: {"avgMs":10.329,"events":["resolve-before-destroy","destroy"]}`；reject 一例 `OLD: {"errorCount":0,"hasMessage":false,"unhandled":["动作里炸了","动作里炸了"]}` / `NEW: {"errorCount":1,"hasMessage":true,"unhandled":[]}`。
文档: 行为变更——异步动作的耗时/吞吐档数值会显著变大（此前只量到同步前缀）；CHANGELOG 需写明。

## R6-073 low packages/benchmark/src/runner.ts:L418-L420
verdict: FIXED（同时闭环 f1-03 台账的 NEEDS-MAIN #1）
改动: `createErrorResult` 把 `scenario.datasetSize` 传给 `ResultBuilder.createErrorResult`（f1-03 已给该函数加好可选末参），`iterations` 改记 **0**（实测迭代数），并补一条 `warnings`：「场景声明 N 轮，实测完成 0 轮（进入测量前或测量中抛错，全部指标为空）」。
回归锁: tests/unit/r6-f2-12-benchmark-runner.test.ts > R6-073（1 例：large 档 12345 轮的场景失败后 `datasetSize === 'large'`、`iterations === 0`、warnings 带声明轮数）
判据: 对照探针 `OLD: {"datasetSize":"xlarge","iterations":12345,"warnings":null}`（旧按迭代数反推档位，且读者会读成「测了 12345 轮都很快」）/ `NEW: {"datasetSize":"large","iterations":0,"warnings":["场景声明 12345 轮…"]}`；`pnpm bench` 第 4 段（非法 stateKeys）与 6b 段断言全绿。
文档: 行为变更（报告面）——失败结果的「数据集规模」与「迭代次数」两列含义修正；f1-03 已记的那条 ResultBuilder 变更现在才在包内生效。

## R6-034 medium src/core/compose/composeStore.ts:L132-L135
verdict: FIXED
改动: ①构造期新增路由键合法性校验（与重名校验同一位置、同一档判据）：`store.name` 必须是「非空且不含 '/'」的字符串，命名空间模式下不满足直接抛错（含 '/' 时 `"user/info/count"` 会被解析成 store `'user'` + 键 `'info/count'`，该子店在 setState/getCached/dispatch/getter 上永远路由不到，而合并视图又读得到——读写分裂且全程静默），平铺模式按 `_mergeStateMaps` 的既有口径只在开发模式告警；②`this.stores[store.name] = store` 与 `createStoreTree` 的 `children[store.name] = …` 改为 `defineOwnProperty`（`../utils/helpers.js` 导出的那份），堵掉 `__proto__` 名走 [[Set]] 时「条目不成为自有键 + stores 原型被换成 Store 实例」的公开方法泄漏与嵌套路由失效。
刻意**不**拒绝 `'__proto__'`：本仓既定判例是「store 名可合法为 `'__proto__'`」（merge.ts 的 `assignMerged` 与 StoreRegistry.createSnapshot 的注释都这么写并为此加了 DefineOwnProperty 守卫），故那两处漏网写点补齐守卫，而不是把名字判成非法。
回归锁: tests/unit/r6-f2-12-compose-cache-api.test.ts > R6-034（4 例：命名空间含斜杠抛错、平铺只告警、`'__proto__'` 自有键 + 原型未被换 + 路由仍可用、createStoreTree children 同口径）
判据: 旧 src 图上跑同一份锁文件为红——`npx jest --config .cache/r6-f2-12-old/jest.src-old.config.cjs tests/unit/r6-f2-12-compose-cache-api.test.ts` → `Tests: 15 failed, 2 passed`（R6-034 四例全红，症状即报告描述的 `composed.stores.getState` 变可调用 / 含斜杠名静默不可路由）。
文档: 行为变更（构造期校验）——命名空间模式下名字含 '/' 的子 store 由「静默不可路由」改为抛错；CHANGELOG 与 GUIDE 的组合章节需写明，并补一句 `stores` 映射对 `'__proto__'` 名的承载方式（自有数据属性）。

## R6-035 medium src/core/compose/composeStore.ts:L773-L825
verdict: FIXED
改动: 缓存 API 与同类方法对齐命名空间路由。①`enableCache(keys)`：命名空间模式下逐个键经 `findTargetStoreWithKey` 解析 `storeName/key` 后**只**投递给归属 store（原先原样透传给每一个子店 ⟹ 带前缀的键谁也不匹配、缓存静默不生效，不带前缀的键又越权开启未点名的 store）；无归属键按 strict 口径处理（strict 抛 `Cannot find store for key`、非 strict 开发模式告警后忽略）；`keys === undefined` 仍是「各子店缓存自己的全部顶层键」，平铺模式保持广播（子店只缓存自己拥有的键）。②`getCacheStats().keys`：命名空间模式回填 `storeName/key` 形式，使 `composed.getCached(composed.getCacheStats().keys[0])` 不再是恒 undefined；平铺模式按键去重（合并视图里一个键只有一份）。③`enableCache` / `disableCache` / `invalidateCache` / `getCacheStats` 四个入口统一改用新的 `_skipDestroyedChild(store, note)` 判据跳过已独立销毁的子店（一次性告警），`_readablePick` 同时改为复用它（读路径文案与「每店只告警一次」的语义逐字不变，hot R6-005 的三条锁仍绿）。
回归锁: tests/unit/r6-f2-12-compose-cache-api.test.ts > R6-035（5 例：只投递归属店、keys 可喂回 getCached、无归属键的 strict/非 strict 两态、平铺去重、死店跳过且只告警一次）
判据: 旧 src 图上同一份锁为红（同上命令，R6-035 五例全红）。
文档: 行为变更——组合层 `getCacheStats().keys` 的元素形状改变（命名空间模式带前缀、平铺去重），`enableCache` 不再向未点名的子店扩散；需写进 CHANGELOG 与 GUIDE 缓存/组合章节。

## R6-079 low src/core/compose/composeStore.ts:L426-L437
verdict: FIXED
改动: `get getters()` 对鸭子类型/桩子 store 兜底——`store.getters` 缺席（falsy）时按「无 getter」跳过该店，与同文件 `getter()`（`s.getGetterNames ? … : []`）、`getGetterNames()`、构造期的 `store.actions ?? {}` / `if (!childHooks) continue` 同一条降级判据，并把理由写进注释（`composed.getters` 是 devtools / analyzer 的只读反射面，插件 `src/plugins/builtin.ts:405` 同样按 `Object.keys(store.actions)` 读形状）。键的合并规则未动（命名空间前缀 + 同名取第一个，与 `getter()` 的路由语义本来就一致）。
回归锁: tests/unit/r6-f2-12-compose-cache-api.test.ts > R6-079（2 例：只有无 getters 的桩店 ⟹ 不抛且返回 `{}`；混合场景真实子店的 getters 照常带前缀合并且 `getter()` 可求值）
判据: 旧 src 图上两例皆红（同一命令）；触发输入即仓库自己的桩件形状（`tests/unit/core/compose/composeStore-degraded.test.ts` 的 `fakeChild` 就没有 `getters`）。
文档: none（纯修复；对外只是「不再在只读路径上抛 TypeError」）

## R6-086 low src/core/store/Store.ts:L385-L400
verdict: FIXED
改动: `$patch` 与 `setState` 的「改没改」判据合流。新增模块级 `readOwnValue(target, key)`（敏感键按**自有属性描述符**取值，其余按 [[Get]]），`$patch` 逐顶层键 `Object.is` 比对，等值键整键跳过；`effective` 为空 ⟹ 不 `deepMerge`、不推进 `_mutationCount`、不标脏、不写缓存、不调度通知，只成对触发 `beforePatch`/`afterPatch`（与 setState 的等值早退同形）。合并/别名脏键采集改为只喂变化键集（`mergedInPlace`、`_markAliasedKeys` 的键集同步收窄），`deepMerge` 的克隆语义与「写入即定格」契约不变。顺带把 setState 里那段等价但重复的三元式换成同一个 `readOwnValue`（热判据不变，且消除了 3 处 non-null assertion 中的 2 处）。
只比顶层、不下探：同内容不同引用的嵌套对象照常合并与计数（注释写明）。
回归锁: tests/unit/r6-f2-12-store-patch-atomicity.test.ts > R6-086（6 例：onlyOnChange 下等值/空补丁 0 次通知而真补丁 1 次、默认配置下同样不再广播、逐键脏标记粒度（监听器内读）、嵌套同内容仍算变更、钩子成对触发、新增键不被误吞）
判据: 旧 src 图上同一文件 `Tests: 15 failed, 2 passed`，其中 R6-086 前 4 例即报告实测的「`$patch({})` 也通知一次」那条反差；实测三态（工作树，`dispatch('viaPatch')` / `viaPatchEmpty` / `viaSetState` 通知数均为 0，`viaRealPatch` 为 1）。
文档: **行为变更（不是纯修复）**——`$patch({})` 与「补丁值与当前状态逐字相同」以前会计一次变更、标脏全部键、写一遍缓存并调度通知，现在全部不做（与 `setState` 一致）。CHANGELOG 需单列一条，`notify.onlyOnChange` 的契约文档可去掉「仅 setState 短路」的暗示，`$patch` 的 API 说明需补「等值顶层键短路」。

---

## 需要主会话/他片配合（跨分片，本片未动他人文件）

1. **同一段错误注释还在两个缓存示例里**（R6-069 的同一判据）：`examples/cache/02-selective-cache.ts:15`、`examples/cache/03-dynamic-cache.ts:33` 各自带着「采集缓存统计…按需开启」+ `enableStats: true`。02 归 f1-01、03 归 f1-02（见 groups6 头部涉及文件），请两片按 01 的口径改：「统计默认已开启，此处只是显式声明；性能敏感场景传 false 才会关掉 hits/misses」。
2. **R6-027 的另一半（getter / concurrency / nestingDepth 三档要真接进测量就得走出 runner）**：新增第 5 个被计时槽会改 `runBenchmarkIteration` 的 `index % 4` 轮转，而 `packages/benchmark/src/smoke.ts` 的 5c 段整段推导（`SLOW_WRITE_MS/4 ⟹ 0.25ms ⟹ 约 4000 ops/s`、区间 (2000,10000)）是按四槽写的；`scenario.concurrency` 要生效需把 `benchmarkUtils.parallel` 接进 `runScenario`（f1-02 的 R6-019 注释已指路）；`datasets[*].getters`/`nestingDepth` 若要删掉得改 `packages/benchmark/src/types/index.ts`、`config.ts`、`constants.ts` 的 `GETTER_*`/`SUBSCRIBE_*` 门限表。以上文件都在 f1-02 / f1-03 名下，本片只在报告里点名，未代改。请主会话定一个方向（补齐测量 or 删档），今天这版报告是「点名后的诚实态」。
3. **`packages/**` 不受根 eslint 约束**（f1-01 把 `packages/**` 加进了 eslint.config.js 的 ignores）：本片按 f1-03 的先例用 `npx eslint --no-ignore --max-warnings 0 packages/benchmark/src/runner.ts` 自查，0 问题。
4. **全库 prettier 的 endOfLine 与 CRLF**：`.prettierrc.json` 现为 `endOfLine: "lf"`，而 `core.autocrlf=true` 使工作树里既有文件普遍是 CRLF ⟹ `prettier --check` 对任何被改过的既有文件都报「整文件差异」（本片三个既有文件同样中招，但 LF 归一化后与 prettier 输出逐字节相同，实测差异行数 0，见自验）。新增的三份测试文件已 `prettier --write` 成真 LF、`--check` 通过。要不要统一 `prettier --write` 请主会话最后收尾时定（与 f1-01/f1-03 的同一尾巴合并处理）。

## 本分片自验（实跑）

```
$ npx jest tests/unit/r6-f2-12-compose-cache-api.test.ts tests/unit/r6-f2-12-benchmark-runner.test.ts \
    tests/unit/r6-f2-12-store-patch-atomicity.test.ts tests/unit/hot-round6.test.ts \
    tests/unit/core/compose tests/unit/core/store --ci
  Test Suites: 9 passed, 9 total
  Tests:       335 passed, 335 total            # 三份新锁共 28 例全绿（17 src 侧 + 11 benchmark 侧）；hot-round6 16 例未破

$ npx jest tests/unit/r6-f2-12-store-patch-atomicity.test.ts tests/unit/r6-f2-12-compose-cache-api.test.ts --ci
  Tests: 17 passed, 17 total                    # src 侧新锁计数
$ npx jest tests/unit/r6-f2-12-benchmark-runner.test.ts --ci
  Tests: 11 passed, 11 total                    # benchmark 侧新锁计数

# 「先失败后通过」取证（一次性夹具，已删除）：git archive HEAD src packages/benchmark 解到 .cache/r6-f2-12-old
$ npx jest --config .cache/r6-f2-12-old/jest.src-old.config.cjs tests/unit/r6-f2-12-store-patch-atomicity.test.ts \
    tests/unit/r6-f2-12-compose-cache-api.test.ts --ci
  Tests: 15 failed, 2 passed, 17 total          # R6-034/035/079 全红；R6-086 四条红（`Expected: 0 Received: 2` 等），
                                               # 通过的 2 例正是刻意保留的正向控制：「钩子成对触发」与
                                               # 「新增键不会被等值早退误吞」（早退不得把手也砍掉）
$ node .cache/r6-f2-12-old/probe.mjs            # 同一份输入喂 HEAD 版与工作树版 runner
  R6-026 delta 0 ⟶ 2000000｜R6-073 xlarge/12345/无 warnings ⟶ large/0/带 warnings
  R6-025 两轮报告 3、4 条目互串 ⟶ 2、2 条目
  R6-028 avgMs 0.051 + destroy 先于 resolve ⟶ 10.329 + resolve 先于 destroy；reject 两条 unhandledRejection ⟶ 0 条且 errors 带原因
  R6-024 warningCount 0 ⟶ 1｜R6-027 actionCount 4/subscribed 0/note false ⟶ 3/2/true

$ npx jest tests/unit tests/integration --ci --silent
  Test Suites: 2 failed, 177 passed, 179 total
  Tests:       2 failed, 3720 passed, 3722 total
  # 2 条红与本分片无关且已归因：
  #   tests/unit/store/action-dirty-keys.test.ts「accumulates dirty keys across async actions and async notifications」
  #     —— 该文件通体不含 `$patch`（grep 命中 0），且在 HEAD 的 src 图上同一用例（onlyOnChange 两态）也是红的
  #   tests/unit/plugins/builtin.test.ts PERSIST-COVER-018 —— plugins/builtin.ts 归 f1-09
  # 上一轮还红的 ocr-low-round4-p2 #322（clone-async）与 enterprise-app-lifecycle 在收工前已自行转绿（f1-08 交回）

$ npx tsc --noEmit -p tsconfig.json                        # src：本片两文件 0 诊断
  # 唯一残留报错在 src/extras/snapshot/clone-async.ts(295,71)（他片在途文件，非本片引入）
$ (cd packages/benchmark && npx tsc --noEmit)              # 0 诊断
$ node dist/smoke.js                                        # ok 计数 60、FAIL 0、末行「全部通过」、退出码 0
$ npx eslint --max-warnings 0 src/core/{compose/composeStore.ts,store/Store.ts} examples/cache/01-basic-cache.ts \
    tests/unit/r6-f2-12-*.test.ts                            # 0 问题（含 hot 层留下的 1 处 non-null assertion 一并消掉）
$ npx eslint --no-ignore --max-warnings 0 packages/benchmark/src/runner.ts   # 0 问题
$ npx prettier --check tests/unit/r6-f2-12-*.test.ts        # All matched files use Prettier code style!
$ npx prettier --check src/core/compose/composeStore.ts src/core/store/Store.ts examples/cache/01-basic-cache.ts
  # 3 份报红全部是 CRLF vs `endOfLine:"lf"` 的整文件行尾问题（见配合项 4）：LF 归一化后 diff 行数 0
$ git status --porcelain | grep -E "examples/cache/01|packages/benchmark/src/runner|src/core/compose/composeStore|src/core/store/Store|tests/unit/r6-f2-12"
  M examples/cache/01-basic-cache.ts / M packages/benchmark/src/runner.ts / M src/core/compose/composeStore.ts
  M src/core/store/Store.ts / ?? tests/unit/r6-f2-12-benchmark-runner.test.ts / ?? tests/unit/r6-f2-12-compose-cache-api.test.ts / ?? tests/unit/r6-f2-12-store-patch-atomicity.test.ts
  # 改动面严格落在分片允许的 4 个文件 + 3 份新锁；`.cache/r6-f2-12-old*`、`.cache/fmt`、`.cache/r6-f2-12-*.txt` 探针已删除
```
