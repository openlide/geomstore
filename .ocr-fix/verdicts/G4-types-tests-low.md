# G4-types-tests-low 判定记录

#384 | fix | 成立：if/else 链尾分支同时接 'info' 与「一切未知值」，新增 ErrorLevel 成员会静默降级成 console.info。改为 switch，五个级别逐个登记，default 带 `const _exhaustive: never = level` 穷尽性守卫；实测给 ErrorLevel 临时加 `'temp-probe'` 后 `npx tsc --noEmit -p tsconfig.typecheck.json` 报 `src/types/error.ts(120,13) TS2322: Type '"temp-probe"' is not assignable to type 'never'`（守卫确实生效，探针已撤）。运行时输出逐字不变。回归用例 tests/unit/types-interface-state.test.ts #384（契约外级别按 info 出声 + warn/critical 通道不变）。
#386 | reject | 双写属实，但报告两条修法都不可采用：「工厂不写 timestamp」会破掉禁改用例 `tests/unit/core/error/ErrorHandler.test.ts:417-425`（「应该生成时间戳」断言 before/after 区间的 `typeof === 'number'`），且属公开 API 的运行时行为变更（low 项不引入）；「删采集器兜底」要改 src/extras/error/ErrorAggregator.ts:58（禁改目录）。保留现状，在 `createErrorContext` 的 JSDoc 写明以工厂为准、采集器那处只是手搓 context 的兜底，彻底收口列待办。
#387 | reject | 快照语义属实，但报告的修法打不到公开面：`types/performance.ts` 的 `PerformanceMonitor` 接口根本没随包发布（`src/core/performance/index.ts:11`、`src/extras/index.ts:37` 只导出 PerformanceMetrics/Options/Stats/MetricType + 实现类），消费者看到的是类签名 `PerformanceMonitor.ts:388 getMetrics(): PerformanceMetrics[]`（禁改目录）。只把接口改 readonly 对外零收益、还与类分叉。改为在接口上把「返回快照、不得交出内部数组」写成契约，并如实记下两个实现的差异（PerformanceMonitor 逐元素复制；MetricsCollector.getAll 只复制数组、元素共享）。统一收紧（接口+类+`PerformanceAnalyzer.analyzeBottlenecks` 入参）列待办。
#388 | reject | 「无界增长」不成立：byOperation 由 `metrics` 派生，而 `PerformanceMonitor.ts:365` 按 `maxSize`（默认 1000）溢出即 shift，分组键数被同一上限约束。「补两个字段」实测破禁改实现：加 required 后 `npx tsc --noEmit -p tsconfig.typecheck.json` 报 `src/core/performance/metrics.ts(74,5) TS2322 ... missing: minDuration, thresholdExceeded`（`computePerformanceStats` 的累加器根本不产出这两项），声明出来就是运行时不存在的成员。保留现状，在 byOperation 的 JSDoc 写明差异缘由与实测错误，实现侧补齐列待办。
#403 | fix | 属实：契约面一字未提 emit 的失败语义。已在 `IHookSystem.emit` 的 JSDoc 写全四条（逐 handler try/catch、不中断其余处理器、不向调用方传播、console.error + 转投 onError 且 onError 自身不递归），并指明「要让异常冒泡到业务方请走 action 错误边界」。所述四条与实现逐条核对（core/hooks/HookSystem.ts:42-62），且该行为已有既有用例锁住（tests/unit/hooks/hooks.test.ts:185-240），无需新增用例。
#404 | fix | 属实：`listenerCount` 只在实现类 `HookSystem`（core/hooks/HookSystem.ts:90）上，`Store.hooks: IHookSystem` 拿不到，双语义 `size()` 因此没有无歧义替代。契约面补 `listenerCount(hookName: HookName): number`（实测仓库内唯一实现方 HookSystem 已提供，`tests/types/plugin-hook-args.typecheck.ts:40` 的 `const hooks: IHookSystem = store.hooks` 与两个 tsc 均通过；测试桩件走 `as never`/`any` 不受影响），并把 `size()` 的量纲漂移风险与保留理由写进 JSDoc。运行时+契约回归见 tests/unit/types-interface-state.test.ts #404。公开接口新增必需成员对第三方实现者是破坏性变更 → 待办记 CHANGELOG。
#408 | FP | 已过时：那条 `见 compose.ts` 注释在 medium 波次（5a677d2，#421 那一笔）已随重写删除（`git log -p -- src/types/selector.ts` 可见 `-  * （combiner 实参类型由调用方泛型推断保证，见 compose.ts）`），当前 `grep -rn "compose\.ts" src/` 零命中。顺手把报告要求的真实位置补进 SelectorComposerInput 文档（`src/extras/selector/selectorComposer.ts` 的 `SelectorComposer.combine`）。
#410 | fix | 属实：文档「有值时」诱导读向真值判断，而 `0` 是合法版本（Store `_mutationCount` 从 0 起算）。已把「一律写 `!== undefined`，不要 `if (item.version)`」写进 `SelectorCacheItem.version` 的 JSDoc，并点名两个现存合规消费点（createSelector.isCacheHit:165/170、parametricSelector:128/139，均实测为 `!== undefined`）。
#411 | fix（文档部分） | 文档缺失属实，已就地补齐：默认 10 / 5000ms、两者只在 `cache` 为真时被读取（resolve() 两处 `if (this.options.cache)`）、`cacheSize` 已有归一化 `Number.isFinite ? Math.max(1,v) : 10`——报告说的「cacheSize 0 会关掉历史命中」在现状不成立（createSelector.ts:93 已夹到 ≥1）。仍缺的是取值校验：`cacheTTL <= 0` 每条立即过期、`NaN` 使 `timestamp+ttl <= now` 恒 false 反而永不过期，已把这两条坑写进文档；校验点在 `SelectorFactory` 构造期，属 src/extras（禁改）→ 待办。
#412 | reject | 前提不成立：`equalityFn` 比较的不是选择器结果 `R`，而是**输入状态**——实测 `createSelector.ts:175` 为 `this.options.equalityFn(item.state, state)`（且只在缓存条目无版本号时调用）。按报告改成 `SelectorOptions<R>` 会把契约标错：比较器实参是 state 形状，写成 R 后「按 result 写的比较器」类型能过而运行时拿到 state。正确方向是 `SelectorOptions<S>` + `(a: S, b: S)`，但要先把 `S` 透传进 `createSelector(selectorFn, options?)` 的 options 位点（src/extras，禁改）→ 待办；本次只把「比的是 state、形参为何保持 unknown」写进文档，并在 tests/types/selector-generic-defaults.typecheck.ts 里钉住现形状。
#413 | fix | 成立：`ParametricSelector<S, P, R>` 三参全必填，与 `Selector`/`SelectorComposerInput` 的 `S = Record<string, unknown>` 默认口径不一致。按报告补 `S/P/R` 默认值（纯放宽「可省略」，显式传参的既有用法逐字不变；两个 tsc 零输出）。断言见新增 tests/types/selector-generic-defaults.typecheck.ts（裸写合法、默认形状 Equal、`S extends State` 约束仍在）。
#417 | fix | 成立：`(callback) => setTimeout(callback, 0)` 是隐式 any，而 `tsconfig.tests.json` 关掉了 `noImplicitAny`（`tsconfig.typecheck.json` 只 include src/**），所以两处 tsc 都查不出这个形状错误——正是该补标注的理由。已标 `(callback: () => void)`，并写明「仍用真实 setTimeout、用例自行 fake timers 后需推进时钟、残留回调交给全局 afterEach」（后半段定时器隐患已由 medium 波次的 afterEach `clearAllTimers()` 收口）。运行时回归见 tests/unit/types-interface-state.test.ts #417（同 tick 不执行、宏任务后执行一次、回调不带实参）。`tests/tsconfig.json` 无需改动。
#419 | FP | 空串在**本库里就不是合法名称**，不是被本工厂掩盖：`src/core/store/Store.ts:178` 写的就是 `options.name || 'store-N'`，且 `tests/unit/store/store.test.ts` STORE-088（禁改目录）显式锁定 `name: ''` 得到 `/^store-\d+$/`。改成 `!== undefined` 后 `''` 会透传给 Store，最终仍被换成非确定性的 `store-N`，反而丢掉本工厂要保证的唯一名可复现。保留真值判断，就地补「为什么」。
#422 | fix（types 层）+ 待办 | 成立：`SubscriptionOptions`(store.ts:185)/`NotifyOptions`(195)/`ActionsWithThis`(106) 被 `StoreOptionsBase`/`StoreOptions` 引用却未导出，公开 `.d.ts` 里只能匿名内联。已加 `export` 并在文档写明用途。但消费者仍命名不到：`package.json` 无 `./types/*` 子路径导出，真正的公开面是 `src/core/index.ts:22-33` 的显式 re-export 清单（禁改目录），需主控把三个类型补进去才算落地；断言见 tests/types/selector-generic-defaults.typecheck.ts（Equal 双向锁形状，防漂白的副本）。
#429 | fix | 成立：`data: ExtractPageData<S, M, G>` 与逐字相同的 `setData` 签名在 PageThis/PageConfig/ComponentThis/ComponentConfig 四处重复，`methods` 形状在组件两处重复。抽出内部基类型 `InjectedDataShape<S, M, G>`（data+setData）与 `ComponentMethodsShape<ExtraMethods, A, M>`，四个类型改为交叉复用；`getTabBar` 留在页面侧两处。刻意**不导出**基类型（公开面仍是那四个类型，非破坏性）。四处同形以 Equal 断言钉住在 tests/types/integration-types.typecheck.ts #429（含 data === ExtractPageData、setData 确切签名、跨四类型两两 Equal、组件侧无 getTabBar），两个 tsc 零输出。
#430 | reject（修法方向反了） | 撞名会变重载集这点成立，但报告的 `Omit<HostStoreApi<S>, keyof ExtractMappedActions<A, M>>` 与运行时相反：`with-app-store.ts` 的 onLaunch 先 `bindActions`(199) 后 `exposeStoreAPI`(210)，后者对 `store/getStore/getState/getCached/dispatch/subscribe` 是无条件 `Object.assign` 覆写（integrations/utils.ts:369-371），所以留下来的是**调试 API**。且反方向还会把带泛型的 `getCached<K extends keyof S>` 过一次映射类型。改为 `Omit<ExtractMappedActions<A, M>, keyof HostStoreApi<S>>`（映射 action 避让，`HostStoreApi<S>` 保持完整），文档写明两侧证据。断言见 tests/types/integration-types.typecheck.ts #430：撞名时 `const s: string = clashThis.getState()` 必须报错（改前按重载集会通过，属类型谎报），同时 `getState(): S`、`getCached('userInfo')` 精确签名与未撞名 action 的精确签名仍成立。
#431 | fix（部分已过时） | 报告时点三个都死；现在 `PageThis` 已被 medium 波次 #428 的断言用掉（integration-types.typecheck.ts:301），`ComponentThis` 被本轮 #429 的同形断言用掉，只有 `ConnectOptions` 仍无使用点——已删（`noUnusedLocals` 在 tsconfig.tests.json 为 false、`no-unused-vars` 对 tests/** 关闭，确实没人会报）。选择「加真实断言」而不是把两个 import 一删了之。
#434 | fix | 成立：两条 `.then(...)` 结果既不 await 也不 catch，文件一旦被执行就是未处理拒绝；且 `mixedActions` 是 `declare const`，真跑还会先抛 ReferenceError。已把两条链收进 `probeThenChannel()` 内部逐个 `await`（保留「`.then` 回调拿到的已是解包值」这一被测点），另加注释说明为何不能浮空。解包精度另由 `probeExecutor` 的 await 通道锁住。
#435 | fix | 成立：`Promise<[unknown, number]>` 把本文件要锁的精度抹成 unknown（`Promise<Promise<T>>` 也能过）。已改为 `Promise<[{ id: string }, number]>`，同时断言 await 异步 action 得到解包值。
#437 | fix | 成立：`@ts-expect-error` 只要求「下一行有任意诊断」，签名重排等无关破法也能让它保持绿色。实测把四条负向断言的指令临时摘掉，四条全部是 **TS2345**（`Plugin<UserState>` 不可赋给 `Plugin<object> | Plugin<NoInfer<CartState>>`），已把预期诊断码写进注释；并把契约本身改写成类型级断言：`Plugin<UserState> extends Parameters<typeof cartStore.use>[0] ? true : false` 必须为 false，另配同状态插件必须在联合里的反向对照（防断言因联合被整体改坏而空过）。

## 主控需知（联动待办）

- **#386**：`ErrorContext.timestamp` 的默认值有两处（`createErrorContext` 与 `src/extras/error/ErrorAggregator.ts:58` 的 `context.timestamp || Date.now()`）。收口需改 extras：删采集器兜底、并把时钟做成可注入（测试/回放），同时注意 `||` 会把合法的 `timestamp: 0` 误判为缺省。
- **#387 + #388**：性能契约的两个实现侧欠账要一并处理，单改 types 必破编译：①`getMetrics()` 全链路（`types/performance.ts` 接口 + `core/performance/PerformanceMonitor.ts:388` 类 + `PerformanceAnalyzer.analyzeBottlenecks` 入参）统一 `readonly PerformanceMetrics[]`，并让 `MetricsCollector.getAll` 也逐元素复制（现只复制数组、元素共享）；②`computePerformanceStats` 的 byOperation 累加器补 `minDuration` / `thresholdExceeded`（实测只改类型报 `metrics.ts(74,5) TS2322`）。①属公开 `.d.ts` 的破坏性收紧，需与 CHANGELOG 同批。
- **#411 + #412**：`SelectorFactory` 构造期补 `cacheTTL` 校验（现只 `?? 5000`：`<=0` 每条立即过期、`NaN` 永不过期）；并把 `equalityFn` 的入参口径改为状态：`SelectorOptions<S>` + `equalityFn?: (a: S, b: S) => boolean`，需先把 `S` 透传进 `createSelector` / `createMemoizedSelector` 的 options 位点（`src/extras/selector/createSelector.ts`）。
- **#404 + #422 + #413**：公开类型面本轮有增无删（`IHookSystem.listenerCount` 新必需成员、`SubscriptionOptions`/`NotifyOptions`/`ActionsWithThis` 加 export、`ParametricSelector` 泛型默认值）。①`src/core/index.ts` 的 re-export 清单要补那三个选项类型，否则 #422 消费者仍命名不到；②`docs/API.md`/CHANGELOG 按 minor 记 `listenerCount` 与 `emit` 的失败语义；③Wave D 复核 `.d.ts` 快照测试（若有）是否需要同步。
- **#430**：`AppThis` 现在让映射 action 避让 6 个调试 API 键，与运行时一致；但 `Extra`（用户在 App 配置里自带的同名成员）仍与二者直接求交，运行时同样是 exposeStoreAPI 覆写。若要彻底对齐，需把 `Extra` 也做同向避让，或在 `exposeStoreAPI` 撞名时告警（属 src/integrations）。
- **#429**：`getTabBar` 只声明在页面两处，组件侧是否需要（微信组件实例上 `getTabBar` 在 tab 页场景可用）未在本轮判定，需要真机/基础库结论再动。

## 验证

- `npx tsc --noEmit -p tsconfig.typecheck.json` → 零输出（exit 0）
- `npx tsc --noEmit -p tsconfig.tests.json` → 零输出（exit 0）
- `npx eslint <13 个改动文件>` → 零输出（exit 0）
- `npx prettier --check <改动文件 + tests/types/*.typecheck.ts>` → `All matched files use Prettier code style!`
  - 注：`src/types/integration.ts` 里 3 个条件类型的 `> =` 折行是 prettier 对**既有非规范格式**的归一（HEAD 版本本身就过不了 prettier 3.8.1），非语义改动；`git diff -w` 可见该文件纯空白差异 38 行，其余文件无空白噪声。
- `npx jest --ci --silent tests/unit tests/integration` → 106 suites / 2806 tests 全通过（`--testPathIgnorePatterns types-interface-state` 时为 105/2783，即本分片新增 1 suite / 23 用例）。`A worker process has failed to exit gracefully` 警告为既有现象（排除本分片文件后同样出现）。
- 未跑 `pnpm build` / `pnpm test:ci`（禁项），未 `git add` / `git commit`。
