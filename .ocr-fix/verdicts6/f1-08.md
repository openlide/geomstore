# f1-08（wave f1）
条数：10｜FIXED 9 / FP 0 / REJECT 0 / NEEDS-MAIN 1（R6-101 的 src 侧已改到位，尾巴在主会话的测试文件；R6-103 也带一条主会话测试侧尾巴，见文末清单）

自验结果见文末「自验」。所有回归锁：
`tests/unit/r6-f1-08-snapshot.test.ts`（16 例）、`tests/unit/r6-f1-08-enterprise.test.ts`（27 例）。

## R6-050 medium src/extras/snapshot/diff.ts:L141-L361
verdict: FIXED
改动: `compareSnapshots` 入口加「输入可信性闸门」——任一来源 `success === false` 时不再逐路径比对，改交付一条 root 级整体差异（宁多勿漏，与结构匹配超预算同一口径），并在 `SnapshotDiff` 上新增必填字段 `inputTrusted`（false 即「输入不可信」信号）；`compareSnapshots` 与 `SnapshotManager.compareSnapshots` 的 JSDoc 补上 types.ts:152 那条「消费前先判 success」在 diff 侧的落点说明。报告列的两种反向结论（两侧 undefined 短路成 `changed:false`、两份超时半成品静默漏报）都被这一步挡住。
回归锁: tests/unit/r6-f1-08-snapshot.test.ts > R6-050 compareSnapshots 的输入可信性闸门（5 例：两侧失败 / 两份半成品 / 单侧失败 / 可信时无噪声 / 成功快照 data 本身就是 undefined）
判据: 旧实现下 `inputTrusted` 字段不存在（断言 `toBe(false)` 直接失败），且 `compareSnapshots(failed, failed).changed` 为 false（新断言为 true）。闸门只看 `success` 不看 `data !== undefined`，是为保住「快照对象本身就是 undefined」这一合法形状（已单独锁住）。
文档: docs/API.md:297 与 docs/CONCEPTS.md:68 / BEST_PRACTICES.md:91 的 compareSnapshots 条目需补一句**行为变更**：返回值新增 `inputTrusted`，任一侧快照 `success:false` 时 `changed` 恒为 true（表示输入不可信，不表示确有差异）；CHANGELOG 记 SnapshotDiff 新增字段（附加式，非破坏）+ 失败输入不再被报成「无差异」。

## R6-101 low src/extras/snapshot/diff.ts:L244-L288
verdict: FIXED（分片内已改到位）+ NEEDS-MAIN（两处跨文件尾巴）
改动: Map 条目路径不再用「两侧各自的迭代下标」，改按**键身份**生成并直接采用克隆引擎的方言：值节点 `parent[String(key)]`、键节点 `parent.key[String(key)]`（新增 `keyToken/mapValuePath/mapKeyPath` 三个辅助，Symbol 键走 `String()`，`toString` 抛错的键退回 `<unstringifiable key>` 而不是让整个 diff 失败）。同一条路径串不再承载两类事实（值差异 / 键删除 / 键新增三者形状各异），也不再随插入顺序漂移。Set 分支的 `[removed:i]`/`[added:k]` 保留本侧下标（集合元素没有可当身份的键，对象元素的 `String()` 恒为 `[object Object]` 反而更没区分度），但把「它不是条目身份、跨快照聚合请用 oldValue/newValue」写进注释与模块头；模块头新增整段路径方言说明。
回归锁: tests/unit/r6-f1-08-snapshot.test.ts > R6-101（6 例：三类事实三种形状 / 下标漂移与双向调用同路径 / 嵌套值前缀 root[a].doomed / 与克隆引擎 errors[].path 逐字相同 / Symbol 与抛错键 / Set 增删）
判据: 命令级证据 —— 改完 diff.ts 后跑既有快照套件，唯一变红的正是断言旧方言的那条：
`tests/unit/r5-extras-snapshot-fixes.test.ts:510` 期望 `root.key[0]`，实收 `root[A]`（`String(key)`），
说明新路径与 clone.ts:393/413 给同一条目记的 path 已逐字一致。
NEEDS-MAIN（要主会话配合的两处，都是本分片无权改的文件）：
1. `tests/unit/r5-extras-snapshot-fixes.test.ts:510` 需把期望串从 `'root.key[0]'` 改成 `'root[A]'`（用例意图不变：「配到同一键之后值仍按该键路径继续比」）。不改则全库 jest 门禁这条恒红。
2. `tests/unit/extras/ocr-low-round4-p2.test.ts:300` 手搓的 `SnapshotDiff` 字面量需补 `inputTrusted: true`（R6-050 新增必填字段），否则 `pnpm typecheck:tests` 报 TS2741。
   另：主会话若在改 clone.ts/clone-async.ts 的 Map/Set path 方言（R6-008 只动 isExactly 与非纯对象门槛，未动 path），本文件的 `mapValuePath`/`mapKeyPath` 必须跟着同步，两侧只此一套方言。
文档: docs/API.md:295 已写了克隆侧的 `path.key[A]` / `path[A]` 口径，需要在 297 的 compareSnapshots 条目补一句**行为变更**：diff 的 `changes[].path` 现在与之一致（Map 值差异记 `root[<String(key)>]`、键增删记 `root.key[<String(key)>]`），不再用迭代下标；Set 增删条目路径是报告序下标、非条目身份。

## R6-051 medium src/extras/snapshot/SnapshotManager.ts:L240-L388
verdict: FIXED
改动: 队列压缩从「每个外层批次开头 `splice(0, queueHead)`」改为按 `shouldCompactQueue()`（已消费前缀 ≥ 剩余长度才裁一次），摊还 O(1)/任务；两条失真注释（240 行「后者 O(n)…」、原 384 行「splice 的搬移量受批大小约束」）改成与实际复杂度一致的口径，并写清旧写法总量 ΣO(Lᵢ) ≈ O(n²/b) 与 shift 同量级。
回归锁: tests/unit/r6-f1-08-snapshot.test.ts > R6-051（2 例：2001 任务/批大小 20 下 splice 次数与总搬移量在线性界内；压缩发生占位起点后超时半成品仍不留源数据没有的 undefined 键）
判据: 实测先失败后通过 —— 把压缩条件临时改回 `if (queueHead > 0)`（旧实现）跑该用例：`expect(watch.calls()).toBeLessThan(40)` 收到 **100**（=批次数），断言变红；恢复新实现后 16 例全绿。搬移量守卫 `< 2001*10`（旧口径 ≈ 10 万次）。
文档: none（纯内部复杂度与注释修正，对外行为不变）

## R6-052 medium src/extras/snapshot/SnapshotManager.ts:L381-L543
verdict: FIXED
改动: 超时不再与「队列已排空」共用一个出口。新增 `droppedTasks`（入队守卫丢掉的任务数，与既有 `/* istanbul ignore else */` 同一处）与 `deliveryIncomplete = queueHead < queue.length || droppedTasks > 0`，结果里 `timedOut = hasTimedOut && deliveryIncomplete`，`success`/`errors` 一律按 `timedOut` 计算；`createSnapshotAsync` 的 JSDoc 补「超时口径」一段。定时器无需再改（句柄翻位后交付的是完整克隆时不再受影响，撤销点见 R6-102）。
回归锁: tests/unit/r6-f1-08-snapshot.test.ts > R6-052 / R6-102 超时状态机与定时器句柄（2 例：队列排空后定时器才翻位 → success true 且 errors 为空；让出 #1 就翻位且 300 个子任务未处理 → 仍 success false + timeout 错误）
判据: 实测先失败后通过 —— 探针保证竞态真的发生（`expect(probe.firedAt()).toBe(2)`），把结果两行临时改回 `!hasTimedOut` / `hasTimedOut`（旧实现）跑该用例：`result.success` Expected true / Received **false**；恢复新实现后全绿。既有三条超时用例（SnapshotManager.test.ts:282/690/1649/1670、r5-extras-snapshot-fixes.test.ts:385、snapshot-async-error-paths.test.ts:66）用的都是大数据+未完成队列，新口径下仍判失败，实测未变红。
文档: CHANGELOG 记一条**行为变更**：异步快照的超时只在「仍有未处理任务或超时后丢掉过入队任务」时使 `success:false`；收尾竞态下交付的完好克隆不再被判为失败。types.ts 的三条不变量仍成立（success:false ⇒ errors 非空），无需改 types.ts。

## R6-102 low src/extras/snapshot/SnapshotManager.ts:L268-L547
verdict: FIXED
改动: 两处定时器撤销由 `if (timeoutId)` 改为 `if (timeoutId !== null)`（成功路径与 catch 路径），与本仓同族硬口径 selectorComposer.ts:394-399 对齐，并就地写明理由（句柄可以是 0）。
回归锁: tests/unit/r6-f1-08-snapshot.test.ts > R6-052 / R6-102 第一条：探针刻意让 `setTimeout` 返回句柄 **0**，断言 `expect(clearSpy).toHaveBeenCalledWith(0)`
判据: 旧写法 `if (0)` 为假 → clearTimeout 永不执行 → 该断言必红；真值判定不成立这一条由用例里的 0 句柄直接构造，无需宿主配合。
文档: none

## R6-053 medium src/integrations/enterprise/env.ts:L115-L161
verdict: FIXED
改动: 按报告给的第二个方向做（不改写既有存储格式）：新增 `needsQuoteEnvelope(value)` —— 字符串只有在「`JSON.parse` 成功且解析结果不是 number/boolean」时（`'null'` / `'"abc"'` / `'{"a":1}'` / `'[1,2]'` …）才套引号信封 `JSON.stringify` 落盘，其余（普通 userId、路径、数字文本 `'1001'`、裸布尔文本）仍原样存。于是 set→get 对字符串类型无损，报告里的登录链（`switchUser(String(res.userInfo?.id))` 写入 `'null'` → 冷启动读回 null → 整轮会话不初始化）被切断。同时改掉 107-108 行那句不成立的断言，并把「与 get 的 number/boolean 例外严格对称」「旧数据何时自愈」写进 get 的 JSDoc 与 needsQuoteEnvelope 的注释。
回归锁: tests/unit/r6-f1-08-enterprise.test.ts > R6-053（5 例 + 10 条 it.each 字符串形状：往返类型无损 / 裸文本格式不变 / 报告触发链 / number·boolean 刻意契约不变 / 历史裸值仍可读）
判据: 旧实现在 `storage.set('k','null')` 后 `get` 返回 `null`（`typeof read === 'string'` 必红）。存储格式只对「本来就会读成另一种值」的字符串改变，普通 userId 的裸文本形态有专门用例锁住（`rawStore['current_user_id'] === 'app-user-2'`），故 store-manager / wechat-enterprise 的既有读写与断言不受影响（跑既有企业级套件验证，见自验）。残留：改造前写下的裸 `'null'` 值在平台层面与「真存了 null」不可区分，读回仍是 null，下一次 set 覆盖即自愈 —— 已写进注释与判据。
文档: docs/API.md:631 附近的企业级 storage 条目需补一句**行为变更**：`storage.set` 对「会被读成另一种值」的字符串加引号信封，`storage.get` 的字符串往返自此类型无损；`number/boolean` 读回原始字符串的不对称契约不变（不是本轮改的）。

## R6-054 medium src/integrations/enterprise/hot-update.ts:L82-L93
verdict: FIXED
改动: 备份侧不再直接 `JSON.stringify(store.$snapshot())`。新增模块内编解码：
- `encodeForBackup`：Date/RegExp/Map/Set/undefined/NaN/±Infinity/-0/BigInt 一律落成带 `'#gs'` 标记的自描述形态（`u`/`n`/`bi`/`d`/`re`/`m`/`s`），类实例与函数/symbol/循环引用记进 `lossy` 清单、按「有损但可用」落普通结构；用户数据自带 `'#gs'` 键时套 `'raw'` 信封（编码因此是单射）；递归用路径栈防环，不再让 JSON.stringify 抛 BigInt 或静默折叠容器。
- `decodeFromBackup`：对称解码回真实例，只认已知标记（其余含 `'#gs'` 的对象按用户数据处理），旧格式备份无标记即原样透传（行为与改造前一致）。
- `BackupData` 新增可选 `lossy: string[]`；`backupState` 在备份时刻就告警列出无法还原的成员，`restoreFromHotUpdate` 读到非空清单时再复述一条「本次为有损恢复」告警。恢复仍走 `$patch` 合并语义、仍返回 boolean（公开签名不变）。
回归锁: tests/unit/r6-f1-08-enterprise.test.ts > R6-054（5 例：容器类型复原（含落盘形态断言 `{'#gs':'s',v:['a','b']}`）/ `.has()`+`.getTime()` 不再 TypeError / 标量与自带标记键对象往返 / 类实例 lossy 与告警留痕 / 旧格式备份仍可恢复）
判据: 旧实现下 `storage.set` 落的是 `JSON.stringify(Set)`→`{}`，重启后 `state.selected instanceof Set` 为 false、`.has(7)` 抛 TypeError，前两条用例必红。报告点到的三种成员（Map/Set/Date）各有一条独立断言。
文档: docs/API.md:631 的热更新条目需补一句**行为变更**：备份存储格式换为带 `'#gs'` 标记的编解码（旧备份仍可读，但不支持集从「JSON 可表达的值」扩到 Date/RegExp/Map/Set/undefined/非有限数字/BigInt；类实例恢复后原型丢失、函数与 symbol 成员恢复为 undefined，二者都会打 lossy 告警）。

## R6-055 medium src/integrations/enterprise/offline.ts:L107-L132
verdict: FIXED
改动: 给队列补上两个自驱动触发点，并按报告建议加止损：
- `scheduleResync(hasWorkSignal)`：以 **0 延时宏任务**起跑（刻意不用微任务：微任务会抢在调用方 `await execute()` 的续体之前把整批快照移进 syncPending，调用方随即读 `getQueueLength()` 拿到的是「同步在途」的 0，看不出自己刚入过队——`tests/integration/enterprise.test.ts` 的三条既有用例正锁着这个同帧读法），rejection 就地记日志（与 344 行网络恢复回调同口径）；守卫为「未释放 && isOnline && !syncing && 队列非空 && 没有已排的定时器」；新增 `resyncTimer` 字段并在 `dispose()` 里按 `!== null` 撤销。
- `enqueue()` 落盘后调 `scheduleResync(true)`（在线失败入队即补跑一轮；离线入队不起跑）。
- `syncQueue()` 的 finally 落盘后调 `scheduleResync(progressed || enqueuedWhileSyncing)`；`progressed` 由「本轮有操作永久离开队列」置位（执行成功 / 落死信 / 丢弃损坏条目），`enqueuedWhileSyncing` 是新实例字段，专治「一轮在途时入队 → 互斥守卫让这一枪放空」。
止损理由写进注释：全失败且都未到重试上限又没有新项时不再自驱，否则「服务端持续 5xx + 网络状态不变」会把 syncQueue 变成不等任何外部信号的紧循环。execute 的 JSDoc 补了四个触发点的契约。队列容量上限/防抖落盘是 R5-296 已判不修项，本轮未翻。
回归锁: tests/unit/r6-f1-08-enterprise.test.ts > R6-055（4 例：在线失败后无任何外部事件即被重放并出队 / 全失败时只补跑一轮就停 / 同步在途入队的项由收尾补跑 / 离线入队不自驱且重试耗尽仍落死信触发 onDrop）
判据: 旧实现下前三例的第二条断言（`replays === 1` / `innerRuns === 1`）恒为 0 —— 全库只有 `wx.onNetworkStatusChange` 与 `App.onShow` 两个 syncQueue 触发点，入队不安排任何一轮。另附实测：微任务版把 ENTERPRISE-019 / REGR-OFF-002 / #350 三条既有用例打成 0，改宏任务后 `tests/integration/enterprise.test.ts` + 本分片新用例 142 例全绿。
文档: docs/API.md:630 的 OfflineManager 条目需补一句**行为变更**：`execute()` 失败入队（在线）与一轮收尾仍有存货时会自动补跑重放（离线态不自驱）；「入队即等待外部事件」的旧读法不再成立。

## R6-103 low src/integrations/enterprise/offline.ts:L189-L194
verdict: FIXED
改动: 把 `wx.showToast` 单独包一层 try/catch（失败只 `logger.warn`「同步失败提示未弹出（不影响同步结果）」），与本目录第五轮 #282 给 `showLoading`/`hideLoading` 各自单独兜底的约定对齐；位置仍在 try 内、finally 前，但异常不再外溢。
回归锁: tests/unit/r6-f1-08-enterprise.test.ts > R6-103（2 例：showToast 抛错 / 宿主根本没有 showToast 时，`await expect(manager.syncQueue()).resolves.toBeUndefined()` 且队列状态不变）
判据: 旧实现下两处都会让 syncQueue 变成 rejection（`resolves` 断言红）。宿主侧把这条读成「同步失败」的两处（wechat-enterprise.ts:176、offline.ts 网络恢复回调）因此不再被误导。
NEEDS-MAIN（本分片无权改的测试）: `tests/unit/integrations/enterprise-app-lifecycle.test.ts:83-108` 那条「App.onShow 中离线队列同步失败被接住」正是**用 showToast 抛错来制造 rejection** 的——即本条判掉的那个 bug，改完后 `errorSpy` 再也收不到「离线队列同步失败」，用例变红。请把制造失败的手段换成仍会外溢的一条（同文件同族的写法见 tests/unit/integrations/enterprise-recovery.test.ts:53 的 `onDrop` 抛错路径），断言意图（onShow 接住 rejection、hideLoading 被调用）不变。
文档: none

## R6-104 low src/integrations/enterprise/offline.ts:L333-L358
verdict: FIXED
改动: `initNetworkListener` 调整为「先 `wx.getNetworkType` 探测、后 `wx.onNetworkStatusChange` 注册」，探测抛错时构造函数不再留下一个永远无人 dispose 的网络监听（dispose 是唯一撤销点而实例没交出去）；注释写明泄漏形状（账号每次切换漏一个、多实例并发落盘同一存储键）。理由与本模块已有的 `wx.offNetworkStatusChange?.()` 承认宿主 API 可能缺失同源。
回归锁: tests/unit/r6-f1-08-enterprise.test.ts > R6-104（2 例：探测抛错 → 构造抛错且 `networkListeners` 长度为 0 / 正常路径监听仍注册、网络恢复会同步、dispose 能撤销）
判据: 旧顺序下第一条会在 `networkListeners` 里留下 1 个闭包（断言红），且该监听此后每次网络恢复都会在一个没人认领的实例上跑 `syncQueue()` 并 dispatch 进 store。
文档: none

## 自验（收工前实跑，全部为实测输出）

```bash
cd /d/WorkSpace/openlide/GeomStore

# 1) 本分片新增回归锁：全绿
npx jest tests/unit/r6-f1-08-snapshot.test.ts tests/unit/r6-f1-08-enterprise.test.ts --ci
#    → Test Suites: 2 passed, 2 total / Tests: 43 passed, 43 total

# 2) 受影响的既有套件（快照 + 选择器同源用例）
npx jest tests/unit/extras/snapshot tests/unit/r5-extras-snapshot-fixes.test.ts tests/unit/extras/ocr-low-round4-p2.test.ts --ci
#    → Tests: 1 failed, 246 passed, 247 total
#      唯一变红：tests/unit/r5-extras-snapshot-fixes.test.ts:510（期望旧方言 'root.key[0]'，实收 'root[A]'）
#      = R6-101 的 NEEDS-MAIN #1，改一个字符串即绿

# 3) 企业级全部套件（unit/integrations + tests/integration + r5 两片）
npx jest tests/unit/integrations tests/integration tests/unit/r5-integrations-p1-enterprise.test.ts tests/unit/r5-integrations-p2.test.ts --ci
#    → Tests: 1 failed, 347 passed, 348 total
#      唯一变红：tests/unit/integrations/enterprise-app-lifecycle.test.ts 的
#      「App.onShow 中离线队列同步失败被接住」——它用 showToast 抛错制造 rejection，
#      正是 R6-103 判掉的那个 bug，需按 R6-103 条目的 NEEDS-MAIN 换驱动手段

# 4) 类型与门禁
npx tsc --noEmit
#    → src 零错；仓库里另有 3 处报错全在别人分片的在途文件
#      （ActionManager.ts / extras/action/decorators/cache.ts / extras/error/ErrorAggregator.ts）
npx tsc --noEmit -p tsconfig.tests.json
#    → 本分片相关的只有 tests/unit/extras/ocr-low-round4-p2.test.ts:300 的 TS2741
#      （SnapshotDiff 新增必填字段 inputTrusted）= R6-101 的 NEEDS-MAIN #2
npx eslint --max-warnings 0 src/extras/snapshot/diff.ts src/extras/snapshot/SnapshotManager.ts \
  src/integrations/enterprise/env.ts src/integrations/enterprise/hot-update.ts \
  src/integrations/enterprise/offline.ts tests/unit/r6-f1-08-snapshot.test.ts tests/unit/r6-f1-08-enterprise.test.ts
#    → 无输出（0 error / 0 warning）
npx prettier --check <同上 7 个文件>
#    → All matched files use Prettier code style!
```

补充：本分片所有 src 改动落盘后被 Edit 工具写成过 CRLF，已统一换行回 LF（仓库 blob 与工作树口径一致），
`git diff --stat` 对这 5 个文件只有局部 hunk，无整文件重写。

## 交给主会话的三件事（去重后的清单）

1. R6-101 NEEDS-MAIN #1：`tests/unit/r5-extras-snapshot-fixes.test.ts:510` 期望串 `root.key[0]` → `root[A]`。
2. R6-101 NEEDS-MAIN #2：`tests/unit/extras/ocr-low-round4-p2.test.ts:300` 的字面量补 `inputTrusted: true`。
3. R6-103 NEEDS-MAIN：`tests/unit/integrations/enterprise-app-lifecycle.test.ts:83-108` 换 rejection 驱动（改用 onDrop 抛错，同 enterprise-recovery.test.ts:53 的口径）。
4. 文档/CHANGELOG 措辞见各条「文档」行（R6-050 / R6-101 / R6-052 / R6-053 / R6-054 / R6-055 六条是**行为变更**，R6-051 / R6-102 / R6-103 / R6-104 为纯修复或 none）。
5. 若 f2-12 / hot-p0 对 clone.ts、clone-async.ts 的 Map/Set path 方言有任何改动，`diff.ts` 的 `mapValuePath` / `mapKeyPath` 必须同步（两侧只此一套方言）。

