# G3-integrations-plugins-medium-p3 / G4-types-tests-medium-p3 判定记录（逐条追加）

## G3-integrations-plugins-medium-p3
#369 | fix | 成立（报告改法方向需修正）：原判定 `'getItem' in userStorage` 只看键名，缺 setItem/removeItem 的后端推迟到首次落盘抛错并被 saveState 吞成日志。按 finding 正文「尽早给出明确错误」实现为安装期 throw TypeError（列出缺失方法），未采用报告 diff 的「校验失败悄悄回落到 wx/内存」（会把数据写到另一个后端，比报错更难排查）；连带修正文件头 `storage: wx` 的错误示例（wx 全局对象没有 getItem，此前靠静默回落掩盖）与 PERSIST-016 / PERSIST-COVER-016 两处用 `storage:{}` 触发 wx 分支的既有用例（改为不传 storage）；新增 #369 回归用例断言抛错且不写 wx、不调用只读后端
#370 | fix（wx 分支；内存分支为报告子结论不成立） | 成立：assertSyncStorageResult 此前只包用户后端，wx 适配器原样透传，Taro/uni-app 类 Promise 版 polyfill 下写入变无人处理的 rejection。三个方法统一过守卫并把错误串改成 `wx.getStorageSync` 等真实定位（原消息写死 `storage.xxx`，对没传 storage 的调用方是指错方向）；报告括注的「内存降级分支同样未覆盖」不成立——该分支返回值全由闭包产出（`?? null`、`void`），不可能出现外来 Promise，包一层是死分支，已在注释写明。新增 #370 用例断言 Promise 版 wx 下恢复/落盘各自明确报错并 emit onError
#371 | fix | 成立：非生产 console.warn、生产完全静默，H5/SSR 场景持久化无声失效。实现为生产走 `store.hooks.emit('onError', new Error(msg), 'persistence')`（保持生产不刷控制台，不破坏 production-mode-silence 用例）；新增 #371 用例用 jest.resetModules + NODE_ENV=production 重新加载模块验证 onError 收到且 warn 未输出
#372 | fix | 成立：clearOnUninstall 的 removeItem 抛错被 `catch {}` 完全吞掉，磁盘残留旧数据下次启动复活。按 saveState 既有口径补 console.error + emit('onError', error, 'persistence')；新增 #372 用例
#376 | fix（数组结论成立；原型污染机制已被同事在 clone 层修掉） | 实测证实数组分支：探针跑 `importHistory('{"snapshots":[{"state":[],"timestamp":1}],...}')` 得 snapshotCount=1（被接受），随后 `goTo(0)` 抛 `[GeomStore] $replaceState: newState must be a plain object`（Store.ts:379），与「畸形数据直接跳过」注释矛盾。__proto__ 半段的机制描述已过期：同一探针得 protoPolluted=false / globalPolluted=undefined / ownProtoKey=true（core/utils/clone.ts:89 已用 defineProperty 复刻自有键）。仍按报告口径在入口收紧（数组 + 条目/state 的自持 __proto__ 键一起排除，提取 isImportableObject），并在注释写明它是入口卫生而非唯一防线；测试用 JSON.parse 字符串载荷（对象字面量的 __proto__ 是设原型，测不到）
#424 | fix | 成立：卸载时若 `store.getter !== wrappedGetter`（后续插件把本包装包在链里）就只 warn 不摘链，闭包仍持有已 clear()、外部无引用入口的 monitor，之后每次 getter 都写入幽灵指标。按报告方案加 `getterActive` 开关，卸载先置 false，包装内 !active 时直接透传 originalGetter；新增 #424 用例（断言卸载后连调两次 getter，monitor 指标仍为 0）
#425 | fix | 成立且报告的两处实证均已核对：HookSystem.ts:47 确实 `this.emit('onError', error, hookName)`、builtin.ts:287 确实传 'persistence'，故「无法区分错误来源」的旧注释不成立。discardPendingEnds 改为按 source 收敛：source 命中配对键或经 HOOK_TO_OPERATION（Map，避免 'constructor' 之类原型键误命中）归一的只弹该类型；无关来源（persistence）一条不弹；无 source 按 dispatch 处理（core 三处 emit('onError', error) 都在 ActionManager 的 dispatch 失败路径，这是唯一可能带残留计时的情形）。原「手动 onError 也应清空全部类型栈」用例固化了被推翻的旧行为，连同 timing-stack 两个弱断言用例一起改为按类型断言

## G4-types-tests-medium-p3
#432 | fix（并补强为精确相等断言） | 成立，已用 tsc 反向验证：把 getter 形参改回 `(state: { count: number })` 后跑 `npx tsc --noEmit -p tsconfig.tests.json`，原 `_doubleFn` 可赋值断言零输出（逆变白送），只有新加的 `Equal<typeof typedGetters.double, (state: UserState) => number>` 报 TS2322 true→false。故除报告要求的「标注成 fixture 状态类型」外，再加 Equal 双向精确断言真正锁住推断结果，并在断言处注明可赋值断言为何空转
#433 | fix | 成立：组件块此前只有一个正向用例，page/app 各有 4-5 条反向断言。补齐同口径的 withComponentStore 正/反例（拼错 mapState/mapGetters/mapActions 键、别名形式值拼错各 4 条），并把从未被断言的 `UserActions.logout` 纳入 ArrayMapped 夹具断言（零参返回 void + 传参反例 + login 漏传 payload 反例）。未做「三块提取成公共函数」：typecheck 用例的价值就在字面 @ts-expect-error 行上，泛型转发会吃掉字面量类型，改为在注释里写明三块须保持同口径
#436 | fix | 成立，已用 tsc 反向验证：把 persistencePlugin 工厂返回类型抹平为 `Plugin`（历史缺陷形状）后，报告点出的既有三行（filter 入参、userStore.use）全部零错误通过，而新增的 `cartStore.use(persistencePlugin<UserState>(...))` 报 TS2578「Unused '@ts-expect-error'」——正证 use 的 `Plugin<NoInfer<S>> | Plugin<State>` 联合让被抹平的返回值在任何 Store 都被接受；另加正向 `const typedPersistence: Plugin<UserState> = persistencePlugin<UserState>(...)`（同场景报 TS2322，Plugin<State> 不可赋给 Plugin<UserState>）。两处均已还原，验证记录在此

## 主控需知 / 待办（G3-p3 + G4-p3）
- #369 是对外行为变更：`persistencePlugin({ storage })` 传入缺少 getItem/setItem/removeItem 任一的物件，安装期即抛 TypeError（此前是静默回落 wx/内存）。`Store.use` 会原样上抛安装异常（core/store/Store.ts:579-588 回滚后 rethrow），`usePlugin` 仍按既有口径吞掉并 console.error。CHANGELOG、docs/API.md、skill references（extras-plugins / persistence 段）需同步；docs/GUIDE.md:235 的写法（`new WxStorageBackend()` 或自封装三方法）本就符合新契约，examples/advanced/plugins.ts 的 memoryBackend 三方法齐全、不受影响。
- #369 连带修正 src/plugins/builtin.ts 文件头示例 `storage: wx` → `new WxStorageBackend()`：wx 全局对象没有 getItem，旧示例一直靠「校验不通过就悄悄回落」蒙混过关。skill 文档里若也有 `storage: wx` 写法需一并改（docs 越界，留 Wave D）。
- #376 是行为收紧：importHistory 现在会跳过 state 为数组或自带 __proto__ 自有键的条目（此前数组条目入栈、goTo/undo 抛核心 TypeError）。
- #425 联动点：core/store/ActionManager.ts 的三处 `emit('onError', error)`（166/218/244）不带 source，本组禁改 core，故 analyzer 把「无 source」按 dispatch 处理（注释已写明依据）。若主控希望精确归因，请在 ActionManager 三处补第二参 'dispatch'（types/plugin.ts 的 HookArgsMap.onError 已允许 source?），analyzer 无需再改即可命中 `pendingEnds.has('dispatch')` 分支。
- 未改 src/types/**：本组 3 条 G4 结论全部落在 tests/types，无类型收紧外溢风险。
- 负向对照用的临时 src/tests 改动（persistencePlugin 返回类型抹平、getter 标注回窄、getterActive 开关摘除、discardPendingEnds 回退为全弹）已全部还原并复核：`grep -rn "TEMP NEGATIVE" src/ tests/` 无残留，两个 tsc 与 eslint 零输出、jest 2717 例全绿。
- 反向对照证据：#432 回退后仅 `Equal` 行报 TS2322（旧的可赋值断言零输出）；#436 抹平返回类型后仅新增两行报 TS2322/TS2578（旧三行全过）；#424/#425 回退后恰有 4 条新用例失败（#424 一条、#425 三条）。
- 格式：tests/unit/plugins/{performance/analyzerPlugin,devtools/timeTravelPlugin,builtin}.test.ts 在 HEAD 就非 prettier-clean（`;(` 行首分号与 eslint no-extra-semi=error 冲突，CI 只跑 lint 不跑 prettier）。本次新增的 builtin.test.ts:498 按 ESLint 侧去掉行首分号，与仓库既有口径一致；其余 prettier 偏差均为存量。
