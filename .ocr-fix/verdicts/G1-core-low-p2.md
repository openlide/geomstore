# G1-core low p2 判定记账（16 条 · src/core · 分片 2/2）

| #id | verdict | 依据 |
| --- | --- | --- |
| #161 | fix | 成立（风险等级偏低但描述准确）：`Symbol.for` 键确为全局可猜。核实 isGeomStore 在 src 内无任何内部消费方（仅 core/index.ts、core/store/index.ts 两条再导出 + 测试），不承载权限判定，故不改成模块级 `Symbol()`——副本重复打包时会让 A 副本的 Store 在 B 副本下判非，正是选 Symbol.for 的原因。按报告第一方案落地：键名加包名命名空间 `@openlide/geomstore:brand`（刻意不带版本，带版本等于重新制造副本分裂），并在声明处写明可伪造性与「非安全边界」的理由 |
| #162 | fix | 成立：`uninstallFn()` 在映射已消费后裸调用，异常必然上抛且无法重试。不引入 try/catch（会把「清理未跑完」变成静默泄漏，也与 destroy() 已自行兜底的分工冲突），按报告第二方案把取舍写进注释：异常由调用方负责，destroy 路径与裸句柄路径口径差异一并说明 |
| #163 | fix | 成立：TS 会把 `unknown \| undefined` 折叠为 `unknown`，并集零信息且与同接口 `set(target, proxy: unknown)` 不齐。改 `get(target: object): unknown`，注释保留「未命中即 undefined，需显式 !== undefined 判定」；实现侧 createProxyCache 返回 `weakMap.get` 无需改动，typecheck 通过 |
| #165 | fix（更正报告前提） | 半失实：core/store/index.ts 确实再导出这 4 个类型，但该子路径不在 package.json 的 exports 映射（`.`/`./core`/`./integrations`/`./extras*`），包外取不到；`src/core/index.ts` 与根 `src/index.ts` 都没带上它们（`grep -n "StateProxyManager\|InternalStateProtectionConfig" src/core/index.ts src/index.ts` 无命中），称「已公开 API」过头。残余问题（注释笼统写「不对外暴露」与 barrel 再导出不一致）成立，按注释方向更正并写清稳定性口径；不删再导出（零行为变更） |
| #166 | fix | 4 个选项类型均存在且已 export（ActionManager.ts:19 / StateProxy.ts:34 / StoreCache.ts:19 / SubscriptionManager.ts:19），barrel 只出类不出选项确为不一致。按报告方案补 4 行 `export type`；同时记录前提偏差——消费者本就读不到该 barrel（见 #165），此改动价值在内部一致性而非新增公开面 |
| #167 | fix | 成立：type FactoryStoreConfig 之上叠了 3 块 JSDoc，只有紧邻的别名文档会被绑定，前两块（createStore 用法/示例 + 设计要点）为悬空注释。按报告方案归位：createStore 文档移到重载 1 的签名之上，设计要点降为 `//` 行注释（不声明绑定关系），别名文档留在别名上；文本内容未删减 |
| #171 | fix(仅文档，不改行为) | 成立但报告的现象对比失实：`[d, d]` 与 `{a:d,b:d}` 一样得到两个不同 Date（Date 分支在 visited 查询之后、且不回写），数组侧并不「保持共享」，真正的事实是 Date/RegExp **从不**参与别名保留。报告许可的第二方案落地：在 deepCloneState 文档写清新建实例、不登记 visited、扩展属性与 RegExp.lastIndex 丢失的口径与取舍（热路径省一次 WeakMap 读写；deepEqual 的 Date/RegExp 分支同样只看 getTime/source+flags，两处窄口径一致）。改 visited 登记属行为变更，low 不顺手做 |
| #172 | fix | 成立且实测复现：`String(new Proxy(fn,{get(t){if(t===Symbol.toPrimitive) return ()=>{throw new TypeError()}}}))` → "Cannot convert object to primitive value"（node 直接验证）。createMutationErrorMessage 在 _handleIllegalMutation 的 warn/silent 分支 return **之前**被调用，抛出即把「放行不抛」契约变崩溃点。按报告方案再套一层 try/catch，末级兜底只拼 typeof（typeof 不可能抛）。回归用例 #172 两条（宿主值不抛 + BigInt 仍走 String） |
| #173 | fix | 成立，且报告点名的注释确为失实：非纯对象分支一句「deepCloneState 对它们保留原引用/副本与活状态共享」只对 class 实例/Promise/WeakMap 成立，Date/RegExp/Map/Set 是被新建的（clone.ts 40-64）。已把两条理由拆开写（内建对象跳过的真因是 freeze 拦不住内部槽位 mutator），并按报告要求补「部分冻结」口径：deepFreezeState 的 @remarks + Store.$snapshot 的 JSDoc/行注释都写明经 Date/RegExp/Map/Set 与非纯对象触达的节点保持可变、Readonly<S> 只到类型层面。顺带更正「自有可枚举键恒为空」为「一般为空，扩展属性/子类字段是例外」 |
| #175 | reject | 现象（吞掉所有异常）成立，但报告的修法无效，予以否证：实测 Node 里 `getVersion` 非函数抛 "TypeError: Getter must be a function"、非法描述符抛 TypeError、覆盖不可配置属性也抛 TypeError，与要吞的「目标非可扩展」同为 TypeError，`instanceof TypeError` 判别**分不开**报告点名的三类调用方缺陷；唯一能被判出的是 Proxy defineProperty 陷阱抛的非 TypeError，而那正是最不该外溢的位置——defineStateVersion 在 setState/$patch 写入热路径上，抛出会把「拿不到版本就回退 deepEqual」的降级入口变成崩溃入口。保留现状，注释里写清否证依据与代价（缺陷静默退化为无版本 + deepEqual，只慢不错） |
| #184 | FP | 「.clear() 恒为 no-op」不成立：destroy 循环只删映射、**不** splice `_plugins`，而循环自己的注释（Store.ts:626-628）就把「清理函数重入再 use()」列为受支持场景——重入会向 _plugins/两张 Map 塞新条目，且 2~6 任一步抛错时步骤 1 之后的清理半途而废，步骤 7 是「teardown 结束前必须全空」的兜底闸门。两处职责不同（步骤 1 权威、步骤 7 兜底），删任一处都会漏清。已按报告的真实诉求（看不出哪个权威）在步骤 7 补注释说明分工，未删代码 |
| #187 | fix（行为变更） | 成立：setStateProtection 是唯一漏加守卫的写接口（`grep -n "_stateProtection\." src/core/store/Store.ts` 显示仅此处变更配置），销毁后仍可改配置并再分配一个 StateProxyManager，与 setState/$patch/subscribe/use/cache/batch 的 `_destroyed` 契约不一致。按报告补同款抛错（消息沿用 `[GeomStore] Cannot call X on a destroyed Store`），只读侧 isStateProtectionEnabled/getStateProtectionConfig 按报告口径豁免。全仓 `grep -rn setStateProtection src tests` 确认无销毁后调用点（4 处测试均在 destroy 前）。回归用例 #187 |
| #188 | fix(仅文档) | 成立且注释确为失实：destroy() 不清 GetterManager（`grep -n "_getterManager" src/core/store/Store.ts` 无清理点），销毁后 `store.getters` 返回初始化时登记的那份而非空对象。按报告第一方案更正文档（同时写明 `getter()` 会抛、`getGetterNames()` 返回 []，以及销毁后调用保留函数会读到未释放的 `_state`）。不在 destroy() 里清定义——那是行为变更且会破坏「销毁后调试可见形状」的用途。回归用例 #188 锁定现状 |
| #191 | fix（行为变更） | 成立：`setsEqual(currentA, currentB, maxDepth, seenPairs, pairLog)` 未带 depth，被调方 `compareWithSeenPairs` 的迭代栈恒从 0 起算，跨 Set 边界深度归零（与 Map 值分支的 depth+1 分叉）。落地：setsEqual 增 depth 形参、候选比较以 `depth + 1` 为基准（compareWithSeenPairs 增 baseDepth 形参，顶层调用默认 0 不变）。旧实现的绕过由 HEAD 源码可判定（compareWithSeenPairs 的栈恒为 `[{a,b,depth:0}]`，被 setsEqual 复用即每层 Set 归零），改后实测 `deepEqual(15 层嵌套 Set 副本, 10)` 为 false、`maxDepth=100` 为 true。行为变更仅限「超深结构由误判相等改为按保守语义判不等」，与既有文档承诺一致。回归用例 #191 三条（含 Map/Set 口径对齐） |
| #192 | fix(部分) | 告警噪音成立、语义不一致的前提已被 #191 顺带消掉（候选比较现在继承外层深度，返回 false 的保守中止与各分支口径一致，且该「超限返回 false」是 deepEqual 文档写明的既有契约）。按报告第一方案做去重：模块级 `depthWarningEmitted` 在顶层入口复位，一次比较内多候选多次命中只警一条（用例断言 1 条、第二次比较再出 1 条）。不采纳「可配置策略（false/抛错/截断）」——新增公开选项不属 low 范畴，且抛错会把热路径比较变成崩溃点 |
| #193 | fix | 成立：Map 键按 SameValueZero 引用匹配、值才深比，键结构等效但引用不同时恒判不等；行内注释虽已说明，但公开入口 deepEqual 的 JSDoc 未提，调用方只能在踩坑后从内部注释找原因。按报告第一方案在 deepEqual 的 @returns/@remarks 补约束、典型触发来源（反序列化/跨 store 克隆/JSON 往返）、可见症状（缓存永不命中的假不等）与规避方式，行内注释指回该文档。不提供可选的键深度匹配（要解多候选匹配，属新特性不属 low） |
