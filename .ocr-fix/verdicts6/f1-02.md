# f1-02（wave f1）
条数：7｜FIXED 7 / FP 0 / REJECT 0 / NEEDS-MAIN 0
（另有 3 项跨分片/跨权限配合，不另计 verdict，逐条见文末「跨分片配合」）

## R6-014 medium documentation examples/cache/03-dynamic-cache.ts:L29-L34
verdict: FIXED
改动: 建店补 `enableCache: true`（原 `_enabled` 恒 false → disableCache() 是空操作）；把批量写入包成导出的 `bulkImportCursor(count)`，写入键换成在缓存键集里的 `cursor`、收尾 `enableCache()`（不传 keys = 全键集）；文件头与函数注释按实测口径重写（写入是**写穿**、不失效；重建只有 enable 那一次；关缓存只对键集内的键有意义）；末尾补两次 `getCached` 让统计里真的有 hits。
回归锁: tests/unit/r6-f1-02-examples.test.ts > 「R6-014: 动态缓存示例的开关是真的开着缓存」> 两个用例（`bulkImportResult.before.enabled` / hits-misses 在关闭窗口内不涨）
判据: 语义探针——临时删掉 `enableCache: true` 后跑用例：`Expected: true / Received: false`（2 failed → 3 failed 那一轮）；整文件回退 HEAD 版则 8 failed（`listStore` 未导出、size 0）。
文档: none（示例内部修正，不动库 API）

## R6-015 medium bug examples/extras/selector.ts:L46-L52
verdict: FIXED
改动: 选择器体改为只用入参 `state.orders.find(...)`（不再绕 `orderStore.getState()`）；工厂返回值不再长期持有，导出 `selectDiscountedFor` + `selectDiscounted(id)`（每次以 `orderStore.getState()` 现场绑定）；注释写清两条约定与「跨 Store 走 createStructuredSelector / composeStore」的方向；`Order`/`OrderState` 导出供类型复用。
回归锁: tests/unit/r6-f1-02-examples.test.ts > 「R6-015: 参数化选择器示例只读入参 state」> `$replaceState 整树换新后读到与新状态一致的结果（附反例对照）`
判据: 用例本身带反例对照：旧写法（体内读活状态 + 持有绑定）在同一时序下算出 `200×0.9=180`，示例的新写法算出 `200×0.5=100`，两条断言同时钉住——把任一半反模式（体内绕开入参 / 长期持有绑定）搬回来都会红（分别得 180 / 90）。
文档: none

## R6-016 medium documentation examples/weapp/app-integration.ts:L36-L44
verdict: FIXED
改动: 给 `withAppStore` 补上第二个实参 `mapState: ['launchedAt','scene','cartCount']` + `mapActions: ['markLaunched']`，`onLaunch` 改用注入的 `this.markLaunched(...)` 并读 `this.globalData.scene`（notify 默认同步，实测 dispatch 后映射值即时可见）；注释与文件头从「全局数据挂载」改为「映射状态挂到 globalData + 映射 action 绑到实例」；同时导出 `appStore` / `appOptions` 并加宿主守卫（见 R6-070）。
回归锁: tests/unit/r6-f1-02-examples.test.ts > 「R6-016: App 集成示例的映射真的配了」> `onLaunch 里注入的 action 与 globalData 的映射状态都可用`
判据: 语义探针——临时把第二个实参摘掉后：`TypeError: this.markLaunched is not a function`（正是报告预言的那条），且 globalData 键集断言也会红（只剩 appName）。
文档: 建议 docs/SKILL 的 App 接入措辞无需改（示例只是把注释吹过的东西真的配上）；CHANGELOG 记「示例修复」纯修复。

## R6-017 medium bug examples/weapp/component-integration.ts:L26-L28
verdict: FIXED
改动: `increment` 改为 `this.$patch({ count: this.state.count + 1 })`（与同文件 add 同口径），并注明「写常量 0 等于把自增做成重置、count=0 时连订阅都不触发」；`mapActions` 补进 `increment`、`onTapPlus` 用它（另加 `onTapPlusTen` 演示 add）；导出 `counterStore` / `counterComponentOptions` + 宿主守卫。
回归锁: tests/unit/r6-f1-02-examples.test.ts > 「R6-017: Component 示例的 increment 是自增」> `increment 每次把 count 加一（修复前写成了常量 0，等于清零）`
判据: 语义探针——把 `$patch({ count: 0 })` 填回去后：`Expected: 1 / Received: 0`。
文档: none

## R6-070 low bug examples/weapp/index.ts:L5-L7
verdict: FIXED
改动: barrel 不再对三个示例做 `export * as`（静态再导出会在模块求值期执行示例体的宿主注册），改成纯清单/文档索引 `weappExamples`（name/path/requiresHost/description）；app-integration.ts 与 component-integration.ts 的 `App(...)` / `Component(...)` 包进 `typeof App === 'function'` / `typeof Component === 'function'` 守卫并导出自身 Store 与配置，两个文件单独 import 也在 Node 下安全。
回归锁: tests/unit/r6-f1-02-examples.test.ts > 「R6-070: weapp 示例 barrel 不在顶层求值宿主模块」> 两个用例（用例内先把 `Page`/`Component`/`App` 三个全局亲手删掉再 `jest.requireActual`，绕开 tests/setup.ts 装的 mock）
判据: 用 HEAD 版 barrel 实跑 → `Error message: "Page is not defined"`（1 failed / 8 passed）；修复后全绿。注：`typecheck:tests` 不含 examples/，所以示例侧宿主全局用 `jest.requireActual` 取，静态 import 会因 `examples/global.d.ts` 不在该程序内而报 TS2304。
文档: 属导出面变更（示例 barrel 的命名空间再导出去掉、换 `weappExamples` 清单）→ CHANGELOG 写「examples/weapp barrel 改为清单索引，不再静态再导出宿主示例（修复 Node 下 import 即抛 ReferenceError）」；`examples/index.ts` 头一句「含 Page/Component/App 的示例需在微信小程序环境执行」宜补一句「barrel 不再求值它们」。

## R6-018 medium test package.json:L92-L95
verdict: FIXED
改动: 新增版本漂移门禁用例 `tests/unit/r6-f1-02-skill-version-sync.test.ts`：以 package.json 的 version 为事实来源，钉住 `hot-update.ts` 的 `LIBRARY_VERSION`、SKILL.md 三处（frontmatter description / 正文「当前版本」/ 文末「当前对应」）、`references/api/*.md` 全部「来源版本」行（并先卡文件数 ≥12，防生成器漏跑）。锚点匹配不到即抛错，不让漂移伪装成「无此条目」。
回归锁: 该用例本身即锁。判据（修复前的门禁缺失）：`git grep -c "codebuddy" HEAD -- tests` 无输出（0 命中），现在 `grep -rln codebuddy tests` → 1 个文件。四处当前值实测都是 0.6.1 → 用例 4 条全绿；漂移任一处必红。
package.json 未改动（理由）: `prepublishOnly` 已含 `pnpm test`，用例因此自动成为发版门禁；按说明书不顺手动版本号 / scripts 顺序；CI 侧的生成物同步需要新脚本与 workflow，越界。
文档: CONTRIBUTING.md 发版第 4 步补一句「漏改 SKILL.md 三处或漏跑 `pnpm run skill:api` 现在会让 `pnpm test` 变红」；CHANGELOG 记「纯修复：新增版本号四处同步门禁」。

## R6-019 medium bug packages/benchmark/src/config.ts:L53-L53
verdict: FIXED
改动: 从 `defaultBenchmarkConfig.scenarios` 删掉 `concurrent-access`（报告给的第二个方向），并在场景数组上方写清约束「只列 runner 真会执行测量的档」+ 复现路径（全包无一处读 `scenario.concurrency`、runner 从未调用 `benchmarkUtils.parallel`、与 medium-workload 同数据集同轮转、无效值随 `report.config` 发布）。`relaxedBenchmarkConfig` 由 `defaultBenchmarkConfig.scenarios` map 派生，自动跟着变 6 档。
回归锁: tests/unit/r6-f1-02-benchmark-scenarios.test.ts > 「R6-019: 无实现的并发档不再进入默认场景集」> 三个用例（默认档名单、两套配置都不声明 concurrency、宽松档与默认档一一对应）
判据: HEAD 版 config.ts 实跑 → `Received + 11 / "concurrency": 10` 等 3 条全红；修复后 3 passed。
文档: 行为变更（默认场景集 7 → 6 档、报告 summary 的场景数随之变）→ CHANGELOG 需注明并提醒按旧场景名比对报告的人；`packages/benchmark/README.md`、`docs/**` 实测都无 `concurrent-access` 提及（grep 无命中），无需同步措辞。

## 跨分片配合（不占 verdict）
1. `packages/benchmark/src/types/index.ts:53` 的 `BenchmarkScenario.concurrency`：仍是公开类型成员、赋值即静默空转（报告 ①后半）。该文件不在本分片（f1-03 认领 packages/benchmark/src/{constants,helpers,index,reporter,smoke,utils}，types 目录未见任何分片）→ 请主会话裁定：接进 runner（见 2）还是删成员/标 `@deprecated`。删成员是 breaking change，建议与 f2-12 的 runner 改造同一批决定。
2. runner 侧把 parallel 接进 `runScenario`：`packages/benchmark/src/runner.ts` 是 hot 文件 + f2-12 分片（R6-…「配置面声明的多个测量维度无人读」）所有 → 与 f2-12 交接：若那边实现并发分发，本分片的 `tests/unit/r6-f1-02-benchmark-scenarios.test.ts` 第二条（不声明 concurrency）要同步改成「声明 concurrency 的档必须真并发」。
3. `examples/weapp/page-integration.ts`：全仓 `grep "codebuddy"` 之外，该文件不在任何分片的涉及文件里，却是 barrel 三个再导出中唯一还没加宿主守卫的（顶层两次 `Page(...)`，报告 R6-070 的最后一句也没落地）。本分片已把 barrel 改为不再静态 import 它，所以 Node 侧安全；请主会话安排：按 app/component 同款加 `typeof Page === 'function'` 守卫 + 合并成一次注册（或拆两个页面文件），并把 `weappExamples` 里的条目补回再导出。
4. CI 的「生成物必须与 dist 同步」：报告给的更彻底方案（`pnpm build && pnpm run skill:api` 后 `git diff --exit-code -- .codebuddy`）落在 `.github/workflows/ci.yml`（f1-01 认领）。本分片只做了不依赖 dist 的版本号四处对齐；生成物内容对齐请 f1-01/主会话加那一步，并保证 CI 里 dist 不被 `git clean` 影响判定。

## 自验（收工前实跑）
```
$ npx jest tests/unit/r6-f1-02-examples.test.ts tests/unit/r6-f1-02-skill-version-sync.test.ts tests/unit/r6-f1-02-benchmark-scenarios.test.ts --ci
Test Suites: 3 passed, 3 total
Tests:       16 passed, 16 total

# 先失败后通过的探针（三组，跑完已全部还原，sha256 对账一致）
# HEAD 版 4 个文件回退 → Tests: 8 failed, 4 passed
# 旧 barrel（无宿主全局）→ Error message: "Page is not defined"
# 摘掉 enableCache: true / withAppStore 第二实参 / increment 写回 0 → 3 failed（Expected true/Received false 等）

$ npx eslint --max-warnings 0 <8 个 examples/tests 文件>   # 通过（无输出）
# 注：packages/benchmark/** 被 eslint 配置忽略（报 "File ignored"），不在 lint 范围
$ npx prettier --check tests/unit/r6-f1-02-*.test.ts       # All matched files use Prettier code style!
# 注：examples/** 与 packages/** 不在 CI 的 prettier 门禁 glob 内（.github/workflows/ci.yml:72
#     只查 "src/**/*.ts" "tests/**/*.ts"），且 HEAD 版同目录文件同样不被判定为已格式化
$ npx tsc --noEmit -p tools/tsconfig.examples.json | grep ^examples/   # 无输出（本分片文件干净）
$ npx tsc --noEmit -p tsconfig.tests.json | grep -E "^(examples|tests/unit/r6-f1-02)"  # 无输出
# 两条 tsc 全量跑仍有其他分片在途文件的报错（src/core/performance/__r6old_probe.test.ts、
# tests/unit/hot-round6.test.ts 等），非本分片产物，未触碰
```
未跑：全量 jest / pnpm build / pnpm test（按说明书交主会话统一门禁）。
