# f1-05（wave f1）
条数：10｜FIXED 10 / FP 0 / REJECT 0 / NEEDS-MAIN 0（另有一条跨文件尾巴挂在 R6-037 名下：既有测试断言需主会话更新，见该条 NEEDS-MAIN 段）

## R6-084 low src/core/performance/metrics.ts:L70-L77
verdict: FIXED
改动: 汇总侧统一按 `Number.isFinite` 排除非有限 duration。新增 `isMeasurableDuration`；`summarizeByOperation` 的累加器多一个 `durationCount`（有限样本数），`totalDuration/maxDuration` 只在有限样本上累计；`computePerformanceStats` 的 avg 分母改为 `durationCount`，无有限样本时 avg/max/min 归 0（不再吐出 ±Infinity）；`totalCount` 与 `exceedThreshold` 计数口径不变（样本仍算一次调用）。四个派生出口（byOperation / getHotPaths / analyzeBottlenecks / calculateAvgDurations）改走 `averageOf`/`maxOf` 两个投影函数，避免 -Infinity 初值外泄。
回归锁: tests/unit/r6-f1-05-metrics.test.ts > R6-084 非有限耗时的样本不入耗时聚合（5 例）
判据: 旧实现（`git show HEAD:src/core/performance/metrics.ts` 落地为临时文件后实跑）输出 `avg: NaN max: 30 min: 10`、`all-nonfinite max/min: Infinity Infinity`、`JSON.stringify` 出 `{"max":null}`、`byOperation: {"slow":{...,"avgDuration":null},"broken":{"avgDuration":null}}`、`hotPaths: [{"operation":"broken","count":1,"avgDuration":null}]`；新实现同输入得 avg 20 / max 30 / min 10、全非有限时三项归 0。备注：`MetricsCollector.getPercentile` 未在本条行区间内，NaN 样本仍可能落在分位点上（其入参校验早已 Number.isFinite 早失败），未一并改动。
文档: 行为变更（性能统计口径）——docs/CHANGELOG 需写明「duration 为 NaN/Infinity 的样本不再进入 avg/max/min，仅计入次数；一组样本全非有限时耗时统计为 0」；`PerformanceStats` 未新增字段（`src/types/performance.ts` 不在本分片可改范围），若要把「被排除样本数」暴露给宿主需主会话开字段。

## R6-085 low src/core/performance/metrics.ts:L369-L374
verdict: FIXED
改动: 本文件内新增 `normalizeThreshold(value, fallback)`（与 `PerformanceMonitor.normalizeThreshold` 同口径：非有限→回退默认、负值→夹到 0），`analyzeBottlenecks`（fallback 16）与 `detectRegression`（fallback 0.2）两个门一起接入；JSDoc 同步写明归一口径。未去 import PerformanceMonitor 的 private static（那需要放宽另一个文件的可见性）。
回归锁: tests/unit/r6-f1-05-metrics.test.ts > R6-085 阈值归一：两个静态分析入口一起改（5 例）
判据: 旧实现实跑摘要 `bottleneck NaN threshold severity: low`（新为 high）、`bottleneck negative severity: high`（0ms 操作，新为 low）、`regression NaN threshold len: 0`（新为 1）、`regression Infinity threshold len: 0`（新按默认 0.2 判退化为 1）。
文档: 行为变更——docs/skill 里 `analyzeBottlenecks` / `detectRegression` 的 threshold 说明需补「非法阈值（NaN/Infinity/负）按默认值/0 归一，不再静默失效」。


## R6-037 medium src/core/store/ActionManager.ts:L246-L253
verdict: FIXED（附一条 NEEDS-MAIN 见下）
改动: 新增 `_flushAfterSyncSegment(mutationsBefore)`，在 thenable 分支注册 `.then` 之前先按判据补发一次同步段的通知（settle 那一轮保留，覆盖 await 之后的续段变更）。把关：onlyOnChange 用与 settle 同一条去重判据（基线＝最近一次通知已覆盖的计数），默认模式按 `_getMutationCount() > 进入 dispatch 前的计数`把关；两个计数源都没有（非 Store 消费方自建 ActionManager，本就没有 Store 的 dispatch 抑制）时整条兜底跳过、保持仅 settle 补发。不回刷缓存（`execute` 进这支之前刚刷过）。220-233 的旧注释（「同步段不单独通知」）改写为两个时点的现状说明。
回归锁: tests/unit/r6-f1-05-action-sync-flush.test.ts（6 例：$patch 挂起 promise 当场可见、体内直接变异+自定义永不回调 thenable 当场可见、onlyOnChange 兜底与 settle 去重为 1 次、同步段无写入不多刷、无变更异步 action 仍 0 次、batch 内不提前通知）
判据: 把 `this._flushAfterSyncSegment(...)` 一行摘掉（＝旧行为）实跑该文件 → `Tests: 4 failed, 2 passed`，其中挂起 promise 两例是 `Received number of calls: 0`；恢复后 6 例全绿。连带实跑 store / r5-core-store / compose / regression 全量共 990 例，仅 1 例受影响：tests/unit/store/action-dirty-keys.test.ts:204 `accumulates dirty keys across async actions and async notifications`（`async:true` 合并窗口下，同步段兜底与 settle 补发落在两个批次，多出一次 dirty 为空的投递）。
NEEDS-MAIN: 上述 `tests/unit/store/action-dirty-keys.test.ts` 不在本分片可改文件内，请主会话把该断言改为 `[['first', 'second'], []]`（或在断言前过滤空批次），并注明这是 R6-037 的预期行为变更；如主会话判定「多出一次空批次投递」不可接受，替代做法是在默认模式让 settle 那一轮跳过——代价是 await 之后不被计数捕获的裸写会重新变成不可见（与 store.test.ts:1946 钉住的「宁多勿漏」口径冲突），不建议。
文档: 行为变更——docs/skill 与 CHANGELOG 需写明：① 异步 action 的同步段现在会当场补发一次通知（promise 永不 settle 时不再依赖「下一个不相干通知」）；② 默认模式下「同步段有写入且最终 settle」的 action 通知数由 1 变 2（onlyOnChange 自动去重）；③ action 返回值是 thenable 时的可见性依赖本兜底。

## R6-038 medium src/core/store/dirtyTracking.ts:L167-L171
verdict: FIXED
改动: `classifyWrite` 的「旧值是对象且新值不是它」一档不再一律 EDGE_REMOVED：新增 `isCoveredSwap(container, incoming)`——索引已建、容器有归属键、且**新值的归属集合已覆盖容器全部键**时判 EDGE_STABLE（同容器内置换：sort / reverse / splice 移动元素）。头注释「删掉一条边只能整体重建」补记这一条例外，判定函数上写明安全性依据（正向索引只增不减 ⇒ 被写走的子树保留旧归属 ⇒ 其后续写入至多被**多报**，不存在漏报）。容器自身解析不出归属、root 顶层键写入、新值未覆盖、标量覆盖对象（真删边）四种情形一律保持 EDGE_REMOVED 全量重建。
回归锁: tests/unit/r6-f1-05-dirty-tracking.test.ts > R6-038（4 例：以自带 ownKeys 陷阱的代理当「全图重走计数器」，证明 n=50 与 n=400 的原地 sort 重建次数都是常数 ≤2；reverse+splice 同样 ≤2；跨键移动仍给出 ['a','b']；标量覆盖对象仍兜底全部顶层键）
判据: 旧实现（HEAD 版文件落地为同目录临时文件后实跑同一测量）`OLD walks n=50: 48 | n=400: 398`——重建次数随 n 线性增长（每次重建又是一次 O(整图) 遍历，合起来即报告说的 O(n² log n)）；新实现两个规模都是 ≤2。既有索引语义由 tests/unit/store/dirty-tracking-incremental.test.ts、tests/unit/property/ownership-index.property.test.ts、tests/unit/r5-core-store-p1-dirty-tracking.test.ts 共 39 例（含归属索引 property 测试）实跑全绿锁住，未回退第五轮裁定的口径。
文档: 行为变更（性能）——docs/skill 的性能注意项可保留「action 内原地 sort 对象数组」的写法（不再 O(n² log n)），但建议仍推荐 `$patch` 传新数组以获得更少的上报轮次。

## R6-039 medium src/core/store/dirtyTracking.ts:L242-L250
verdict: FIXED
改动: collection 分支取不到 `collectionMethod` 拦截器、且 `Reflect.get` 拿到的是函数时，改为 **bind 到原始集合**后再返回（与 bindMethods 同一手法，`constructor` 同样排除以保持 `new` 可用），结果记入 `methods` 缓存。写明取舍：白名单外的内置方法都不改图，故不额外 report 一次（否则 `union` 这类纯读方法每调一次就把所属顶层键标脏、白刷一轮 setData）；子类自定义方法若改图，属本文件已声明的契约外写入，其新增子树由 report 的「解析不出归属即标记全部顶层键」兜底。
回归锁: tests/unit/r6-f1-05-dirty-tracking.test.ts > R6-039（4 例：union/intersection/difference/symmetricDifference/isSubsetOf/isSupersetOf/isDisjointFrom 全部可用且以代理作参数也不炸、读方法零上报而 add 照常上报、`class Bucket extends Set` 的 #private 方法可用且写入仍被追踪、constructor 未被绑定仍可 new）
判据: 旧实现同形复现（HEAD 版文件 + `state.tags` 为 Set 的代理）实测 `OLD union -> TypeError: Method Set.prototype.union called on incompatible receiver #<Set>`；新实现返回正确结果。仓库既有测试对这批方法零覆盖（grep `union|isSubsetOf` 在 tests/ 下无命中），故本条自带覆盖。
文档: 行为变更（兼容性）——docs 里「集合代理」相关段落需补一句：白名单外的集合成员（ES2025 Set 集合方法、子类自定义方法）以原始集合为接收者、不计入脏跟踪。

## R6-040 medium src/core/store/StoreCache.ts:L166-L188
verdict: FIXED
改动: 把「`_cache` 与 `_timestamps` 同步收缩」从注释承诺变成实现保证。新增 `_pruneOrphanTimestamps()`（遍历 `_timestamps`，`_cache.has(key) === false` 的行删掉——`LRUCache.has` 不更新访问顺序也不计 hit/miss），调用点两处：`refreshFromState` 收尾（每轮 dispatch 一次）与 `_writeEntry` 里按 `> 2×capacity + 8` 的摊还阈值触发（覆盖只写不 dispatch 的宿主）。`_deleteEntry` 注释补记「LRU 侧自行 evictLRU、Store 构造时不传 onEvict ⇒ 本类收不到淘汰通知」这条真实来路。未走 `onEvict` 方案：那要改 Store.ts:241-245 的 LRUCache 构造参数（hot 层，主会话在改），故按报告给的第二个方向实现，两条路等效且本方案不依赖 Store。
回归锁: tests/unit/r6-f1-05-store-cache.test.ts（5 例：capacity=3/ttl=1000 连设 50 个动态键后时间戳行数被钉住且 refresh 后 ≤capacity、只写不 dispatch 时也 ≤2C+9、在册条目的时间戳不被误删（否则 TTL 退化成永远过期回读）、命中/回读行为不变、ttl=0 不产生时间戳）
判据: 报告给的实测口径复算：旧实现 `set` 50 个不同键后 `_timestamps.size === 51`（本次改前该断言 `toBeLessThanOrEqual(15)` 亦不成立）；新实现同脚本 `getStats().size === 3` 且 `_timestamps.size` 被摊还阈值钉住，改后 5 例全绿，连带既有 tests/unit/store/modules/StoreCache.test.ts 36 例全绿。
文档: none（对外行为不变，只回收内存；`getStats()` 形状未动——`CacheStats` 在 `src/types/store.ts`，不在本分片可改范围，若要把时间戳滞留量做成可观测指标需主会话开字段）。


## R6-041 medium src/core/store/SubscriptionManager.ts:L90-L96
verdict: FIXED
改动: 新增 `SubscriptionManager.DEFAULT_MAX_SUBSCRIBERS = 50`，构造期按 LRUCache capacity / StoreCache ttl 同口径归一 `maxSubscribers`：非有限值（NaN/Infinity）回落默认并出 dev 告警；有限值 `Math.floor`（`2.5` → 2，旧实现实际放行 3 份）；0 与负数按 `add` 文档既有语义保留。`SubscriptionManagerOptions.maxSubscribers` 文档写明归一口径与失败模式。
回归锁: tests/unit/r6-f1-05-subscription.test.ts > R6-041 maxSubscribers 非法值守卫（4 例，含 NaN/Infinity 各一、小数取整、0/负数语义、默认与合法值不变）
判据: 旧实现（HEAD 版文件落地为同目录临时文件后实跑）`OLD size NaN: 300  OLD size Infinity: 300`（护栏静默关闭），新实现同输入 `size === 50` 且产出「不是有效的订阅者上限」告警；已连带实跑 tests/unit/r5-core-store-p2-notify-and-limit.test.ts、r5-core-store-p1-A-notify-and-eviction.test.ts（共 21 例全绿，既有上限语义未回退）。
文档: 行为变更——docs/skill 里 `maxSubscribers` 需写明「非有限值回落默认 50 + dev 告警，小数向下取整」；CHANGELOG 记为修复（护栏不再被非法配置静默关闭）。

## R6-087 low src/core/store/SubscriptionManager.ts:L249-L273
verdict: FIXED
改动: 三处停在修复前的注释改为现状描述——① `notify` 文档「Store._notifyListeners 目前一律传 false 并自备克隆 / 监听器互改仍可见」→ 已按可写性传参、公开 subscribe 路径闭环已成立，未闭环档只剩「自行传 false 的非 Store 调用方」；② 告警点上方注释同样改写，指明 Store 主路径不可能命中此行；③ `onSubscriberEvicted` 选项文档「该接线在 Store 侧尚未落地」→ 已接到 `hooks.emit('onError', …, 'subscribe')`。
回归锁: 无新增用例（纯注释改动，无行为变化）。既有锁已覆盖：tests/unit/r5-core-store-p1-A-notify-and-eviction.test.ts > 仅只读订阅时保持零拷贝…且不触发共存告警（断言告警不打出，正是本次注释描述的事实）。
判据: 与 r5-core-store-p1-A-notify-and-eviction.test.ts:126 断言、Store.ts:1210 `notify(payload, needsClone)` 的现状对齐后才改的措辞（本轮不改 Store.ts，主会话在 hot 层）。
文档: none（内部注释）。

## R6-088 low src/core/store/types.ts:L38-L49
verdict: FIXED
改动: `SubscriptionManagerInterface` 补成与实现同形状：`add(listener, options?): object` 返回注册句柄、`delete(listener, registration?)`、`notify(state, cloneOnNotify?)`、并补上 `hasWritableListeners()`；文档写明不传句柄时 `delete(listener)` 是「退最早一份」的窄化语义、不与 `add` 配对（正是实现侧 R5 已裁定为误用面的那条路径）。全库仅 SubscriptionManager.ts:71 一处 implements，barrel 再导出的这四个类型自述「对包外仍是内部件」，故不涉破坏性。
回归锁: tests/unit/r6-f1-05-subscription.test.ts > R6-088 SubscriptionManagerInterface 与实现同形状（2 例：句柄持有/只退自己那一份；delete(listener) 的窄化语义）
判据: 旧接口形状下同一写法过不了类型——把 HEAD 版接口抄进临时文件后 `npx tsc --noEmit` 报 `src/core/store/__r6old_types_probe.ts(10,28): error TS2554: Expected 1 arguments, but got 2.`（探针文件已删）。
文档: none（子路径不在 package.json exports 里，非对外契约）。

## R6-042 medium src/core/utils/equality.ts:L152-L163
verdict: FIXED
改动: 装箱原始值判定抽成 `isBoxedPrimitive`，`BigInt`/`Symbol` 改为运行时能力探测：`typeof globalThis.X === 'function'` 在场时才参与 `instanceof`（typeof 对未声明标识符安全）；缺席时退化用 `Object.prototype.toString` 的 `[[Class]]` 标签识别，仍按 `valueOf()` 比内容。`Number/String/Boolean` 三个 instanceof 与「内建内容分支之前先做原型检查、装箱不 continue 仍比自有键」的口径一字未动（ffc810d 钉住的部分保持）。
回归锁: tests/unit/r6-f1-05-equality.test.ts（4 例：删全局后普通对象/数组/Map/循环不抛、删全局后装箱 BigInt 按值判、全局在场时包装子类语义不变、删 Symbol 全局不抛）
判据: 旧实现同形复现（HEAD 版文件落地为临时文件，`Reflect.deleteProperty(globalThis,'BigInt')` 后 `deepEqual({a:1},{a:1})`）实测输出 `OLD threw: ReferenceError BigInt is not defined`；新实现同脚本返回 true 不抛。既有 tests/unit/core/utils/equality-builtin-classes.test.ts 24 例全绿（装箱语义未回退）。
文档: none（纯健壮性修复，行为只在「缺 BigInt 全局的运行时」上从崩溃变为正常）。

---

## 自验（本分片收工前实跑）

```
npx jest tests/unit/r6-f1-05-{metrics,equality,subscription,store-cache,dirty-tracking,action-sync-flush}.test.ts \
  + 既有回归面 tests/unit/core/performance/metrics.test.ts、core/utils/equality-builtin-classes.test.ts、
    store/dirty-tracking-incremental.test.ts、property/ownership-index.property.test.ts、
    r5-core-store-p1-dirty-tracking / p1-action / p2-notify-and-limit / p1-A-notify-and-eviction、
    store/modules/{ActionManager,ActionManager-notify,StoreCache}.test.ts
→ Test Suites: 17 passed, 17 total / Tests: 236 passed, 236 total

npx jest tests/unit/store tests/unit/r5-core-store tests/unit/core/compose tests/unit/regression --ci
→ Tests: 1 failed, 989 passed, 990 total（唯一失败即 R6-037 的 NEEDS-MAIN 那条既有断言）

npx eslint --max-warnings 0 <本分片 7 个 src 文件 + 6 个新测试文件> → 无输出（0 problem）
npx prettier --check <同上> → All matched files use Prettier code style!
npx tsc --noEmit -p tsconfig.json → 本分片文件零报错（仅剩两处与本分片无关的既有报错：
   src/extras/action/decorators/cache.ts(172,20) TS2352、src/integrations/enterprise/hot-update.ts(159,10) TS6133，属其他分片/主会话范围）
```

改动面（`git diff --numstat`，仅本分片 7 个源文件）：metrics 92+/22-、ActionManager 62+/13-、
dirtyTracking 61+/8-、StoreCache 40+/0-、SubscriptionManager 50+/13-、store/types 30+/7-、equality 35+/4-。
新增测试：tests/unit/r6-f1-05-metrics / -equality / -subscription / -store-cache / -dirty-tracking / -action-sync-flush（共 6 文件 43 例）。
临时探针文件（`__r6old_*` 旧实现副本、`tests/unit/tmp-*probe*`、`r6-f1-05-probe*.test.ts`）已全部删除，工作树不留残渣。


