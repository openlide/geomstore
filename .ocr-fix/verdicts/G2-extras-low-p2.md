# G2-extras low p2 判定记账（20 条 · src/extras · 分片 2/2）

| #id | verdict | 依据 |
| --- | --- | --- |
| #283 | fix(仅文档) | 成立：`createRetrySelectorAsync` 的 JSDoc 写「延迟/退避/中止能力由本异步变体承载」，但全文件无 `signal`/`AbortSignal`（`grep -n "中止\|abort\|Abort" src/extras/selector/retrySelector.ts` 只命中该行注释），循环里也没有任何取消读取点。新增取消选项属新特性、不改 low → 按报告第一方案删掉「中止」并写明真实口径：已在执行的 selector 无法打断，提前停止只能由 `shouldRetry` 返回 false 表达 |
| #288 | fix(行为变更) | 成立且比报告点更靠前：`value instanceof Date/RegExp/Map/Set` 与 `Object.getPrototypeOf` 同走 [[GetPrototypeOf]]，故 Proxy 陷阱抛错时探针在 clone.ts:303 就冲出，`getPrototypeOf`@421 只是第二处。实测（修复前首轮跑测）`createSnapshot({bad:proxy})` 只落一条 `unknown`@root、onError 没被咨询；根节点为陷阱 Proxy 时更是连失败结果都不交付（异常从 createSnapshot 抛出）。按异步路径（clone-async 的 85-204 同构保护）把「类型判定 + 外壳构造」整体纳入同一 try → handleCloneError（cloneError@本节点 path → 咨询 onError → 继续则 SKIP）；`git diff -w` 只有 +20/-2，其余 124 行改动全是缩进。**连带必要修复**：SnapshotManager.getDataType 也用 instanceof，它在结果组装阶段被调用（成功 138 / 失败 172，异步路径 505/539 同），抛出会在出口处重新制造同一个洞 → 其 instanceof 段加 try/catch 回落 typeof。回归用例 #288 三条（顶层记 cloneError@root / 嵌套记 root.list[1] 且 depth=2、兄弟元素保留 / onError 返回 false 只咨询一次） |
| #289 | fix | 成立：index.ts 的 10 个公开类型全从 `./SnapshotManager.js` 中转，而 SnapshotManager.ts:19 自己写着「仅为保持既有导入路径不变」；SnapshotDiff 更特殊——它定义在 diff.ts，SnapshotManager 只在 585 行尾部再导出一次，中转链断一环 extras/snapshot 的 .d.ts 就静默缺项。按报告方案改为从定义处再导出（types.js + diff.js 两条），入口运行时值导出不受影响；`tsc -p tsconfig.tests.json` 通过，回归用例 #289 从 barrel 同时 import 这 5 个类型作编译期护栏 |
| #292 | fix(仅文档) | 成立：对象参数侧 `objectParamsCache` 是 WeakMap，只有读侧 TTL 判定、没有清扫也没有上限（`maintainPrimitiveCache` 只作用于 primitiveParamsCache，`maxEntries` 的 JSDoc 也已写明「原始类型参数」）。给对象侧补容量需要另存键列表（WeakMap 无 size/无迭代），把弱引用换成强引用反而制造泄漏 → 按报告第二方案把不对称、复用对象原地改内容的陈旧结果口径与不补上限的理由写进 JSDoc。改淘汰策略属行为变更，low 不顺手做 |
| #295 | fix(仅文档) | 成立：`getCacheStatus().cacheHit` 返回 `this.cache`（最近写入或被 findCacheHit 提升为当前的那条），而紧邻 JSDoc 的示例打印成 `Last cache hit:`，与 findCacheHit 可能返回 history 任意一条的事实不符。字段名是公开 API（`cacheHit` 改名即破坏性变更，报告的第一方案不做）→ 只更正文档：写明它与 hasCache 同源、不是「最近命中的那条」，示例标签改为 `Current cache entry:` |
| #296 | fix | 成立：`withCacheResult` 与 `execute` 各写一份「findCacheHit → selector → updateCache」，只差返回包装（findCacheHit 抽出前就漂移过一次）。按报告方案新增 `private resolve(state): SelectorResult<R>`，两个入口都委托它；`SelectorResult` 恰为 `{value, fromCache}`，返回类型直接复用无新增形状。等价性护栏见回归用例 #296（历史条目命中时两者同值且 fromCache=true、cache:false 时每次都重算） |
| #297 | fix(仅文档) | 成立：参数类型每键可选 + 运行期 `typeof selector === 'function'` 才写入 + 返回值断言成完整 R，三者叠加使「R 声明必填、映射省略/放非函数」在类型上不报错而运行期缺键。收紧为完整映射（`{[K in keyof R]: Selector<S,R[K]>}`）或返回 Partial 都会破坏既有公开面，改告警又会把受支持的稀疏映射（tests/unit/extras/selector/createSelector.test.ts 有「应该支持部分选择器」用例）变成日志噪音 → 按 createObjectSelector 已有的 ⚠️ 块风格，把可选/静默跳过/断言三件事与不收紧的理由写进 JSDoc，并在 @param 上标注 |
| #301 | fix(仅注释) | 成立：原 ignore 文案「任务处理是同步的」确实把结论挂在 processNodeAsync 当下是同步实现这一细节上。核实可达性：定时器回调只在栈空时执行，enqueue 的全部调用点都在 processNodeAsync 内、批内 for 循环无 await，唯一的 await 在批末且被外层 while 的 !hasTimedOut 重判 → else 当下确实走不到。不删 escape hatch：jest.config.js 有 global branches 95 + `./src/extras/snapshot/**` 分文件阈值，留个未覆盖单侧会直接压破 test:ci → 保留守卫与标注，把注释改成运行期无关的不变量表述（声明「只是当下调用形状不可达、批内一旦让出本分支即成为超时丢弃任务的生效点」） |
| #302 | fix(命名常量 + 文档) | 成立：`depth > 10` 与 opts.maxDepth（默认 100）无关联，深结构的 totalCount 系统性偏小、percentage 提前饱和 100。不跟随 maxDepth：估算本身是入口处一次额外前序遍历，抬到 100 等于把估算做成与克隆同量级的第二遍扫描，而它唯一的用途是给进度条一个分母 → 按报告第二方案：魔法数改为具名 `MAX_ESTIMATE_DEPTH` 并写明不跟随的理由与代价，同时在 SnapshotProgress.total/percentage 的类型文档注明「近似值、不可当完成判据」 |
| #306 | fix | 成立：AsyncSnapshotOptions 在已继承 `SnapshotOptions.batchSize` 的情况下又重声明一遍同名同类型成员（types.ts:198-199），两侧后续任一侧改动都可能静默漂移。按报告删掉重声明、只留 batchInterval/timeout（async 收窄为 true 保留）；继承链使 `Required<AsyncSnapshotOptions>.batchSize`（SnapshotManager:214 与 clone-async 的签名）照常存在，两个 tsc 均零输出，回归用例 #289/#306 里 `{ async: true, batchSize: 2, batchInterval: 0 }` 仍编译且取值不变 |
| #307 | fix | 成立：注释写「弱引用集合」而类型是 WeakMap，且两侧 clone 路径都 `set(value, cloned)`、循环命中时 `visited.get()` 读回的就是该节点的克隆，`unknown` 等于没契约。核实值域：登记的只有 Date/RegExp 之外的容器壳与对象壳（原语直返、customCloner 命中早退不登记）→ 值收窄为 `object`（SnapshotManager 两处 `new WeakMap<object, unknown>` 同步改为 `<object, object>`），注释改写清「源对象 → 其克隆」与为什么不用 WeakSet；CloneContext 的读取侧（clone/clone-async）不受影响，两个 tsc 零输出 |
| #309 | fix(部分·行为变更) | 成立：回调内 `selector(state)` 同步重入时那次调用会复用未清空的 currentPromise 并另起定时器覆盖 timeoutId，而 finally 无条件 `timeoutId = null` → 新定时器失去句柄、后续调用 clearTimeout 不到它、它再在已结算槽位上空跑一次（读码推得修复前执行序 [1,3,3]）。按报告建议落地保守版本：回调开头记下 firedTimer，finally 只在 `timeoutId === firedTimer` 时清空（既有 3 条 debounce 测试全绿）。报告点名的另一半「重入调用拿到旧值」不改：要先认领槽位才能让重入调用拿到自己的 promise，代价是无人 await 时其 rejection 变成 unhandled rejection，超出 low 幅度；残余口径已在 currentPromise 复用处注明并列入待办。回归用例 #309 两条（重入后 [1,3]/常规防抖语义不变） |
| #311 | fix | 成立：`pipe(...(selectors as [never]))` 把 createDerived 的类型检查整个丢掉（`[never]` 可赋给任意 rest，连首参不是接受 state 的选择器都查不出）。按报告第二方案（对共用实现编程）做：新增 `private static runPipe(selectors)` 承载 reduce 实现，pipe 与 createDerived 的实现的签名都只调它，断言随之消失；运行期与两入口的公开重载签名均未变。等价性护栏见回归用例 #311 |
| #312 | fix | 成立：combine 的 @example 里 `selectors` 数组第二、三项之间漏逗号（`(s) => s.taxRate` 换行直接跟 `(s) => s.shipping`），复制即编译失败。补逗号；示例算式 `100 * 1.1 + 10 = 120` 核对无误 |
| #313 | fix | 成立：createArraySelector 的 @example 写 `(item) => item.value * 2` 配 `[1, 2, 3]`，数字元素没有 .value → 实际得 `[NaN, NaN, NaN]`，与注释里的 `[2, 4, 6]` 不符。改为 `(item: number) => item * 2`（顺带补显式类型参数让示例可独立编译） |
| #315 | FP | 前提失实：报告称「Map/Set 结构匹配用的 deepEqual 走 Object.is，故 0 与 -0 判不等」。读 src/core/utils/equality.ts:98，fast path 是 `currentA === currentB \|\| Object.is(currentA, currentB)`——`0 === -0` 为 true，Object.is 只是补 NaN；241 行 Set 分支同理。故两条链路的相等语义本来就一致（=== 判 0/-0，Object.is 补 NaN），不存在「同一组值一边无差异一边报 added/removed」。实测锁定：`compareSnapshots` 对 `{v:0}` vs `{v:-0}`（属性链路）与键为 `{v:0}` vs `{v:-0}` 的 Map（结构匹配链路）都报 changed=false/Set 的 NaN 键也不报差异，同时真实差异仍出 added+removed —— 见回归用例 #315，未改代码 |
| #316 | fix | 成立：diff.ts:40 把 `SnapshotDiff.changes` 的元素类型原样内联抄了一遍，kind 枚举日后扩展要改两处。按报告改为 `SnapshotDiff['changes']`（同文件 19 行已定义该接口），并留一行说明防回退 |
| #321 | fix(仅文档) | 成立：clone-async 的 Map 分支对键走 `cloneDeep` 同步递归，模块头的「每个节点的工作量有界」在键这一侧不成立（大对象为键、或以 Map 为键的链式 Map 会把阻塞搬回当前批）。改为键入队会让 entry 等键值双就绪、Map 插入序失真（迭代序是 Map 语义的一部分），属结构性改动不是 low 顺手项 → 按报告的第一诉求「至少写明最坏情况」落地：模块头补一处例外声明，Map 分支写明代价、规避（把大对象放在值的位置）与 customCloner 出口 |
| #322 | fix | 成立：原语分支的 defineProperty 直接读 `descriptor.enumerable/configurable`，绕开了上一行刚算好的 descriptorFlags（对象分支与同步路径都用它），正是 normalizeDescriptorFlags 要消除的两套方言。改为统一取 descriptorFlags，并把只有单一别名的 `targetWritable` 一并去掉（三标志一律 spread descriptorFlags，与同步路径 clone.ts:473 写法对齐）。运行期等价（defineProperty 内部本就 ToBoolean，合法描述符的两个字面量恒为布尔），无行为变更；回归用例 #322 锁住不可写/不可配置/不可枚举源属性在两路径的描述符还原一致 |
| #323 | reject(保留现状 + 文档) | 现象成立但修法方向不对：只按报告的 `else if (hasOwnProperty)` 改 clone-async，会让异步保洞而同步 `cloned[i] = undefined` 仍把洞落成真实键——恰好拆散两路径一直在对齐的数组语义（clone.ts 的注释就是这条对齐）。两条路径一起改则是行为变更（自有键集、nodeCount/estimatedSize 计数与 diff 归因都跟着动，元素描述符还原还得给 index 目标补 descriptor），不属 low 幅度。保留现状：在 clone-async 数组分支写明洞落成 undefined、元素描述符不还原、与同步同口径，以及三条相关代价（含实测到的一个报告未提的相邻事实：`[ok, bad]` 里尾部节点被 SKIP 时克隆长度随之下落到 1，见首轮 #288 用例跑测输出），clone.ts 数组分支补一句指回；列入待办供后续单独评估 |

## 遗留与待办（本组）

1. #309 残余：防抖选择器被同步重入时，重入调用仍复用上一轮的 currentPromise（拿到旧值）。彻底修法要在回调开头认领并清空 promise/resolve/reject 槽位，代价是无人 await 的重入返回值会把 rejection 变成 unhandled rejection —— 需要单独评估（现仅恢复定时器可清除性）。
2. #323：数组「洞落成真实 undefined 键 + 元素描述符不还原 + 尾部洞缩短克隆长度」三条已在两条路径的注释里写明口径；要真保洞需 sync/async 一起改成 hasOwnProperty + 显式 length + 元素 descriptor，另开一条改动评估。
3. #292：对象参数侧缓存若确需上限/清扫，得先决定用什么承载键列表（WeakMap 无 size/无迭代），属新特性不属 low。
4. #297：若日后要收紧为完整映射或返回 Partial，是破坏性类型变更，需在 CHANGELOG 与 docs 同步（本轮只补文档）。
5. 本组只跑了 `tests/unit/extras`（750 通过）；`tests/unit/regression`、`tests/unit/store` 等消费 extras 选择器的套件与 `pnpm test:ci` 的覆盖率阈值由 Wave D 统一验证。
6. 已知噪音：`src/extras/snapshot/SnapshotManager.ts` 的 processQueue 填充段（`;(...)` 前缀 4 行）在 HEAD 就不满足 prettier，非本次改动引入，未顺手改；Wave D 跑 `pnpm format` 会一并归位。
