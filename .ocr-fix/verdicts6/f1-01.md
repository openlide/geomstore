# f1-01（wave f1）
条数：7｜FIXED 7 / FP 0 / REJECT 0 / NEEDS-MAIN 1（R6-066 可改两条已落地，`.gitattributes` + `format:check` 脚本待主会话，故同时挂 NEEDS-MAIN）
涉及文件：.github/workflows/ci.yml、.gitignore、.prettierrc.json、eslint.config.js、examples/advanced/plugins.ts、examples/basic/01-simple-store.ts、examples/cache/02-selective-cache.ts、tests/unit/r6-f1-01-cache-observability.test.ts（新增回归锁）

## R6-010 medium test .github/workflows/ci.yml:L109-L153
verdict: FIXED
改动: ① verify 的 `Build` 步改成 `pnpm run build:release`（step 名 `Build release artifact (tsc + minify --strict)`），于是下方那段冒烟 import 的就是被发布的那份压缩产物（另在冒烟块里加一行注释说明此刻 dist 已被 --strict 覆写）；② 新增 `Pack dry-run (exercise prepack checks)` 步：`pnpm pack --dry-run` + `pnpm run stubs:clean`，把 generate-subpath-stubs 的四条前置校验前移到 PR 阶段，刻意不加 `--ignore-scripts`（加了 prepack 就不跑，等于零校验）。无新增外网步骤：`pnpm pack` / `minify-dist` 都是本地脚本，terser 是 devDependency，第三方 Action 未动。
判据: 本机 dist 现状就是压缩产物（`node -e` 实测 dist/core/store/Store.js = 13037 字节 / 1 行 / 0 个 `/*` 注释块；dist/index.js = 29 字节 1 行），把 ci.yml 里那段冒烟脚本原样抽出（`node -e` 按 `node --input-type=module -e '` 定位切片）落到 `.cache/r6-f1-01-smoke.mjs` 后执行 → 输出 `ESM SMOKE OK (exports map exercised by node resolver)`、exit=0，证明冒烟在压缩产物上成立（临时文件已删）。`pnpm pack --dry-run` 本机实跑 exit=0、打出 14 个 stub 目录 + dist/** 清单 → prepack 四条校验在本地能跑通；同时实测 `--dry-run` **不跑 postpack**（11 个 stub 目录留在工作树），故该步显式补 `pnpm run stubs:clean`（实跑输出 `[stubs] removed 14 subpath stub dirs`，`git status --untracked-files=all` 复查已无残留）。
回归锁: 无新增用例（CI yml 不是本仓测试可触达的对象；判据即上面的实跑命令）
文档: none（CI 编排，不改对外行为）；建议 CHANGELOG 不提，但「发布链门禁前移」可作为工程条目

## R6-011 medium bug .gitignore:L67-L73
verdict: FIXED
改动: 把逐轮点名的白名单改成模式三行——`!.ocr-fix/decisions*.md` / `!.ocr-fix/verdicts*/` / `!.ocr-fix/verdicts*/**`，删掉 decisions.md、decisions5.md、verdicts/、verdicts5/ 四条枚举（模式已覆盖），并写明「按轮次枚举 = 每轮漏一次、且 git add 只安静跳过」的理由。第六轮台账 decisions6.md / verdicts6/** 从此自动放开。
判据: 反向实测（本仓）`git check-ignore -q --no-index .ocr-fix/decisions6.md` → exit 1、`.ocr-fix/verdicts6/f1-01.md` → exit 1（=不被忽略），而中间产物仍命中：`.ocr-fix/ledger6.json` → exit 0 且 `-v` 显示 `.gitignore:71:.ocr-fix/*`；`git status --short --untracked-files=all .ocr-fix` 现在列出 `?? .ocr-fix/verdicts6/*.md`（改前这些文件不出现，`git add .ocr-fix` 会静默跳过）。旧规则的行为用 /tmp 探针仓复现（同两条路径 → `.gitignore:1:.ocr-fix/*`，exit 0）。
回归锁: 无（.gitignore 不参与任何测试；判据即上面的命令级证据）
文档: none（仓库内部忽略策略，无对外行为）

## R6-066 low maintainability .prettierrc.json:L16
verdict: FIXED（本分片可改的两条已落地）+ NEEDS-MAIN（.gitattributes）
改动: ① `.prettierrc.json` 的 `endOfLine` 由 `auto` 改 `lf`——`auto` 让 prettier 顺着文件现有换行判绿，等于对 CRLF/LF 差异视而不见；② `ci.yml` 的 verify-static job 在 Lint 之后新增 `Format check` 步：`pnpm exec prettier --check "src/**/*.ts" "tests/**/*.ts"`，glob 与 package.json 的 `format` 脚本逐字相同（→ `pnpm format` 就是这一步的修复器，不另开口径），无基线冻结。
判据: `git config --get core.autocrlf` = true、`git ls-files --eol` → `i/crlf` 计数 0 / 共 400 个文件 → index 侧全 LF，Linux runner 检出即 LF，门禁可判绿；实测把 src+tests 共 283 个 .ts 复制成 LF 版本后用本仓 config 跑 `prettier --check` → 报红的 6 个文件全是**其他分片在途改动**（StoreRegistry.ts、src/core/performance/__r6old_probe.test.ts、extras/action/decorators/cache.ts、extras/error/ErrorAggregator.ts、tests/unit/r6-f1-03-benchmark-results.test.ts、r6-f1-08-snapshot.test.ts），且同一批文件在 `--end-of-line auto` 下报同样 6 个 → 与 endOfLine 取值无关，本条改动不引入新红。本机 `prettier --check .github/workflows/ci.yml` 的 warn 也只是 CRLF：`diff <(sed 's/\r$//' ci.yml) <(prettier ci.yml)` 空输出，`--end-of-line auto` → 「All matched files use Prettier code style!」。
NEEDS-MAIN: 请主会话新建 `.gitattributes`（内容：`* text=auto eol=lf`，再按 `pnpm-lock.yaml -text`、图片/字体等二进制资源 `-text diff` 豁免）。它不在本分片可改文件里。缺它时 endOfLine=lf 的唯一后果是 Windows 工作树（autocrlf=true，实测 231 个 w/crlf）本地 `prettier --check` 假红 148 个文件、`pnpm format` 会把工作树改写成 LF（git 侧无 diff），CI 不受影响。另需配套：package.json 加 `"format:check": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\""`，让 CI 那步能写成 `pnpm run format:check`（package.json 不在本分片可改清单，故 CI 里先用 `pnpm exec prettier` 原样命令）。
回归锁: 无新增用例（配置/CI 门禁，判据为上面命令级实测）
文档: CONTRIBUTING/docs 若提到 `pnpm format`，需要一句「仓库规范换行符为 LF，新增 .gitattributes 后 Windows 检出亦为 LF」——纯修复，非行为变更（对外 API 无影响）

## R6-067 low maintainability eslint.config.js:L84-L90
verdict: FIXED
改动: 全局 `ignores` 补 `dist-weapp/**`、`miniprogram_npm/**`、`packages/**`（报告点名的三个），并顺手补 `.ocr-fix/**`、`.cache/**`（同类裸命令红灯来源，实测占了原噪声的 60 条），理由与「与 .prettierignore/.gitignore 用同一组产物名」写进注释。
判据: 改前 `npx eslint --print-config dist-weapp/index.js` 输出完整 config 对象（=未被忽略，会被 `npx eslint .` 当源码），`npx eslint --print-config scripts/minify-dist.mjs` 输出 `undefined`（=已忽略），两侧对照钉住机制；`npx eslint dist-weapp/index.js` → `'module'/'require' is not defined` 3 条 no-undef error（dist-weapp 实测 105 个 .js，packages 下 20 个 .ts），即报告说的「海量 error」；`find dist-weapp -name '*.js' | wc -l` = 105。改后同一批路径 `--print-config` 全部翻成 `undefined`，裸命令 `npx eslint .` 由 **70 problems（61 errors）** 降到 **9 problems（1 error，且该 error 在 tests/unit/r6-f1-03-benchmark-results.test.ts:96 `Parsing error: Invalid character`——另一分片在途文件）**，其余 8 条为 examples/global.d.ts 的 no-explicit-any 警告与 Store.ts 在途改动，均非本分片文件。
文档: none（仓库内部 lint 范围）
NEEDS-MAIN: `.prettierignore` 只有 `miniprogram_npm`，缺 `dist-weapp` / `packages`（该文件不在本分片可改清单）；默认调用下 prettier 还会读 .gitignore 兜住 dist-weapp，但显式 `--ignore-path .prettierignore` 模式下会漏（同第五轮 R5-369 的判例）。

## R6-012 medium documentation examples/advanced/plugins.ts:L38-L39
verdict: FIXED
改动: 注释按 0.6.1 真实契约改写——loggerPlugin 打印 beforeDispatch 的 action 名与实参数组、afterDispatch 的 action 名与返回值、before/afterSetState 的键值、外加一条只读订阅打印整份状态；删掉「耗时」并点名要耗时该用 `extras/performance` 的 analyzerPlugin；补上「生产环境整体静默」这一条（`isProduction()` 早退）。
判据: `sed -n '30,90p' src/plugins/builtin.ts` 实测 loggerPlugin 全文只有 4 个 `hooks.on(...)` + 1 条 `store.subscribe(..., { readOnly: true })`，无 `Date.now()`、无 duration 字段、无异常分支计时；输出面确实比「每次 action」更宽（setState 与状态变更也打印）。
回归锁: 无新增用例（纯注释）。既有锁：`tests/unit/plugins/builtin.test.ts:717-727` 已在断言 loggerPlugin 只读订阅与 `hasWritableListeners()` 的关系。
文档: docs/SKILL 里若有「日志插件打印耗时」的同款措辞，需要照此改（纯文案修复，非行为变更）——交主会话统一收尾
NEEDS-MAIN: 同一句错话还在 **src/plugins/builtin.ts 的 loggerPlugin JSDoc**（本轮实读为「日志插件：将 Action 调用（名称、参数、耗时、异常）输出到控制台」——既不计时，也没有异常分支），该文件不在本分片可改清单，请主会话或 src 分片把「耗时、异常」删成「名称、参数、返回值 + setState/状态变更」。

## R6-068 low documentation examples/basic/01-simple-store.ts:L47-L50
verdict: FIXED
改动: 注释改成真实门槛——零拷贝要求「一个可写订阅者都没有」（`needsClone = (显式 notify.clone) || hasWritableListeners()`），并就地讲清本示例的两段写入：40 那次因第一个订阅是可写的仍深拷贝，`unsubscribe()` 之后 50 那次只剩只读订阅者才命中零拷贝（示例其实已经走到过快路径，只是原文案没讲对）。
判据: `grep -n "needsClone" src/core/store/Store.ts` → 1210 行 `const needsClone = (this._notifyCloneExplicit && this._notifyClone) || this._subscriptionManager.hasWritableListeners()`；204-205 行 `_notifyClone = options.notify?.clone ?? true` / `_notifyCloneExplicit = options.notify?.clone !== undefined`（未显式配置时该分支恒 false，判据只剩可写订阅者数）；`src/core/store/SubscriptionManager.ts` 的 `hasWritableListeners()` 即 `return this._writableCount > 0`，其 doc 亦写「仅当存在可写监听器时才需要深拷贝…纯只读订阅（零拷贝）」。
回归锁: 无新增用例（纯注释）。既有锁：`tests/unit/r5-core-store-p1-A-notify-and-eviction.test.ts:113`「仅只读订阅时保持零拷贝」、`tests/unit/r5-core-misc-p1-regressions.test.ts:165`「可写注册清零后回到零拷贝路径」、`tests/unit/store/modules/SubscriptionManager.test.ts:365`。
文档: none

## R6-013 medium documentation examples/cache/02-selective-cache.ts:L20-L25
verdict: FIXED
改动: 两条读取由 `getState().xxx` 改为 `getCached('visibleRows')` / `getCached('rawResponse')`，并补第三步 `invalidateCache('visibleRows')` + 重读，让「热键命中 / 冷键回源」真的能被 `getCacheStats()` 观测到；文件头与行内注释写明「只有 getCached 走缓存，getState 既不查缓存也不记统计」与「冷键被 cacheKeys 过滤分支提前返回、不记 hit/miss」两条真实语义；导出 `dashboardStore` 供回归锁断言。
判据: 实测输出 `缓存统计: { enabled: true, size: 1, keys: [ 'visibleRows' ], hits: 1, misses: 1, evictions: 0 }`（改前按报告的 getState 读法是 hits 0 / misses 0、但 keys/size 因 enableCache 预填非零 → 结论自相矛盾）。`src/core/store/Store.ts` 的 `getState()` 直接 `return this._state`，`getCached()` 才走 `_cacheManager.get(...)`；`StoreCache.get()` 里 `if (this._cacheKeys && !this._cacheKeys.has(key)) return getState()` 在记账之前提前返回，故报告「冷键那侧才会真正记一次 miss」这句只对了方向、结论按实测修正为「冷键不记账」。
回归锁: `tests/unit/r6-f1-01-cache-observability.test.ts` > 4 用例：「enableCache 预填后 getState() 连读两次不产生任何 hit/miss」「getCached 读缓存键记 hit，读非缓存键既不 hit 也不 miss 且每次回源」「缓存键失效后重读 → 记一次 miss 并回填」「示例 02-selective-cache 跑完自己就能观测到 hit 与 miss」。最后一条对旧版示例实测先红（`TypeError: Cannot read properties of undefined (reading 'getCacheStats')`，1 failed）后绿（4 passed）。
文档: none（示例内部；docs 若抄过这段读法需同步，交主会话）

---

## 自验（收工前实测）

```
npx jest tests/unit/r6-f1-01-cache-observability.test.ts --ci
  → PASS 1 suite / Tests: 4 passed, 4 total
npx eslint --max-warnings 0 eslint.config.js examples/{advanced/plugins,basic/01-simple-store,cache/02-selective-cache}.ts tests/unit/r6-f1-01-cache-observability.test.ts
  → exit 0（零 warning，零 eslint-disable）
npx tsc --noEmit -p tsconfig.tests.json   → r6-f1-01 相关文件 0 报错
npx tsc --noEmit -p tools/tsconfig.examples.json → examples/ 侧 0 报错（仅其他分片在途的 src/ 报错）
for f in 改动的 7 个文件; do sed "s/\r$//" $f | npx prettier --check --stdin-filepath $f; done
  → 全部 clean（LF 内容口径）；本机 --check 直跑仍 warn，唯一原因是工作树 CRLF（见 R6-066）
```

补充：`eslint.config.js` 顺带跑了一次 `npx prettier --write`（HEAD 既有的数组换行/注释空格漂移，纯格式化，
`git diff` 核实无规则语义变化；改后 `npx eslint src/index.ts` 与 `--print-config src/index.ts` 均正常）。
临时产物 `.cache/r6-f1-01-lfcheck/`、`.cache/r6-f1-01-smoke.mjs` 与 `pnpm pack --dry-run` 留下的 11 个 stub 目录
（`pnpm run stubs:clean` → `removed 14 subpath stub dirs`）都已清掉，`git status --untracked-files=all` 复查无残留。
