# f1-04（wave f1）
条数：10｜FIXED 10 / FP 0 / REJECT 0 / NEEDS-MAIN 0（另有 2 条跨文件跟进口子见文末）

涉及文件（均已改）：`scripts/clean-dist.mjs`、`scripts/generate-subpath-stubs.mjs`、
`scripts/postbuild-dist.mjs`、`scripts/verify-weapp-bundle.mjs`、
`src/core/cache/LRUCache.ts`、`src/core/compose/StoreRegistry.ts`、
`src/core/errors/GeomStoreError.ts`。
新增测试：`tests/unit/r6-f1-04-lru-foreach.test.ts`、`r6-f1-04-error-shape.test.ts`、
`r6-f1-04-registry.test.ts`、`r6-f1-04-build-scripts.test.ts`（共 37 条用例）。

## R6-031 medium scripts/clean-dist.mjs:L66-L77
verdict: FIXED
改动: `classifyEntry` 的等价判定改为**两侧同源**——先 `realpathSync(path.dirname(full))` 解析父目录，再 `path.join(realParent, basename)` 与 `realpathSync(full)` 比较；父目录也解析不动时仍按保守侧返回 'link'。修复了「仓库根经由符号链接到达（POSIX /tmp、/var/folders、bind mount、`--preserve-symloops` 类调用形态；Windows 8.3 短名）时 dist 内每个真实目录都被判成 link，countFiles 少算、removeDirTree 走 unlink/rmdir 抛错、只剩一条 WARN、prebuild 事实上什么都没清」这条空转路径，与同文件 147-162 行顶层守卫的写法对齐。
回归锁: `tests/unit/r6-f1-04-build-scripts.test.ts` > `R6-031 clean-dist`（两条：符号链接化项目根下 dist 整树真被删除、日志按真实文件数计 `removed dist (4 files)`；dist 内部 junction 仍按 link 处理、目标内容不动）。
判据/局限（写给主会话）: 本机 win32 无法构造「先失败后通过」的触发面——实测 Node 会对 ESM 入口做 realpath：`node "$TEMP/r6probe/link/scripts/p.mjs"` 打印的是 `file:///C:/.../r6probe/**real**/scripts/p.mjs`（链接路径被解析掉），故 `import.meta.url` 推出的 projectRoot 已是真路径，正例只在 `--preserve-symlinks-main` 下成立（该取证命令被权限系统拦下）。因此这两条用例按「修复后不得回归」的锁法落地，判定本身按报告方向（两侧同源是严格更正确的写法，判不准时保守侧不变）。
文档: none（脚本内部判据，不对外）

## R6-032 medium scripts/generate-subpath-stubs.mjs:L226-L248
verdict: FIXED
改动: ①`checkPreconditions` 的 miniprogram 分支新增「字段值必须就是被 `verify:weapp` 验过的产物目录」这条断言（`WEAPP_ARTIFACT_DIR = 'dist-weapp'`，不等即退出码 1，并写清「改字段或把 build/verify 的产物目录一并改成同一个值」两条出路）；②字段整体缺失也拦（无该字段时微信退回「从 main 起做依赖分析并拼接」＝0.6.0 事故路径）；③`scripts/verify-weapp-bundle.mjs` 增加第 1 项断言「miniprogram 字段与被验产物目录同源」，两侧互为闭环：改字段 → prepack + verify 同时红；改 verify 的 OUT_DIR → verify 红。未按报告之外的方向改为「校验目录内容是不是 CJS」。
调用顺序核对（无需改 package.json）: `prepublishOnly = pnpm test && build:release && build:weapp && verify:weapp`，`prepack = generate-subpath-stubs`、`postpack = --clean`；`npm publish` 时 prepublishOnly 先跑、prepack 后跑，两道断言都在同一次发布里生效；`pnpm pack` / `npm pack --ignore-scripts` 只跑 prepack（正是字段判据必须落在 prepack 的原因）→ 单侧即覆盖，package.json 不需要配合改动。
回归锁: `tests/unit/r6-f1-04-build-scripts.test.ts` > `R6-032 prepack`（3 条：字段写成 dist 被拦、字段缺失被拦、字段=dist-weapp 时不再报同源问题）。反向证据：临时把两条判据改成常量假值后实跑 `npx jest tests/unit/r6-f1-04-build-scripts.test.ts` → `Tests: 4 failed`，stderr 里不再有 `miniprogram 字段指向 dist/` / `没有 miniprogram 字段`，随后已恢复。
跟进（见文末 NEEDS-MAIN 段）: `'dist-weapp'` 字面量现存于 build-weapp / verify-weapp-bundle / generate-subpath-stubs 三处。
文档: 需要主会话在 docs/发布链相关文档里补一句：`miniprogram` 字段与微信产物门禁是**绑定关系**，改字段必须同步改 build/verify 的产物目录，否则 prepack 直接失败（行为变更：以前可全绿出包）。

## R6-077 low scripts/postbuild-dist.mjs:L35-L73
verdict: FIXED
改动: ①把 minify-dist 的「dist 必须是仓库内真实目录」断言前置到本脚本（它是构建链里第一个原地写 dist 的步骤）：新增 `rejectUntrustedDist()`（realpath 全等 + 必须是目录，理由与文案同 clean-dist），并在其上先做 `lstat` 版存在性判定（断链的 dist 不再被当成「不存在」而跳过校验）；②type 标记这一段改走同一套判据：`lstatOrNull(markerPath)` + `classifyEntry(markerPath) !== 'file'` → 退出码 1，不再用 existsSync/readFileSync/writeFileSync 跟随重解析点；③顺带修掉本文件 `classifyEntry` 里与 R6-031 同源的 `path.resolve` 参照系缺陷（否则符号链接化仓库根下 map 扫描会把每个目录判成 link、汇总行装作清理干净）。
回归锁: `tests/unit/r6-f1-04-build-scripts.test.ts` > `R6-077 postbuild-dist`（3 条：`dist/package.json` 是 junction 时退出码 1 且目标目录一字未改；dist 整体是 junction 时不在目标里建 package.json、不删 map；正常真实 dist 仍合并 type 标记并只剔 `.js.map`/`.d.ts.map`）。反向证据：把两处判据临时改为 `null` / `false &&` 后实跑该文件 → `Tests: 4 failed`，marker 用例退化成「解析失败（EISDIR…）」这条误导性文案、dist-junction 用例退出码变 0 并在链接目标里写出 package.json、删掉 map；随后已恢复。
文档: none（脚本行为，若 docs 有「postbuild 失败分级」一节则由主会话补「dist 或 dist/package.json 不可信 → 退出码 1」一条；纯修复，非对外 API 变更）

## R6-078 low scripts/verify-weapp-bundle.mjs:L69-L89
verdict: FIXED
改动: ①`mkdtempSync` 之后的全部主体包进 `try { … } finally { fs.rmSync(loadRoot, {recursive:true, force:true}) }`，汇总输出移到 try 之外（`bail()` 的两处调用都在 mkdtemp 之前，不会绕过收尾）；②进入加载区之前先按 outAbs 同口径检查 `dist/`，缺失即 `bail('找不到 dist/（本门禁要拿 dist 的 ESM 产物作对照），先跑 pnpm build …')`，不再无条件 `listJs(dist)` 抛裸 ENOENT 栈；③`listJs` 提到模块作用域（try 内外都要用），文件头补上「缺产物一律 bail」和「临时加载区生命周期」两段口径。
回归锁: `tests/unit/r6-f1-04-build-scripts.test.ts` > `R6-078 verify-weapp`（2 条：只跑 build:weapp（无 dist）时退出码 1 + stderr 含可读文案 + 不含 ENOENT + TMPDIR 里不留 `geomstore-weapp-verify-*`；产物目录里挂悬空 junction 使加载区中途抛错时同样不留残骸）。反向证据：临时删掉 dist 检查、并把 `finally` 换成 `catch { console.error; process.exit(1) }` 后实跑 → `Tests: 2 failed`，一条得到裸 ENOENT 栈、一条 `Received: ["geomstore-weapp-verify-17Blkg"]`（泄漏目录）；随后已恢复。
文档: 用法说明需要改（主会话）：`verify:weapp` 现在要求 `dist/` 存在，文档里「需先 pnpm run build:weapp」应写成「需先 `pnpm build` 与 `pnpm run build:weapp`」；纯修复。

## R6-033 medium src/core/cache/LRUCache.ts:L520-L530
verdict: FIXED
改动: `forEach` 改为与 `clear()` 同口径的**键快照**遍历——`for (const key of this.keys())` 先取快照，再按键从 Map 取「回调时刻的当前值」，节点已不在册（回调内被删）则跳过该键。新口径（写进 JSDoc）：①回调内删除任意键（含非当前键）→ 该键不再参与回调、其余条目不丢；②回调内 `get`/`getOrSet` 命中引起 moveToHead 重排 → 快照键各访问一次；③遍历期间新写入的键本次不访问；④值不取快照（改过的条目以新值参与回调）。没有采纳「只把预取后继改成回读 node.next」那种只治第二类的写法。
回归锁: `tests/unit/r6-f1-04-lru-foreach.test.ts`（7 条，含报告复现的两类：删紧邻下一个键期望 `d,b,a` 且已删键不再收到回调；回调内 get 使 a 移到链首时期望 4 键各一次；另有删当前键保持既有 #25 口径、回调内 clear、遍历内新增键、值取当前值、顺序与 keys() 一致）。反向证据：把 forEach 换回旧实现后实跑该文件 → `Tests: 4 failed, 3 passed`，第一条实得 `[['d',4],['c',3]]`（正是报告说的「多访问已删条目＋静默截断后续」）。
文档: 需要主会话改 docs/API.md 的 LRUCache 表格行——现为「`forEach(fn)` | 遍历时删除当前项安全（先取后继再回调）」，应改为「进入时取键快照：回调内删除/重排都不会漏访问也不会回调已删条目；遍历期间新写入的键本次不访问；值取回调时刻的当前值」。**行为变更**（对外语义收紧），CHANGELOG 按行为变更记。

## R6-080 low src/core/compose/StoreRegistry.ts:L94-L99
verdict: FIXED
改动: 引入 `isProduction()`（`../store/utils.js`，与同目录 composeStore/helpers 同一入口）。①幂等重注册那条由无条件 `console.log` 降级为 `if (!isProduction()) console.debug(...)`；②L103「already registered … overwriting」与 L116「destroy() 期间被重新注册」两条配置歧义级告警同样收进 `if (!isProduction())`；③`_destroyInstance` / createSnapshot / restoreSnapshot 的 `console.error` 保持无条件输出。行为（销毁、覆盖、注册结果）不变，只动日志。
回归锁: `tests/unit/r6-f1-04-registry.test.ts` > `R6-080`（4 条：开发下幂等重注册走 debug 且不再占 console.log、覆盖注册留 warn 且照常销毁旧实例；生产下 debug/log/warn 全静默而注册结果不变、destroy 抛错仍 console.error）。反向证据：`dist/core/compose/StoreRegistry.js`（修复前产物）实跑 `NODE_ENV=production` + 同实例重复注册 → 控制台仍打出 `[StoreRegistry] Store "x" is already registered with the same instance, ignoring`。
文档: 主会话在「日志/静默」相关文档与 CHANGELOG 记一句：StoreRegistry 的幂等重注册与覆盖注册提示改为**仅非生产输出**（幂等那条同时由 log 降为 debug）；纯修复（无 API/行为变更），但按日志面变更需在静默清单里点名。

## R6-081 low src/core/compose/StoreRegistry.ts:L544-L560
verdict: FIXED
改动: `globalRegistry` 改为「globalThis 槽位 + 首次取用惰性构造」：`Symbol.for('@openlide/geomstore:store-registry')`（与 STATE_VERSION / GEOMSTORE_BRAND 同一取舍，键名带包名命名空间、不带版本号）；复用判据刻意**不用 instanceof**（跨副本恒 false，正是本条故障），改按方法形状 `isStoreRegistryLike`（register/unregister/get/getAll/clear 五项全查）；写入用 `defineProperty`（不可枚举，宿主枚举 globalThis 带不走整册 store），写失败（globalThis 被冻结/键不可写）时退回本副本私有实例并在非生产下 warn 一次，不抛错。JSDoc 把作用域改写为「进程内唯一，跨包副本共享；globalThis 不可写时退化为本副本一份」。
回归锁: `tests/unit/r6-f1-04-registry.test.ts` > `R6-081`（4 条：两份模块副本的 `globalRegistry` 同一实例且 A 注册 B 可读、setDefault 同步；槽位存在且不可枚举；形状不符的占用值不复用不抛错；槽位不可写时降级为私有实例并出声）。反向证据：`dist/core/compose/StoreRegistry.js` 与一份拷贝（模拟第二副本）实跑 → `[old] 两副本 globalRegistry 同一: false`、`B 副本能否看到 A 注册的 store: false`。
文档: 需要主会话改 docs/API.md:220 的 `export const globalRegistry: StoreRegistry` 说明——作用域为**进程内**（globalThis 品牌槽位，跨包副本共享），不再是「当前模块副本」；SKILL 文档若称其为模块级单例需同步。**行为变更**（此前多副本各持一册）。

## R6-036 medium src/core/errors/GeomStoreError.ts:L533-L595
verdict: FIXED
改动: 采报告给的「品牌键」方案，与 `isGeomStore` 同一取舍：新增模块内 `GEOMSTORE_ERROR_BRAND = Symbol.for('@openlide/geomstore:error-brand')`，构造器以**非可枚举自有属性**挂载（`enumerable:false`，故 `Object.keys`/`JSON.stringify`/快照与深比较的枚举面不变，ERROR-008 锁定的 toJSON 形状不变）；`isGeomStoreError` = `instanceof ∥ (Error 实例 + 品牌键 + typeof code === 'string')`；六个派生守卫统一走 `matchesDerivedErrorShape(error, name)`（先过基类判定，再比构造器显式赋值的 name 字面量——压缩后 constructor.name 不可信，name 才是设计上的判别字段）。文件头与守卫段写明取舍、可伪造性（不是安全边界）与新判定口径。
回归锁: `tests/unit/r6-f1-04-error-shape.test.ts` > `R6-036`（5 条：先自检两次 `jest.resetModules()` 加载的确实是不同类对象且跨副本 instanceof 为 false；副本 A 抛的 ActionError 在副本 B 下 isGeomStoreError/isActionError 均为 true、isStateError false；本副本七个守卫按 name 分流不误判；缺品牌键/品牌键不在 Error 上/code 非字符串的伪冒值一律放行失败；品牌键不进 Object.keys 且 toJSON 输出形状不变）。反向证据：dist 两份拷贝实跑 → `[old] cross-copy isGeomStoreError=false isActionError=false（类不同一: true）`。
文档: 需要主会话在 README/docs 的错误处理段补一句：类型守卫对**同一包多副本**同样成立（跨分包/宿主内嵌可直接 `if (isGeomStoreError(e))`）；派生守卫按 `error.name` 判别。**行为变更**（此前跨副本静默漏判）。

## R6-082 low src/core/errors/GeomStoreError.ts:L29-L35
verdict: FIXED
改动: `toSerializableValue` 的 toJSON 分支不再无条件早退：①`toJSON` 的探测取值并入 try（取值即抛的访问器 → `[Unreadable]`，与子键读取同一口径）；②序列化器给出**原始值**（Date/Number 等）时仍原样交回 value，保持 R5-079 已裁定的「带 toJSON 的对象按其自身序列化器处理」（`toJSON()` 返回值里 Date 仍是 Date，序列化后仍是 ISO 串）；③给出**对象/数组**时把该结果续走同一套带深度与祖先链的归一：value 自身进 ancestors（拦住「toJSON 返回自己」），超 `MAX_CONTEXT_DEPTH` 以 `[Truncated]` 收尾——于是「每次 toJSON() 构造新身份」的图节点不再无限递归到 RangeError，序列化器内部的 BigInt/环路也一并收敛。toJSON() 的 remarks（原文「该序列化器抛错不在本方法的兜底范围内」）已同步改写。
回归锁: `tests/unit/r6-f1-04-error-shape.test.ts` > `R6-082`（4 条：自造新身份的图节点沿 parent 走到底必须是 `[Truncated]` 且层数 ≤ 6；抛错访问器 → `[Unreadable]` 且 `toJSON()` 不抛；Date 仍是 Date 且序列化为 ISO；序列化器给出对象时内层 BigInt → `'1234n'`、自引用 → `[Circular]`）。反向证据：dist（修复前产物）实跑 → `[old] deep-toJSON 抛出: RangeError / Maximum call stack size exceeded`、`[old] toJSON 取值即抛: accessor boom`。
文档: 需要主会话在 docs 的错误/日志章节加一句：context 里带 `toJSON` 的值受 `MAX_CONTEXT_DEPTH` 约束、其序列化器抛错不会再把打印错误变成第二次故障（纯修复，附带把「序列化器抛错不兜底」这条既有说明改掉）。

## R6-083 low src/core/errors/GeomStoreError.ts:L79-L84
verdict: FIXED
改动: `toSerializableCause` 先走「带 toJSON 的抛出值」这条路：取 `value.toJSON()` 的结果再过 `toSerializableValue`（GeomStoreError 系因此保留 `code`/`context`/内层 `cause` 链，两层以上包装看得到根因）；序列化器抛错时退回原生 Error 的 `{name, message}`，不向上抛；原生 Error 维持既有 name/message 形状；其余值走与 context 同一套归一。函数注释与 toJSON() 的 remarks 同步说明。
回归锁: `tests/unit/r6-f1-04-error-shape.test.ts` > `R6-083`（3 条：本库错误作 cause 时 `toJSON().cause` 含 name/code/context 且 `cause.cause.code` 可见、整串 JSON 里能搜到根因错误码；TypeError 仍是 `{name,message}`（与 R5-078 既有用例一致，实跑 450 条全绿）；序列化器抛错时退回 name/message）。反向证据：dist 实跑 → `[old] cause 形状: {"name":"PluginError","message":"pf"}`（code/context/内层链全丢）。
文档: 需要主会话同步：`toJSON().cause` 的形状变更——被包装的是本库错误时从 `{name,message}` 变为完整的 `{name,message,code,context,stack,cause}`。**行为变更**（按 cause 形状做断言/解析日志的消费方需注意）。

---

## 跨文件跟进（不阻塞本分片，交主会话）

1. `scripts/minify-dist.mjs:138-148` 的 `classifyEntry` 与修复前的 R6-031 **同一处参照系缺陷**（`samePath(real, path.resolve(full))`）。该文件不在本分片清单（本轮由 f1-07 在改 R6-074/R6-076），请把「父目录 realpath 后拼条目名」的同一条改动移植过去，否则 `--strict` 侧的扫描/统计在符号链接化仓库根下同样空转。
2. 微信产物目录字面量现存三处：`scripts/build-weapp.mjs:52`、`scripts/verify-weapp-bundle.mjs:46`、`scripts/generate-subpath-stubs.mjs:61`（本分片新增，注释里已点名另外两处）。`build-weapp.mjs` 与 `weapp-entries.mjs` 不在本分片清单，建议由主会话把它收敛为 `weapp-entries.mjs` 的导出常量（如 `export const WEAPP_OUT_DIR = 'dist-weapp'`），三处 import 同一真值；当前实现已用「字段 ↔ 被验目录」双向断言兜住误改，不会因此留白。
3. **分片归属冲突（对账用）**：f1-07 的收尾报告称已在 `scripts/clean-dist.mjs`、`scripts/postbuild-dist.mjs`、`scripts/generate-subpath-stubs.mjs` 落地 R6-075（`isSubpathTarget`：sourcemap 的 `sources`/`file` 不再按 `.split('/').pop()` 反推子路径，改按 realpath 前缀判归属）与 R6-076（落盘前 `unmapOutboundSymlink`），但这三个文件的当前内容里找不到对应实现：`grep -in "subpath|symlink" scripts/clean-dist.mjs` 只命中本分片 R6-031 的注释行、`grep -in "symlink|subpath" scripts/postbuild-dist.mjs` 只命中「子路径转发 stub 不在此生成」一行、`grep -n "isSubpathTarget|realpathSync|symlink" scripts/generate-subpath-stubs.mjs` 零命中。这三份文件按本轮分片清单归 f1-04（本分片只做了 R6-031/R6-032/R6-077 的定点改动与 prettier 格式化，未整文件重写 clean-dist/postbuild/stubs），请主会话确认 R6-075/R6-076 是「未落地」还是「被覆盖需重做」，别让这两条静默消失。

## 自验结果（收工前实跑）

```
npx jest tests/unit/r6-f1-04-lru-foreach.test.ts tests/unit/r6-f1-04-error-shape.test.ts \
         tests/unit/r6-f1-04-registry.test.ts tests/unit/r6-f1-04-build-scripts.test.ts --ci
  → Test Suites: 4 passed, 4 total；Tests: 37 passed, 37 total
npx jest tests/unit/core tests/unit/cache tests/unit/extras/error tests/unit/store \
         tests/unit/r5-core-misc-p1-regressions.test.ts --ci
  → Test Suites: 1 failed, 43 passed, 44 total；Tests: 1 failed, 1524 passed
    唯一失败为 tests/unit/store/action-dirty-keys.test.ts「accumulates dirty keys across async actions」，
    属 core-store 分片（R6-052 异步脏键）改动的在途状态，与本分片 4 个文件无关（本分片文件全部通过）。
npx eslint --max-warnings 0 src/core/cache/LRUCache.ts src/core/compose/StoreRegistry.ts \
         src/core/errors/GeomStoreError.ts tests/unit/r6-f1-04-*.test.ts   → 0 问题
    （scripts/** 在 eslint.config.js 的 ignores 内，故未参与 lint；未新增任何 eslint-disable）
npx prettier --check <上述 7 个源文件 + 4 个测试文件>   → All matched files use Prettier code style!
npx tsc --noEmit / npx tsc --noEmit -p tsconfig.tests.json
  → 本分片 7 个源文件与 4 个测试文件零报错（仓库当前报错全部在他人在途文件：
    src/core/performance/__r6old_probe.test.ts、tests/unit/hot-round6.test.ts、
    tests/unit/extras/ocr-low-round4-p2.test.ts 等）
```
