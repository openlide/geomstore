# G6-scripts-tools-medium (p1+p2) 判定记录

#24 | FP | 三条证据任一即可否掉：①全仓 `find . -name "*.example.ts" -not -path "./node_modules/*"` 命中 0（该 exclude 项当前无任何匹配对象）；②tsconfig 的 include/exclude 一律相对**配置文件自身目录**解析，故在 `tools/tsconfig.examples.json` 里加 `**/*.example.ts` 只会覆盖 `tools/**`，碰不到 include 声明的 `../examples/**/*`、`../src/**/*` —— 报告给的这行 diff 本身是空操作（要覆盖 examples 得写 `../examples/**/*.example.ts`）；③报告首句已承认 exclude 拦不住被 import 传递拉入的文件，即 exclude 承担不了它想要的保护。故未改。
#29 | fix | 成立。实测两类错误 code 可区分：`node --input-type=module -e` 下 require 未安装包 → `MODULE_NOT_FOUND`，require 存在但内部抛错的包 → code 为 undefined（探针已按此实现分支）。minify-dist 的 `tryRequire` 改为只吞 MODULE_NOT_FOUND，其余 `console.error` 后 rethrow（由 main catch → 退出码 1，与文件头「真实故障不走宽松降级」一致）；skill-api 的 `getAliasedSymbol` catch 改为打印符号名后 rethrow，不再静默退回别名文本。
#32 | fix | 成立：改前 `grep -n mkdirSync scripts/postbuild-dist.mjs` 命中第 21 行 `fs.mkdirSync(distDir,{recursive:true})`，全文无产物校验。现改为「dist 存在 + readdirSync 非空」双检，失败 `process.exit(1)`，文件头补写失败分级。generate-subpath-stubs.mjs 的 stub 目标校验不在本组可改清单 → 待办。
#34 | fix | 成立（type 标记覆写部分）：改前无条件写 `{type:'module'}`（实测真 `dist/package.json` 就只有这一项，无法区分是谁写的）。改为存在则「解析→合并→回写」，解析失败/非对象一律 exit 1。探针实跑（把脚本副本放到 `tmp-probe/scripts/`，其 distDir 落为 `tmp-probe/dist`，不触碰真 dist）：预置带 name/main/exports/sideEffects 的 package.json 后运行 → 退出 0，四字段全在且追加 `"type": "module"`，日志 `marker merged into dist/package.json`。clean()/generate() 的归属标记部分属 generate-subpath-stubs.mjs → 待办。
#35 | fix | 成立且实测已偏移：`node -e "require.resolve('esbuild')"` → `node_modules/esbuild/lib/main.js`（`grep esbuild pnpm-lock.yaml:138` 显示它只是 ts-jest 的 peer、被 `pnpm-workspace.yaml` 的 `nodeLinker: hoisted` 提升的幽灵依赖，且 `allowBuilds.esbuild: true` 早已批准其构建脚本），故文件头「esbuild 未采用」为假、第 101 行的 esbuild 分支实际长期胜出。现改探测顺序为 terser（package.json:133 显式声明、锁版本）优先，esbuild 降为兜底并在头注释写明理由；两后端选项对齐并显式化：共用 `ECMASCRIPT_TARGET=2020`（与 tsconfig `target: ES2020` 一致），esbuild 补 `format:'esm'`（此前依赖隐式默认，对 ESM 属赌博）+ `legalComments:'none'`，terser 补 `mangle.toplevel`（与 esbuild 标识符压缩对齐）+ `format.comments:false`（terser 默认仍留 `/*!`）。
#36 | fix | 成立：`fs.existsSync/readdirSync` 跟随链接，`removeDirTree` 的 unlink/rmdir 不可回滚。新增 `rejectUntrustedTarget()`：`realpathSync(dist)` 与 `path.join(realpathSync(root),'dist')` **全等**比较（比「仍在 root 内」更严，仓库内错向链接同样拒绝；junction 的 `lstat().isSymbolicLink()` 在 Windows 上不稳，故以 realpath 为准），并校验是目录；不通过 → `process.exit(1)`（这不是「清理失败」而是「目标不可信」，已在文件头把该豁免写清）。实测本机不误伤：realpath 两侧均为 `D:\WorkSpace\openlide\GeomStore\dist`，win32 下再按 lowercase 比较。
#37 | fix | 成立：改前 `collectFiles(distDir,'.map')` 裸后缀匹配，与文件头「.js.map / .d.ts.map」承诺不符。改为 `MAP_PATTERN=/\.(?:js|d\.ts)\.map$/`；`node --input-type=module -e` 实测：a.js.map / a.d.ts.map / a.min.js.map → true，a.css.map / vendor.map / x.map / a.js.tsbuildinfo → false。探针 dist 里的 index.js.map 被删、keep.css.map 未被删。
#38 | fix | 成立：`before` 为 0 时 `(saved/before)*100` → `-NaN%`。现在压缩器探测**之前**双条件短路（`files.length===0`、`before===0`），并区分失败等级：宽松只告警跳过，`--strict` 报「发布链路要求必须有压缩产物」+ 退出码 1；汇总行注释标明 before 已保证 >0。
#40 | reject | 事实一半成立、修法方向危险。`Dirent.isDirectory()` 对符号链接目录确为 false（Node 用 lstat 语义），但报告建议的 `|| entry.isSymbolicLink()` 让本脚本去**跟随**链接，而它 `writeFileSync` 原地写回——等于把压缩结果写到 dist 之外，正是同一批报告在 #36 里要求防的事。`.mjs/.cjs` 一项实测无对象：真 dist 文件普查只有 104 .js / 104 .d.ts / 1 .json，构建链是纯 tsc 且 src 无 .mts/.cts。正确修法已落地：保持不跟随，但把链接收集出来上报（默认 WARN 继续压其余真实文件，`--strict` 因「全部 js 已压缩」不成立而中止），并加与 #36 同一套的 dist 真实目录校验；`.js`-only 附注理由，不加匹配不到的后缀。
#41 | FP | 两条依据都不成立：①「项目规则禁止嵌套三元」查无实据——`grep -rniE "nested[ _-]?ternary|嵌套三元"` 全仓仅命中 ocr.md 与其派生分组文件，eslint.config.js 无 `no-nested-ternary`，且 `lint` 脚本是 `eslint src tests --ext .ts`、ignores 里明确含 `scripts/**`，*.mjs 根本不进 ESLint；CONTRIBUTING.md/docs 亦无该约定。②「两个都是 '.' 时返回 -1」不可达：`entries` 来自 `Object.entries(pkg.exports)`，键唯一，'.' 至多一个。当前实现即「main 置顶 + 其余字典序」，与注释一致 → 未改。（附带情报：ocr.md:1440 用了同一句「project rule forbids them」，同属无据断言，主控可据此复核同类条目。）
#45 | fix | 成立：改前脚本用调用方 cwd。现按脚本自身位置定位仓库根（`SCRIPT_DIR=$(cd -- "$(dirname -- "$0")" && pwd -P)` + `cd "$SCRIPT_DIR/.."`，不用 `readlink -f`：BSD/macOS 与部分 Windows shim 不支持），并 `[ ! -f package.json ]` → 退出 1。实测（在 `tools/` 下按同一逻辑跑）：`resolved cwd: /d/WorkSpace/openlide/GeomStore`、guard OK；`bash -n` 通过。
#46 | fix | 成立：第 1/2 步只 `echo` 标签不做任何事，且标签宣称「已修复导入/测试」，与真实行为（只跑 build + test）相反。取报告第一方案「remove/rename」：删掉两步、把脚本自述改成「编译 + 测试校验」，结尾文案改为「校验通过：编译与测试均无错误」，并在注释里指明可自动修复部分走既有的 `pnpm lint:fix`。文件名保留（`grep -rn fix-errors` 全仓只命中 .ocr-fix/ 内部文件，docs 与 .github 均无引用，但避免本地用法失配不动它）。

## 实跑边界与待办
- 允许范围内做过：`node --check`（4 个 .mjs 全过）、`bash -n`、纯函数级 `node --input-type=module -e`（MAP_PATTERN、require code、realpath 比较）、脚本副本指向 `tmp-probe/dist` 的 postbuild 实跑。
- 未跑（本沙箱被权限拦截 / 指令禁止，需主控统一验证）：
  1. `pnpm build`（验证 postbuild 的「dist 缺失/为空 → exit 1」端到端，以及 clean-dist 新守卫在正常布局下不误伤）。
  2. `pnpm build:min` 与 `pnpm build:release`（验证 terser 分支胜出、`-NaN%` 不再出现、压缩前后体积报告合理）。
  3. `pnpm skill:api`（验证新增 diagnostics / 零导出门禁对健康 dist 不误报；若有误报，优先核对 `[语义]` 一类，`skipLibCheck:true` 下理论上应为空）。
  4. `tools/fix-errors.sh` 端到端（内含 npm run build）。
  5. generate-subpath-stubs.mjs 的两项（#32 stub 目标存在性校验、#34 删除/覆写的归属标记）——该文件不在本组可改清单内。
- 临时探针：创建于 `D:\WorkSpace\openlide\GeomStore\tmp-probe\`（含 probe33/、probe24/、scripts/、dist/、broken-mod/ 等），已 `rm -rf tmp-probe` 删除并二次确认 `test -e` 不存在、`git status --porcelain` 中无 tmp-probe 条目。
