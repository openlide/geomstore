# f2-13（wave f2）
条数：2｜FIXED 2 / FP 0 / REJECT 0 / NEEDS-MAIN 3（异步数组补趟、round-4 测试改口径、isIndexKey 谓词收敛）
涉及文件：src/extras/snapshot/clone.ts（本片只动这一个）＋新建 tests/unit/r6-f2-13-snapshot-clone-arrays.test.ts

## R6-099 low src/extras/snapshot/clone.ts:L199-L570
verdict: FIXED
改动: `normalizeDescriptorFlags` 把 `enumerable` 一律落为 `true`（三个标志里唯一被故意改写的一位），
  `writable` / `configurable` 继续按源还原。能走到这里的不可枚举描述符只可能来自
  `includeNonEnumerable: true`（该选项关闭时键集取自 `Object.keys`，Proxy 的 ownKeys 陷阱也要过同一层
  可枚举过滤），所以提升不会波及默认路径；于是这个选项第一次有了下游读者——`Object.keys`、
  `JSON.stringify`、`diff.ts:398-399` 的键集比对（两侧都按 `Object.keys` 取键）。
  两条克隆路径共用这一个函数，异步引擎 `clone-async.ts` **无需改动即同口径生效**（它的占位属性与
  processQueue 的填充描述符都取归一化结果，见 SnapshotManager.ts:454-460），故这条不留异步侧 NEEDS-MAIN。
  方向取报告建议的第一条（写成可枚举）：另一条「只在 types.ts 写清限制」的落点不在本片可改文件内，
  且「写进了对象但没人能读到」的选项等于没有选项，按行为修。
回归锁: tests/unit/r6-f2-13-snapshot-clone-arrays.test.ts › R6-099（5 例：不可枚举键落为可枚举且
  writable/configurable 按源还原、两次快照间只有不可枚举标记变化时 `compareSnapshots().changed` 报得出 true、
  访问器属性被该选项拉进来时同口径、默认关闭该选项时快照键集不扩大、源本就可枚举的键标志位不受影响）
  —— 修复前先红（前 3 例失败，其中 diff 那条正是 `changed: false`），修复后 13/13 绿
判据: 行为变更由实跑确认覆盖两条路径——`npx jest tests/unit/extras --ci` → 805 passed / 1 failed，
  唯一失败是第四轮 #322 那条锁着老口径的用例，且它断言的是**异步**克隆品（`clone-async` 未改仍走到同一个
  `normalizeDescriptorFlags`），恰好证明异步侧一并改了
文档: 行为变更（非纯修复），需同步三处 ——
  ① `src/extras/snapshot/types.ts:21` 的选项注释由「是否包含不可枚举属性」改为
    「是否把不可枚举的自有键也带进快照；这类属性在克隆品里落为**可枚举**，故 `Object.keys` /
    `JSON.stringify` / `compareSnapshots` 都读得到。默认关闭，关闭时快照键集与源的可枚举键集一致」；
  ② CHANGELOG：`includeNonEnumerable: true` 的快照形状变化（多出原先不存在的可枚举键），
    按 `Object.keys` 消费快照 data 的调用方会看到它们；默认路径无变化；
  ③ docs 快照章节若写过「克隆还原源属性描述符」，补一句 `enumerable` 是唯一被提升的一位。
NEEDS-MAIN: 请主会话把 `tests/unit/extras/ocr-low-round4-p2.test.ts` 的 #322 用例改成新口径——
  标题改为「includeNonEnumerable 下不可枚举的原语属性提升为可枚举（两路径一致）」，
  第 290 行断言改为 `{ writable: false, enumerable: true, configurable: false }`；
  第 291 行的两路径一致性断言无需改（它锁的正是本次要保的东西）。该测试文件不在本片可改清单内。

## R6-100 low src/extras/snapshot/clone.ts:L461-L491
verdict: FIXED（同步侧）＋ NEEDS-MAIN（异步侧镜像）
改动: 数组分支在逐下标克隆之后补一趟非下标自有键，与 `core/utils/clone.ts:197-204` 的既有决定合流。
  键集口径与对象分支一致（`includeNonEnumerable` 决定取 `Object.keys` 还是 `Object.getOwnPropertyNames`），
  过滤掉 `'length'`（它是每个数组的自有键且**不可配置**，在克隆品上重新定义会直接抛 TypeError）与规范下标键。
  同时把单键的「取描述符 / 求访问器值 / 深克隆子值 / 还原标志 / 失败落账」抽成 `copyOwnKey`，
  对象分支的键循环与数组附加键共用同一份判据（不再有「两条路径各写一遍」的第三份方言）。
  未推翻 hot R6-008 的三道门槛：`isSlotBearingBuiltin` 保留原引用、`Date/RegExp/Map/Set/Array` 的 `isExactly`
  门槛、类实例仍重建为同类实例——锁里专门留了一条「Array 子类带 meta 仍返回原引用」守住它。
回归锁: tests/unit/r6-f2-13-snapshot-clone-arrays.test.ts › R6-100（8 例：附加可枚举键随快照保留且
  `deepEqual(克隆, 源)` 由 false 转 true、附加键的对象值走深克隆不与活状态共享、附加键上的循环引用经 visited
  收敛回克隆品自身、非规范数字键 '01' 作为附加键保留而 `length` 不被复制成自有数据属性、
  `__proto__` 作附加键时承载为克隆品自有数据属性且副本原型仍是 Array.prototype、
  `includeNonEnumerable` 打开时数组上的不可枚举附加键同样带进来、稀疏数组「洞落成真实 undefined 元素」的既有
  取舍未被改动、Array 子类仍保留原引用）—— 修复前这 8 例全红
判据: `npx jest tests/unit/extras/snapshot tests/unit/hot-round6.test.ts tests/unit/core/utils --ci` 全绿
  （含 hot 的 R6-008 5 例），说明补趟没有回归到「重建子类」那条老路上
文档: 行为变更 —— 快照 data 里带附加属性的数组不再丢这些键（与 `deepCloneState` 对齐），
  回滚/回放场景不再少字段；CHANGELOG 与 docs 快照章节需写「数组的非下标自有键随快照保留」，
  并把 `clone.ts` / `clone-async.ts` 数组分支上「仅按下标克隆」的注释同步掉（异步侧改完后那句要一起改）。
NEEDS-MAIN（1，异步镜像）: `src/extras/snapshot/clone-async.ts` 数组分支（现 199-231 行）是同一段代码的第二份，
  要在那边补同一趟——按下标入队之外，把 `extraKeys`（同一套过滤：排除 `'length'` 与规范下标键）按
  `target.kind: 'prop'` 入队，`descriptor` 取 `normalizeDescriptorFlags(descriptor, isAccessor)` 的结果，
  与它自己的对象分支同一写法；**在补上之前两条路径对「带附加属性的数组」结论不同**（同步保留、异步丢失），
  补完后 `deepEqual(snap.data, state)` 与快照两路径的一致性才重新成立。该文件不在本片可改清单内。
NEEDS-MAIN（2，谓词收敛）: `isIndexKey` 现在仓库里是第三份私有副本（`core/utils/clone.ts:120`、
  `extras/action/decorators/cache.ts:64`、本文件新增一处，三处实现逐字相同）。建议主会话把 core 那份导出、
  三处改为 import 同一份以消除漂移；本片不动 core 与 cache 文件，故先按现状在各处注释里指路。

## 顺带核实的两条「非本片」用例状态（gate 阶段无需再查）

`tests/unit/r5-extras-snapshot-fixes.test.ts` 中途曾报 2 红，均已排除与本分片的因果关系，且**现地复核已转绿**
（`npx jest tests/unit/r5-extras-snapshot-fixes.test.ts --ci` → 36 passed，diff.ts 的 owner 同期在飞修的就是这两条）：

1. 「同步：容器登记后才抛错时，半成品容器从 visited 上撤销」——当时实得 `Map {"k" => 1}`，即**源实例本身**
   （部分克隆的 Map 只可能是空的）。唯一能产出该形状的是本工作树新增的
   `if (!isExactly(value, Map.prototype)) return value`（`git diff src/extras/snapshot/clone.ts` 显示为 hot R6-008 的新增行），
   与本分片改的 `normalizeDescriptorFlags` / 数组分支无交集。提醒：hot-p0 自验只跑了 `tests/unit/extras/snapshot`（目录），
   没跑 tests/unit 根下的这些锁，同类漏网可能还在别的分片里。
2. 「配到同一键之后值仍按该键路径继续比」——期望 `root.key[0]` 实得 `root[A]`；
   `git diff src/extras/snapshot/diff.ts` 显示 `keyToken` / `mapValuePath` 是在飞新增（条目路径改按键身份生成），
   属 f1-08 的 diff.ts 口径，与本分片无关。

## 本分片自验

- `npx jest tests/unit/r6-f2-13-snapshot-clone-arrays.test.ts --ci` → 13 passed（修复前同一条命令 9 failed / 4 passed）
- `npx jest tests/unit/r6-f2-13-snapshot-clone-arrays.test.ts tests/unit/extras/snapshot tests/unit/hot-round6.test.ts --ci` → 8 suites / 228 passed
- `npx jest tests/unit/extras --ci` → 805 passed / 1 failed（唯一失败即上面 R6-099 的 NEEDS-MAIN 老口径用例
  `ocr-low-round4-p2.test.ts:281-292`；它的第 291 行「两路径一致」断言**通过**，正说明异步侧一并改了）
- `npx jest tests/unit/core/utils --ci` → passed（`deepCloneState` 侧无改动，合流方向是快照向 core 靠）
- `npx eslint --max-warnings 0 src/extras/snapshot/clone.ts tests/unit/r6-f2-13-snapshot-clone-arrays.test.ts` → 0 错
- `npx prettier --check src/extras/snapshot/clone.ts tests/unit/r6-f2-13-snapshot-clone-arrays.test.ts` → All matched files use Prettier code style
- `npx tsc -p tsconfig.json --noEmit` → 全库 0 错（`grep -c "error TS"` = 0）
- 改动范围：`git status --porcelain` 下本分片只动 `src/extras/snapshot/clone.ts`，新增 `tests/unit/r6-f2-13-snapshot-clone-arrays.test.ts`
  （`clone-async.ts` / `diff.ts` / `SnapshotManager.ts` 的 M 状态属 hot 与 f1 分片）

