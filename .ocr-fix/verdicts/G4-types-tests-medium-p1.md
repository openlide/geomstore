# G4-types-tests-medium-p1 判定记录

#383 | reject | 「'warn' 实为死别名」不成立：实测把 'warn' 从 ErrorLevel 删掉后 `npx tsc --noEmit -p tsconfig.tests.json` 报 8 处 TS2345/TS2339（tests/unit/core/error/ErrorHandler.test.ts:312/317/337/345/362/525…，禁改目录），且该文件 #36 回归用例「warn 别名走 warning 通道」与 error-boundaries.test.ts:102 显式断言别名行为；ErrorLevel 已随 dist 发布，删除即破坏性变更。按报告自备的退路处置：保留别名、就地 `@deprecated` 标注并在类型 JSDoc 写明规范集合/别名由来，行为不变（warn 仍与 warning 同分支）。
#385 | fix | 属实：`ErrorContext.error` 契约是 Error，但 `ErrorHandler.handle` 的 JSDoc 通行写法即 `error as Error`；`throw null/undefined` 时 `error.message`（warning/info 分支）与 `error.stack`（error/critical 分支，报告未点出的同一缺陷）会在处理器内抛 TypeError 顶掉原始失败，字符串抛值只打 `undefined`。新增 `describeErrorProperty` 助手同时兜底 message/stack，非字符串以 `String(error)` 收口；真实 Error 的输出逐字不变（既有用例全绿）。
#390 | fix | 成立：同文件内 setItem 记录后重抛、getItem/removeItem 记录后吞掉，读取失败与「键无数据」不可区分（后续落盘会覆盖真实数据），删除失败会让 clearOnUninstall 谎报。取报告第一方案「一律重抛交给调用方」：persistencePlugin 三条路径本就各自 try/catch + 日志（src/plugins/builtin.ts:197/285/315），无行为回归；并把契约写进 `StorageBackend` 的 JSDoc（`getItem` 返回 null 只代表无数据）。回归用例见 tests/unit/types-interface-state.test.ts #390。
#391 | fix（部分按报告写法不可行） | 内联 wx 形状写两遍（getter 签名 + globalThis 断言）属实，已提取单一 `WxStorageApi` 供两处引用。但报告建议的 `Partial<Pick<StorageBackend, 'getItem'|…>>` 派生不可用：两侧方法名（getStorageSync vs getItem）与返回类型（unknown vs string|null）都不同，正是 #389 靠 `unknown` + 类型守卫收口的差异；已在类型 JSDoc 说明「为何不能从 StorageBackend 派生」。
#392 | fix | 属实：实现是交叉（`&`），三处注释都写「联合」。ExtractStates/ExtractActions/ExtractGetters 的标题统一改为「交叉（合并）」，并在 ExtractStates 补一句交叉 vs 联合的语义差别（组合 store 同时拥有全部成员键）。
#394 | fix | 逐点处置：`StoreTreeNode.store` 与 `ComposedStore.stores` 由 `any` 收成 `Store | null` / `Record<string, Store>`（比报告的 `unknown` 更准：树节点持的就是本库 Store，消费方仍可用公开成员，误用成员名会被检查）；`ResolveState` / `Actions` / `InferGetterReturn` / `StoreLike.getters` 的 `any` 属必要写法，各补实测过的理由（临时用例证实：`(...args: unknown[])` 会拒掉带形参的工厂与对象字面量 actions，TS2322/TS2353）；`tests/setup.ts` 的 `globalThis as any` 换成最小 `WxTestGlobals` 接口。报告点到的 `tests/jest.d.ts` 已在 Wave A #405/#406 整文件删除，该子项已过时。
#395 | fix | 三份同构递归已收敛为 `ExtractField<Stores, K>` + `ExtractMember<First, K>`。关键实测：守卫不能无条件套到 state/actions 上——`undefined extends any` 为 true，元组里出现 `let store: any`（tests/unit/core/compose/composeStore.test.ts 的既有写法）时整个交叉会塌成 `Record<never, never>`，改出 100+ 处禁改目录错误；故加 `IsAny` 直通分支，`#393` 的可选 getters 守卫保留。回归断言 tests/types/compose-field-extraction.typecheck.ts（含 any 成员与交叉精度双向赋值）。
#396 | fix | 成立：`StoreLike.actions` 与 `./store.js` 的 `Actions` 定义逐字相同，已改为 `actions: Actions` 并删掉重复的 eslint-disable；fixture 断言 `[StoreLike['actions']] extends [Actions]` 双向成立、具体 store 仍满足 `StoreLike`。按报告提示未把 `getters` 换成 `Getters`，并在该字段上把「为什么必须保留 any」写进注释。
#397 | fix | 双向可赋值性实测：改前 `[ActionDecorator] extends [MethodDecorator]` 成立、反向 TS2322（`target: unknown` vs `Object` 的参数逆变），且 `const d: ActionDecorator = withRetry()` / `withLoading()` 直接编译不过（实测 3 处）；库内 8 个公开装饰器返回类型均为 `MethodDecorator`，`ActionDecorator` 无任何内部引用。已改为 `export type ActionDecorator = MethodDecorator` 并补 @ts-expect-error/装饰器语法位断言。
#398 | fix（报告原片段不可直接采用） | 非法状态确实可表示（实测 `success: true` + `error` 原样通过）。但报告给的联合形式会破掉既有消费方：`history[0].data` / `history[0].error` 未收窄即读取，实测 TS2339 两处（对应 tests/unit/extras/action/AsyncActionSupport.test.ts:55/85/103/730-732，禁改目录）。改为「交叉键声明为可选 `undefined`」的判别联合：非法组合仍不可表示（实测 TS2352），未收窄读取保持 `T | undefined` / `Error | undefined`，收窄后精确。`duration` 与时间戳的派生关系写进 JSDoc（类型层无法强制，库内两条构造路径均为 `endTime - startTime`）。
#399 | fix | 成立：`A = unknown` 使默认情形下 `ctx.actions.x()` 不可调用。按报告收紧为 `S extends State = State, A extends Actions = Actions`，与 `Store<S, A extends Actions>` / `ActionExecutor` / `ActionUtils` 同口径（三者默认值是 `AsyncActions`，报告写作 `Actions`，取更宽的 `Actions`）。实测副作用已在 JSDoc 与 fixture 注明：`Actions` 带 Record 索引签名，以 `interface` 声明的 action 集合不满足约束（TS2344），须用 type alias/字面量推断。补 tests/types/action-decorator-result-types.typecheck.ts 作为该「此前从未被引用」类型的固定用例。
#400 | reject | 问题真实（`ActionLoader` 只在构造函数读一次该字段、`private options` 已 `Omit` 掉它、`setOptions()` 静默忽略），但报告的修法（移出公开 options、改专用内部参数/symbol 注入）必然联动 src/extras：`ActionLoader.ts:84/100-106` 的入参与 `withLoading.ts:126/133` 的注入点都以 `ActionLoaderOptions` 形状透传（`{ ...options, sharedLoadingCounts }`），types 侧单独移除即 TS2353/TS2345。本次只强化字段文档（写明「仅构造期读取、setOptions 忽略、跨 loader 共用同一 Map 由注入方负责不变量」），彻底方案留给主控联动，见回报待办。

## 主控需知（联动待办）
- #400 彻底修法需要改 src/extras：`ActionLoader.ts` 构造函数入参拆成 `ActionLoaderOptions | { internal }`（或 symbol 键），同步 `withLoading.ts:126/133` 的注入点与 `setOptions()`；完成后把 `sharedLoadingCounts` 从 `src/types/action.ts` 的 `ActionLoaderOptions` 移出，并删掉 docs/API.md:309 那一行（或标注 @internal）。
- #390 使 `WxStorageBackend.removeItem` 由「吞」变「抛」：src/plugins/builtin.ts:315-317 的 `catch { /* 存储不可用时静默失败 */ }` 现在会顶掉自定义后端（不带自身日志）的删除失败，建议该 catch 补 `console.error` / emit `onError`（src/plugins 属我的禁改目录）。
- 给 `StoreTreeNode.store` 定类型后暴露 core 侧既有签名问题：`src/core/compose/composeStore.ts:910` 的 `createStoreTree(stores: Store[])` 用默认泛型 `Store`（= `Store<object, Actions, Getters<object>>`），具体状态的 Store 会因 `getters` 参数逆变而无法传入（实测 TS2322，现有测试全靠 `let store: any` 才没发现）。建议改收 `StoreLike[]`。
- #397/#398/#399/#383 属公开类型面变更（`ActionDecorator` 等价 `MethodDecorator`、`ActionResult` 变判别联合、`ActionExecutionContext` 默认泛型收紧、`ErrorLevel` 的 `warn` 标 @deprecated），Wave D 需同步 CHANGELOG（按 minor）与 docs/API.md 的 Action 章节；docs/API.md 未列这三个类型，无需改写形状描述。
- 验证：`npx tsc --noEmit -p tsconfig.typecheck.json`、`npx tsc --noEmit -p tsconfig.tests.json`、`npx eslint src/types tests/setup.ts tests/types tests/unit/types-interface-state.test.ts` 全部零输出；`npx jest --ci --silent` 按目录分批跑完 tests/unit 全部 11 个子目录/文件（cache、core、error、extras、hooks、integrations、plugins、property、regression、store、types-interface-state），无失败用例（未跑 `test:ci`，属禁项）。
