# f1-06（wave f1）
条数：7｜FIXED 6 / FP 0 / REJECT 0 / NEEDS-MAIN 1

## R6-043 medium src/core/utils/helpers.ts:L364-L396
verdict: FIXED
改动: 给 `clone()` 的内建特判补 `isExactly` 同口径准入门（新增模块私有 `isExactlyBuiltin`）——顶层 Date/RegExp 只在原型恰为 `Date.prototype`/`RegExp.prototype` 时新建；shallow 的 Array/Map/Set 分支同门槛。子类实例因此统一落到降级口径（shallow 走「非纯对象返回原引用」，deep/safe 交给 deepCloneState 返回原引用），顶层与嵌套处不再两套结果。JSDoc 补 @remarks 写明子类实例走哪条降级路径、json 模式不受影响。
回归锁: tests/unit/r6-f1-06-clone.test.ts > R6-043 clone 对内建类型子类不再降级成基类副本（4 用例）
判据: 报告成立。修复前在旧构建产物上实跑（`node -e` 读 dist/core/utils/helpers.js）：`shallow MyMap -> false undefined`、`deep MyDate -> false undefined`（instanceof 子类为 false、子类方法丢失），正是新用例断言的两点，先失败后通过。`isExactly` 未从 clone.ts 导出，故本模块复述同一条判据并在注释里点名「两处必须一起漂移」；收敛为单一导出需改 `src/core/utils/clone.ts`（不在本分片文件清单，非阻塞）。
文档: 行为变更（此前顶层内建子类返回基类新副本，现在返回原引用），属修 bug 而非 API 变更。CHANGELOG 建议措辞：「`clone()` 不再把 Date/RegExp/Map/Set/Array 的子类实例降级成基类副本，与 `deepCloneState` 同口径」。

## R6-089 low src/extras/action/ActionHistory.ts:L50-L65
verdict: FIXED
改动: 给桶数设上限 `MAX_TRACKED_ACTIONS = 1000`（模块私有常量，不进公开 API）。`record` 命中已有桶时 delete+set 把该桶挪到 Map 表尾，迭代序即「最近记录序」；新开桶且已达上限时调 `evictLeastRecentlyRecorded()` 从表头淘汰整桶，并在 dev（`!isProduction()`）打一条点名被淘汰 Action 与疑似原因的 `console.debug`。类注释与方法文档同步改成「每桶有界 + 桶数有界」。
回归锁: tests/unit/r6-f1-06-action-history-cap.test.ts > R6-089 ActionHistory 桶数有界（3 用例：越界整桶淘汰 + dev 提示 / 按最近记录序而非插入序 / 正常规模不受影响）
判据: 报告成立。修复前在旧构建产物上实跑（dist/extras/action/ActionHistory.js）：1001 个不同名字 → 桶数 1001；再追加 5000 个 → 桶数 6001，线性增长确认。采纳「桶数上限 + LRU 整桶淘汰」，不采纳「记录前判目标是否为函数」：后者要改 `AsyncActionSupport.recordOutcome` 的语义（非函数派发本身是应当留在诊断里的失败），且 `ActionExecutor` 是公开入口、拿不到 actions 的静态形状；前者对任意输入都有界且不丢错误可见性。
文档: 改了可观察行为（超上限后旧 Action 的历史/统计会被丢弃）。CHANGELOG 建议措辞：「ActionHistory/ActionExecutor 的 Action 名桶数新增上限 1000，越界按最近记录序整桶淘汰（开发期有 console.debug 提示）」。

## R6-044 medium src/extras/action/ActionLoader.ts:L569-L600
verdict: FIXED
改动: `lastSetState` 的赋值点从「wrap 时」挪到「每次调用时」（wrapper 内在 `captureCallScope` 之前 `loader.lastSetState = setState`）。`clear()` 仍把字段清成 undefined（丢掉 bind 住 store 的旧引用），但同一个 wrapper 再被调用时会在调用时刻重新登记，复位能力与在途写入函数始终一致——wrapper 生命周期与 store 生命周期解耦，不偷偷重建 wrapper。同步改写字段文档、`clear()` 的 @param 与注释（补上「丢掉不影响后续复位能力」的理由，替换原先站不住的 GC 论据）、`resetDerivedState` 的空值守卫注释。
回归锁: tests/unit/r6-f1-06-action-loader.test.ts > R6-044 clear() 之后复用同一个 wrapper 仍有复位能力（3 用例：loading 复位 / error+errorData 复位 / 换键 setOptions 给旧键补写复位值）
判据: 报告成立。修复前在旧构建产物上实跑（dist/extras/action/ActionLoader.js）：wrap→call→clear→复用同一 wrapper→clear 之后 `after second clear loading= true`（复位值一个也没写出去）。
文档: 无对外契约变更（只是让 `clear()` 已有的承诺成立）。`clear()` 的 @param 文案已从「最近一次 wrap 注入的」改成「最近一次调用登记的」，docs 里若有同一旧措辞需一并改。

## R6-090 low src/extras/action/AsyncActionSupport.ts:L379-L388
verdict: FIXED
改动: 门面 `getHistory` 的 JSDoc 改成与转发目标 `ActionHistoryTracker.getHistory` 一致的两段式排序口径（带 actionName 为时间正序、不带参数聚合时才按 startTime 倒序），补上 `history[0]` 的正确取法与「只有数组容器是副本、条目与内部桶共享」的只读告警。纯注释改动。
回归锁: 不加（documentation 类、纯 JSDoc，无行为变化）。排序行为本身已由 tests/unit/extras/action/ActionHistory.test.ts 与 r5-extras-action-p1-* 覆盖
判据: 报告成立（原第 384 行「执行历史数组（按时间倒序）」与带参调用的实现相反），已与 ActionHistory.ts 的口径对齐。
文档: 若 docs/API.md 中 `getHistory` 的返回值描述也写着「按时间倒序」，需同步成两段式口径（本分片不许动 docs/**）。

## R6-045 medium src/extras/action/decorators/cache.ts:L138-L163
verdict: FIXED
改动: `sortKeysDeep` 的对象分支改用以 symbol 为准的取键方式：新增 `ownEnumerableKeys`（`Reflect.ownKeys` + `propertyIsEnumerable`）、`keyValuePairs`（把进不了 JSON.stringify 键位的 symbol 键折成 `[键标记, 值标记]` 对并按标记排序）、`isIndexKey`。对象分支的「无可枚举键」身份判据也改用含 symbol 的键集（原来只有一个 `[TOKEN]` 品牌键的类实例会被误判成无键）；产出改为 `['__obj', typeTag, sorted, symbolPairs]`。数组分支同口径修掉另一类撞键：产出 `['__arr', 元素标记, 非下标自有键对]`（含 `arr.meta` 这类字符串附加键与 symbol 键）。字符串键与 symbol 键分开排序（`sort` 的默认比较器对 symbol 做 ToString 会抛 TypeError，混排会让键生成整体失败）。失效清单注释补上这两类。
回归锁: tests/unit/r6-f1-06-cache-key.test.ts > R6-045 互异参数必须生成互异缓存键（6 用例：只差 symbol 值 / 只差 symbol 键 vs 空对象 / 同 description 的不同 symbol 作键 / symbol 键顺序无关仍命中 / 数组附加键 / 只有 symbol 品牌键的类实例）
判据: 报告成立，按 correctness 优先修（不是损失命中率而是返回别人的结果）。修复前在旧构建产物上实跑（dist/extras/action/decorators/cache.js）：`run({id:1,[TOKEN]:'a'})` 与 `run({id:1,[TOKEN]:'b'})` 得到同一个结果、`calls=1` 且打印 `[Cache] Hit`。
文档: 纯修复（缓存键生成的内部形状变化，不对外暴露）。若 BEST_PRACTICES/GUIDE 里有关于 `withCache` 默认键「忽略 symbol 键」的表述需删除。

## R6-046 medium src/extras/action/decorators/debounce.ts:L129-L144
verdict: NEEDS-MAIN
改动（本分片内已落地）: 把「本组入口按调用时的 `this` 定位状态、装饰 store action 时不可用」写进 `debounce.ts` 模块头与 `cancelDebouncedCalls`/`flushDebouncedCalls`/`disposeDebouncedState` 的 `@param host`；`throttle.ts` 同步同一限制（模块头 + 三个入口），避免只修一半。措辞与「`withCache`/`withRetry` 没有对应收尾入口」同口径。
回归锁: 不加——把「入口对 store action 无效」钉成断言会和主会话可能的修法（暴露可寻址宿主）直接对撞；现状行为由 ActionManager 一侧决定
判据（NEEDS-MAIN 阻塞点）: 报告事实成立，且根因不在 debounce.ts：`src/core/store/ActionManager.ts:127-149`（f1-05 分片 R6-037 的文件，本分片不许改）里
  `boundActions[key] = (...args) => this._withInternalAccess(() => originalAction.call(actionContext, ...args))`，
  `actionContext` 是 `initialize()` 为 `contextBase` 现造的 Proxy，只被那批箭头闭包捕获、不挂在任何公开成员上（`store.actions` 是 `boundActions`，另一个对象）。
  `debounceStates`/`throttleStates` 的键就是那次调用的 `this`，所以在 Page `onUnload` 里 `cancelDebouncedCalls(this)` 必然 `collectDebounceSlots` 判空、静默 no-op。
  需要主会话拍板两选一：(A) 维持现状 + 文档口径（本分片已按 A 落地注释，另需同步 docs/GUIDE.md:231、docs/FAQ.md:150-151、BEST_PRACTICES 的检查项）；
  (B) 由 ActionManager 把 action 上下文（或 store 实例）作为可寻址宿主暴露（例如 `actionContext` 上挂一个 symbol 或由 Store 提供 `getActionContext()`，让 `withDebounce`/`withThrottle` 在装饰 action 时把状态登记到该宿主），届时需把本分片写的「不可用」注释改回「可用 + 用法」，并补跨模块回归锁。
文档: 需要主会话同步 docs/GUIDE.md:231 与 docs/FAQ.md:150-151（当前文档假设 `cancelDebouncedCalls(this)` 对 store action 也成立）；CHANGELOG：若最终选 (B) 记为行为新增能力，选 (A) 记为文档纠正。

## R6-091 low src/extras/action/decorators/throttle.ts:L220-L227
verdict: FIXED
改动: `withThrottle` 的装饰函数 `descriptor` 改为可选并在替换 `descriptor.value` 之前判 `descriptor === undefined || typeof descriptor.value !== 'function'`，抛 `[withThrottle] can only decorate a method, but "<key>" is not a function`（与 debounce/retry/timeout/createDecorator 同族形态与消息口径）。`cache.ts:366` 同一形态一并处理（`[withCache] ...`）。`debounce.ts` 的同类缺口也顺手补上：它有 value 判据但签名把 descriptor 声明为必填、且裸读 `descriptor.value`，字段误用仍抛无关的 undefined 错误，现改为同一判据（消息不变）。未合并 `isTrackableHost` 的两处重复（第五轮已明确维持现状）。
回归锁: tests/unit/r6-f1-06-decorator-guards.test.ts > R6-091 装饰器族在装饰期拒绝非方法目标（7 用例：三者各自的字段误用/访问器误用 + 正常方法仍被包装的正向锁）
判据: 报告成立。修复前在旧构建产物上实跑（直接按运行时形状调用装饰器函数）：
  `withThrottle`/`withCache`/`withDebounce` 字段误用 → `TypeError: Cannot read properties of undefined (reading 'value')`（真实原因被盖掉）；
  `withThrottle`/`withCache` 装饰访问器 → 装饰函数自身**不报错**，报错被推到 TS `__decorate` 的 `Object.defineProperty`（即报告说的 `Invalid property descriptor…`，信息里没有装饰器名字；我的探针没有模拟那一步，所以只观察到「装饰期不报错」）；
  `withDebounce` 访问器场景已有正确报错（`[withDebounce] can only decorate a method…`）。
文档: none（报错文案更清晰，不改变任何受支持用法的语义；SKILL/docs 里若引用过旧的 undefined 报错形状需知晓已变）

## 自验（分片内命令与结果）

```
npx jest tests/unit/r6-f1-06-{clone,action-loader,action-history-cap,cache-key,decorator-guards}.test.ts \
  tests/unit/extras tests/unit/core tests/unit/store tests/unit/r5-core-misc-p2-clone.test.ts --ci
  → Test Suites: 86 passed, 86 total; Tests: 2252 passed, 2252 total

npx eslint --max-warnings 0 <本分片 7 个 src 文件 + 5 个新测试文件>   → 无输出（0 problem）
npx prettier --check <同上 12 个文件>                                → All matched files use Prettier code style!
npx tsc -p tsconfig.json --noEmit | grep <本分片文件>                → 无命中（本分片 0 个类型错误）
```

未跑全量 jest / build（按简报由主会话统一收口）。注意 `npx tsc -p tsconfig.tests.json --noEmit` 里另有**不属于本分片**的报错，别计入 f1-06：
`src/core/performance/__r6old_probe.test.ts`（他人遗留在 src/ 下的探针文件，且被 tests 工程的 include 收到）、
`tests/unit/r6-f1-08-snapshot.test.ts:193`、`tests/unit/extras/ocr-low-round4-p2.test.ts:300`、`examples/weapp/app-integration.ts`（缺 wx 类型）。
