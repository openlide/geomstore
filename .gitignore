# Dependencies
# 依赖锚定由 pnpm-lock.yaml 承担，且它照常入库（git ls-files 命中、git check-ignore 退出 1）。
# 下面两条只针对「误用 npm/yarn 时顺手提交的第二把锁」：本仓库是 pnpm-only
# （package.json > packageManager + CI pnpm install --frozen-lockfile），
# 出现第二把锁只会让 CI 与本地解析出两套树，故一律忽略、不入库。
node_modules/
package-lock.json
yarn.lock

# Build outputs
dist/
# 微信「构建 npm」专用产物（`miniprogram` 字段指向的目录，由 pnpm run build:weapp 生成）
dist-weapp/
build/
*.log
*.tsbuildinfo

# 说明：子路径转发 stub（store/ hooks/ plugins/ 等）由 prepack 钩子在打包发布时
# 生成、postpack 清理，不落盘到仓库，故无需忽略规则。详见 scripts/generate-subpath-stubs.mjs

# Test coverage
coverage/
coverage-report.json
.nyc_output/
# 根级 jest 汇总产物；锚到仓库根，避免子包/工具目录里的同名合法文件被静默吞掉
/coverage-*.json

# IDE
# .codebuddy 整体保持忽略（内含会话与本地数据），仅放开 skills 目录，使 project skill 可随仓库分发
.codebuddy/*
!.codebuddy/skills/
!.codebuddy/skills/**
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Temporary files
*.tmp
*.temp
.cache/

# 示例工程 examples/ 是仓库源码，不忽略：tools/tsconfig.examples.json 的 include 覆盖整个
# examples/**，CI 的「Typecheck (examples)」步骤就跑在它上面；整目录忽略会让干净检出缺文件
# （曾漏网：examples/global.d.ts 声明的 App/Page/Component 被已跟踪的 examples/weapp/*.ts 调用，
#  以及 examples/*/index.ts 汇总入口），新增示例也不再需要 git add -f。
# src 下的 *.example.ts 由 tsconfig 的 exclude 挡在编译之外，与此处无关。

# Environment variables
# .env.* 覆盖 .env.local / .env.production / .env.development / .env.test 等全部变体；
# 仅允许签入模板文件 .env.example（不含真实凭据）
.env
.env.*
!.env.example

# 审查素材与逐条判定台账
# ocr*.md / review.json 是外部审查工具的原始输出，只作输入不作产物；
# .ocr-fix 下除「判定结论」（decisions*.md 与 verdicts*/）外全部是可由原始报告重新生成的
# 中间产物（ledger / state / groups 分片 markdown、切分脚本），故整目录忽略后白名单放开结论文件。
# 三份素材都钉在仓库根：不加前导 / 时它们会命中任意层级，docs/ocr.md、packages/*/review.json
# 这类合法文件名会被静默忽略。
/ocr.md
/ocrreview.md
/review.json
.ocr-fix/*
!.ocr-fix/decisions.md
!.ocr-fix/verdicts/
!.ocr-fix/verdicts/**
!.ocr-fix/decisions5.md
!.ocr-fix/verdicts5/
!.ocr-fix/verdicts5/**

