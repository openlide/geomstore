# 第六轮收尾：tests/** 遗留 TS 报错清零（主会话代做）

口径：零配置改动（`tsconfig*` / `eslint.config.js` / `.prettierrc.json` 一律不动）、零 `src/**` 改动、禁 `any` / `@ts-ignore` / `@ts-expect-error` / `eslint-disable`、不为过编译放宽类型、不删断言。
门禁（改完实跑）：`npx tsc -p tsconfig.tests.json --noEmit` = 0 错；`npx jest <下列 7 个文件> --ci` = 7 suites / 93 tests 全绿，用例数与改前逐文件一致（dirty-tracking 9、enterprise 27、snapshot 16、benchmark-runner 11、compose-cache-api 11、store-patch-atomicity 6、extras/ocr-low-round4-p2 13）。
附带：`prettier --check` 与 `eslint` 对这批文件同样干净。

| 文件 | 改法 | 实跑结果 |
| --- | --- | --- |
| `tests/unit/extras/ocr-low-round4-p2.test.ts` | L304 的手写 `SnapshotDiff` 字面量补上 f1-08（R6-050）新增的必填字段 `inputTrusted: true`（该处是「无差异」场景：`changed: false` 只有在两份输入都可信时才是合法报告），并加一条 `expect(diff.inputTrusted).toBe(true)` 让这个必填字段在读侧也落地（断言只增不减）。 | tsc 该文件 0 错；jest 13 tests 全绿（用例数不变） |
| `tests/unit/r6-f1-05-dirty-tracking.test.ts` | 不改 lib：文件内新增最小局部接口 `Es2025SetLike extends Set<string>`，把七个 ES2025 集合方法按宿主形状写清楚（`union`/`intersection`/`difference`/`symmetricDifference` 返回 `Set<string>`，`isSubsetOf`/`isSupersetOf`/`isDisjointFrom` 返回 `boolean`），再加唯一收口点 `asEs2025Set(view: unknown)` 做 `as unknown as` 转换；三处 `proxy.tags as unknown as Set<string>`（两处 `tags`、一处 `other`）改走该函数。注释写明「TS 基线 lib（`tsconfig.json` 的 `lib: ["ES2020"]`，tests 配置原样继承）不含 ES2025 集合方法，而本用例断言的正是运行时白名单是否放行它们」，因此既不动配置也不用 `any`/`as never` 把这段检查抹掉。 | tsc 该文件 0 错；jest 9 tests 全绿（Node v24 宿主确实提供这七个方法，代理侧行为断言原样通过） |
| `tests/unit/r6-f1-08-enterprise.test.ts` | 按实现事实对齐泛型，不放宽：`src/integrations/enterprise/offline.ts:60` 是 `class OfflineManager<S extends State = State>`，S 在 `store.getters` 上不变。用例里的 store 一律是 `createStore({ state: { x: 0 } })` ⟹ 实例为 `OfflineManager<{ x: number }>`，故新增局部别名 `type OfflineManagerForX = OfflineManager<{ x: number }>`，把两个接收侧注解改到同一 S：holder 字段（L337）与辅助函数返回类型 `managerWithFailedItem(): Promise<OfflineManagerForX>`（L407）。 | tsc 该文件 0 错；jest 27 tests 全绿（用例数不变） |
| `tests/unit/r6-f1-08-snapshot.test.ts` | L193 的 `Array.prototype.splice` 观察器形参由 `...args: unknown[]` 改成 splice 的调用契约元组 `[start: number, deleteCount: number, ...items: unknown[]]`，`original.apply(this, args)` 于是无需 `as []` 空元组断言。口径依据：本用例统计的是 `ARRAY_MUTATING_METHODS`（`src/core/store/StateProxy.ts:18`）里的 `'splice'` 那一条，实际调用点固定为 `SnapshotManager.ts:404` 的 `queue.splice(0, queueHead)`（两实参），计数逻辑 `Number(args[1] ?? 0)` 与过滤条件一字未动。 | tsc 该文件 0 错；jest 16 tests 全绿（calls/moved 门限断言原样通过） |
| `tests/unit/r6-f2-12-benchmark-runner.test.ts` | ① L238 `runnerFor(makeStore as CreateStoreFn)` 是「形参逆变 + 缺索引签名」的双重断言不足 → 改 `makeStore as unknown as CreateStoreFn`；② L339/L357 两处实参真正卡的是 `BenchmarkRunner` 构造器形参（`packages/benchmark/src/runner.ts:146` 内联的 `<S extends State>(config: { state: S; … }) => BenchmarkStore<S>`），`CreateStoreFn` 返回 `BenchmarkStore<Record<string, unknown>>` 与之本就不相容，硬套 `as CreateStoreFn` 只是把错误挪到调用点 → 按该形参形状在测试侧命名 `type RunnerCreateStoreFn = <S extends State>(config: { state: S; actions?; getters?; enableCache?; cacheConfig?; cacheKeys? }) => BenchmarkStore<S>`，两处改 `as unknown as RunnerCreateStoreFn`；未新增 `as never`、未改局部 `CreateStoreFn` 的既有语义。 | tsc 该文件 0 错；jest 11 tests 全绿（用例数不变） |
| `tests/unit/r6-f2-12-compose-cache-api.test.ts` | ① L70-73 的 `composed.stores`：`composeStore` 的类型面返回 `Store<…>`（契约里没有路由映射表），运行时实例是 `ComposedStore` 且 `public stores: Record<string, Store>`（`src/core/compose/composeStore.ts:47`）→ 按实现暴露的字段名新增局部接口 `StoresMapOwner { stores: Record<string, unknown> }` + `storesMapOf(composed: unknown)` 窄化一次，四条断言全部保留（含原有的 `getState`/`destroy` 未被泄漏成可调用），不给 `Store` 加宽；② L88 `tree.children['__proto__'].store`：`StoreTreeNode.children` 按契约是可选字段（`src/types/compose.ts:47`）→ 改可选链 `tree.children?.['__proto__']?.store`，取不到 children 时该断言直接失败，而不是用 `!` 掩盖。 | tsc 该文件 0 错；jest 11 tests 全绿（用例数不变） |
| `tests/unit/r6-f2-12-store-patch-atomicity.test.ts` | L13 的 `interface CountingActions` 改成同成员的 `type CountingActions = { … }`：`createStore` 的 A 约束是 `Actions = Record<string, (...args: any[]) => any>`（`src/types/store.ts:125`），TS 只为**对象类型字面量**推隐式索引签名、不为 `interface` 推，故原写法必然 TS2344。四个 action 签名、`makeStore` 里的 `this.$patch` / `this.setState` 用法与全部断言不动。 | tsc 该文件 0 错；jest 6 tests 全绿（用例数不变） |
