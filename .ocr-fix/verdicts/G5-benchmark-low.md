# G5-benchmark-low 判定记录（18 条）

基线：`npx tsc --noEmit -p packages/benchmark/tsconfig.json` 改前退出码 0。

#17 | fix | 成立：`grep -rn "from '@/" packages/benchmark/src` 零命中，paths 全然是死配置；本包 `module: NodeNext` + `"type": "module"`，无 bundler/tsc-alias/`imports` 映射，`@/x` 只会被原样 emit 后在 Node 运行期炸。已删 tsconfig.json 的 `paths` 块（compilerOptions 其余项不动，删后 tsc 退出码仍 0）。
#20 | fix | 成立：根 package.json `engines.node: ">=22.0.0"`（package.json:138-140），`.github/workflows/ci.yml:23` 矩阵只有 `[22, 24]`，且 utils.ts/runner.ts 用全局 `performance`（Node 16+ 才免 flag）。已把 benchmark 的 `>=14.0.0` 对齐为 `>=22.0.0`。
#70 | reject（前提失实，改为注释说明） | 报告说「默认跑必然压在 large/xlarge 边界、上调 DEFAULT_ITERATIONS 会静默重分类默认场景」不成立：`DEFAULT_ITERATIONS` 包内零读取方（只在 index.ts 再导出），默认场景的迭代数是 config.ts 里的逐条字面量，runner 的 datasetSize 直接取 `scenario.datasetSize`；`inferDatasetSize` 只服务 createErrorResult/mergeResults 的兜底推断。数值重合是巧合而非耦合，改 DEFAULT_ITERATIONS 不影响任何档位判定。已在两个常量上补注释写明读取方与边界闭区间语义，未动数值（现有档位表本身自洽，把 LARGE_MAX 表达成 DEFAULT_ITERATIONS 的函数反而会引入真实耦合）。
#71 | fix | 成立：`1024` 逐字出现两次（PER_STATE_ITEM/PER_CACHE_ITEM，注释同为 1KB），PER_STORE 的 `10 * 1024 * 1024` 也靠注释解释。已抽 `const KB = 1024` / `const MB = 1024 * KB` 并改为 `KB`、`10 * MB`，两处阈值仍是各自独立的旋钮（可单独调成 `2 * KB`）。

#54 | fix | 成立：`BenchmarkStore.subscribe`/`destroy` 只有「订阅状态变化 / 销毁 Store」一行字，幂等性、通知中增删订阅、缓存释放全部未规定。已把契约写进接口：退订句柄幂等且只减自己那一笔、通知按进入本轮时的在册快照派发（实现不得边派发边收缩在册集合）、允许同一函数多次注册；destroy 幂等、返回后不得再回调、释放限本实例、`getCached` 不得返回旧值、`getCacheStats` 若销毁后仍可调用要报清零而非抛错。口径与主包一致（`src/core/store/SubscriptionManager.ts` 的 consumed 旗标 + 快照注释、`Store.ts:632` 的「幂等：重复销毁不报错」），不是凭空许愿；harness 自身的时序也写清了（每实例只销毁一次：预热循环一个建一个销、场景在 finally 里销）。
#60 | fix | 成立：`Partial<BenchmarkConfig>` 是浅 Partial，`{ general: { warmupIterations: 5 } }`、`{ datasets: { medium: { stateKeys: 50 } } }` 这类「本函数运行时真支持」的写法在类型层被拒（原 relaxedBenchmarkConfig 就被迫把 general 整份抄一遍）。已加 `DeepPartial<T>` + `BenchmarkConfigOverride`（数组走整体替换，与 scenarios 语义一致），`mergeConfig` 第二参与 `BenchmarkRunner` 构造 config 同换。类型探针实测：深 partial 通过、`{general:{nope:1}}`/`{scenarios:[{name:'x'}]}`/`{datasets:{huge:...}}` 三处仍报错（@ts-expect-error 未被判 unused）；运行时实测逐层合并结果正确且默认配置未被污染。
#62 | fix（datasets/scenarios 一半已被 medium #61 解决） | 成立：relaxed 手写一整套并行阈值，新键只会进默认表。已改为逐键乘倍数派生（`OPERATION_TIME_RELAXATION`/`MEMORY_RELAXATION`/`THROUGHPUT_RELAXATION` 三张 `Record<keyof 分组, number>` 表 + `RELAXED_CACHE_HIT_RATE`），加键漏登记即编译错误。node 直跑真实 config 实测：12 个阈值键派生值与原手写值 `===` 全等（顺带确认浮点无残差），`datasets`/`scenarios` 不再共享引用是 #61 已修的部分。
#66 | fix（文档口径）+ reject（evictions 必填） | 比率部分成立但危害被夸大：包内产出 `results.cache` 的三处（buildCacheResult/emptyCacheResult/mergeCacheStats）全部从 hits/misses 现算，上游 `CacheStats` 根本没有比率入口，适配方无法注入矛盾比率；改不掉字段是因为 reporter.ts（本组清单外）与 runner 门限判定都直接读它，故按报告的第二方案在类型上写清派生不变式与「必须走 buildCacheResult」。`evictions` 改必填不予采纳：与已提交的 medium #50（`getCacheStats` 可选、省略即未启用）同源，「未统计」≠「0 次淘汰」，helpers.mergeCacheStats 正是靠 undefined 保留这个区分，必填只会逼实现方伪造 0——这条属修法方向错，已在注释里写明取舍。
#75 | fix | 成立，且比报告说的更多：取键逻辑实为五处（setState/getCached/$patch 连续键/dispatch 动作名/runWarmupIterations），各写各的 `%` 与守卫。已抽 `pickKey(items, index)`（空集统一返回 undefined），五处调用点全部收敛，行为逐字对齐改前（`keys.length > 0` 守卫 → `key !== undefined` 判定，语义等价）。
#76 | fix（并扩到第三处） | 成立：`runWarmupIterations`/`runBenchmarkIteration` 体内零 await（`measureTime`、`getMemorySnapshot` 均同步），热循环每轮白多一个 promise + microtask 跳；同类问题报告漏了 `runWarmup`（同样无 await），一并改同步。三处调用点的 `await` 同时去掉。报告括号里的「可能触发 require-await」在本仓不成立：`grep require-await eslint.config.js` 零命中，故改前 eslint 也不会报（现有 `npx eslint packages/benchmark/src` 退出码 0）。`ActionUtils.execute` 在 src/** 内，越界不动，交由主包片处理。
#78 | fix | 成立且后果可测：判定用 `setState * sizeMultiplier * modeMultiplier * 10`（small 档即 1ms），建议却拿裸 0.1ms 比；内存门限参与判定但建议清单里一档都没有 → 只栽在内存上的场景「不通过却零建议」；错误结果（耗时/吞吐全 0）反而会被判出「吞吐量较低」盖住真实报错。已让 `checkThresholds` 返回 `{ passed, issues }`（issues 用判定当场算出的放宽阈值成文，含内存档），按结果对象存进 `thresholdIssues` Map（不用场景名回查，避免重名张冠李戴），`generateRecommendations` 只做汇总、无评估记录时给出「未能完成 + errors」一条。端到端实测：不通过场景的建议逐条带上真实门限（如「峰值内存增量 315.02 KB 超过门限 48.83 KB」= 10000 × 1 × quick 5），passed 场景不再灌提示，判定数学与 MIN_PASS_RATIO 一字未改。
#79 | fix | 成立：`memorySnapshots` 只有 1 处写（runAll push）+ 1 处重置，零读取方（`grep -rn memorySnapshots packages/benchmark/src` 三条命中全在 runner.ts 自身），每场景还要多打一次 `process.memoryUsage()`。删字段与 push；报告给的「写进报告」方案不采纳——本包没有逐场景内存趋势的展示口径（reporter 只渲染 result.results.memory），凭空加字段属于反向扩面。腾出的位置给了 #78 真正需要的 `thresholdIssues`。
#80 | fix | 成立：`passed: true` 建好即被下一行覆盖，纯死状态。改为 `measured: Omit<BenchmarkResult,'passed'>` 先测量、再一次成型 `{ ...measured, passed: evaluation.passed }`，不给占位值（这也让 `checkThresholds` 的入参收窄成 `Pick<BenchmarkResult,'results'>`，它本来也只读 results）。
#81 | fix | 成立（实测）：`iterations: 0` 时 durations/memSnapshots 皆空 → `0 / (0/1000)` 与 `0 / 0` 让 opsPerSecond 和 memory.avg 双双 NaN，NaN 与门限比较恒 false ⇒ 整场景「什么都不达标」；`getAverageMemorySnapshot(0)` 实测直接 `TypeError: Cannot read properties of undefined (reading 'heapLimit')`（-5/NaN 同样）。已三层收口：runScenario 进测量前校验 `Number.isInteger(iterations) && >= 1`（与 #57 的 dataset 校验同口径，抛错计入 errors 而非产假数）、吞吐除零兜底 `totalTime > 0 ? … : 0`（与 helpers.calculateThroughput 一致，计时精度低于单轮耗时时仍可能整轮 0）、`getAverageMemorySnapshot` 把 samples 夹到 `max(1, floor(samples))`。端到端实测：报告里再无 NaN/Infinity，两个坏场景各得一条「未能完成」建议。
#96 | fix | 成立（实测）：一次 `calculateTimeStats` 触发 3 次拷贝 + 3 次排序（给 `Array.prototype.sort` 打桩计数改前得 3、改后得 1）。已抽模块级 `percentileOfSorted(sorted, p)`，中位/p95/p99 共用一份有序数组；下标夹取算式一字未动，800 组随机数组逐点比对改前改后 median/p95/p99 全部相等（mismatch=0）。公开方法 `calculatePercentile`/`calculateMedian` 签名与行为保持（接口仍要求它们）。
#97 | fix（负值不采纳夹零） | 成立且比报告更糟：改前实测 `formatBytes(NaN)="NaN B"`、`(Infinity)="Infinity TB"`、`(-1)="-1.00 B"`、`(-1536)="-1536.00 B"`（`size >= 1024` 对负数恒不成立，负值根本进不了换算循环）。非有限值按报告改 `'N/A'`；负值不采纳 `if (bytes < 0) return '0.00 B'`——那会把「内存增量为负」这类真实信号抹平成 0，改为按绝对值选单位、符号前置，实测 `-1536 → -1.50 KB`、`-1 → -1.00 B`、`10240 → 10.00 KB`。
#106 | fix（报告建议的 Pick 形式收敛为接口本身） | 成立：inline 手抄的 10 个成员恰是 `BenchmarkStore<S>` 的全部成员，`Pick<BenchmarkStore<S>, 全成员>` 与该接口等价，故直接把入参写成 `BenchmarkStore<S>`（漂移不可能，且不再需要 ActionMap/CacheStats 两个 import）。strictFunctionTypes 的说法只在非泛型成员上成立，已实测：`dispatch(name: 'only', …)` 的实现赋给旧的手抄形状报错、赋给新入参通过（@ts-expect-error 精确命中）；而报告暗示的 `setState` 那一类泛型成员两种写法都拒（窄化到 `'a'` 时新旧签名同样报错），这点不作修正依据。运行期实测适配器仍正常包装（setState/getCacheStats/destroy 全通）。
#107 | fix（选改名方案，不选别名再导出） | 成立：显式导出优先级高于 `export type *`，`import type { BenchmarkUtils }` 只能拿到类实例类型，接口在包名下不可达且编译器全程静默（改前 `tsc -p` 零报错即证据）。按报告的第二选择处理——把接口改名为 `BenchmarkUtilsContract`（单一真相源，不给同一个东西留两个入口），utils.ts 的 `implements` 同步。类型探针实测：`Pkg.BenchmarkUtils`（类）与 `BenchmarkUtilsContract` 两个名字各自可达且互不等价（类多出 calculateTimeStats/repeat 等实现侧成员，反向赋值 @ts-expect-error 命中），类仍满足接口。改名安全性：全仓 grep 无第三方引用（包 `private: true` 且 pnpm-workspace 注明「暂未接入」，exports 只开 `.` 与 `./package.json`，tests/scripts/tools 零引用）。
