# G5-benchmark-medium-p1 判定记录

#19 | fix | 实测成立：`npx tsc -p packages/benchmark/tsconfig.json --traceResolution` 显示 `types:["node"]` 命中的是根 `node_modules/@types/node@24.8.1`（pnpm-workspace.yaml 设了 `nodeLinker: hoisted`，`packages/benchmark` 下根本没有 node_modules），runner.ts 的 `import os from 'node:os'` 确实依赖它；已在 devDependencies 显式声明 `@types/node: ^24.0.0`（与根 pin 24.8.1 兼容，hoisted 布局下不产生第二份副本）。
#48 | fix | 成立：default 分支把未知格式静默降级成 markdown。改为 `case 'markdown'` 显式列出 + default 抛 `TypeError`；并用 `@ts-expect-error generate(report, 'csv')` 随项目 typecheck 验证编译期同样拒绝（指令未被判为 unused）。
#49 | fix | 成立且已漂移：markdown 有 `数据集规模`/`P99 耗时`，HTML 两者皆无。抽出模块级 `toMetricRows(result)` / `toSummaryRows(report)` 行数据，markdown 与 HTML 各自只负责标记语法；HTML 侧对行值统一走 `escapeHtml`（`datasetSize` 是外部可写的 string，不能沿用 #47 的口子）。
#50 | fix | 取报告第一方案：`BenchmarkStore.getCacheStats` 改为可选（与 `getCached` 对称），runner 改成 `store.getCacheStats ? buildCacheResult(...) : emptyCacheResult()`——顺带删掉 runner 里手抄的同一份 hit/miss 算式；adapter 两侧都保持可选（省略即「缓存未启用」）。`@ts-expect-error store.getCacheStats()` 验证未收窄不可调用（TS2722）。
#51 | fix | 成立：`getState()` 返回活引用。改签名为 `Readonly<S>`（零运行时开销，符合基准库定位），JSDoc 写明「就地改会绕过 setState/$patch 并破坏缓存失效与订阅通知」；实测 `store.getState().p = 1` 编译不过。
#52 | fix（泛型方案不采纳） | 两处 actions 已统一为共享的 `StoreAction<S>` / `ActionMap<S>`（含 `StoreConfig.actions` 的 `this: { state: S }` 绑定），adapter 入参同步引用。报告建议的 `BenchmarkStore<S, A>` 类型映射不采纳：runner 的动作名来自 `Object.keys(store.actions)` 的运行时结果，编译期拿不到字面量名，`dispatch<K extends keyof A>` 在本包唯一调用点上退化为 `string`，只增签名噪音——该取舍已写进 dispatch 的 JSDoc。
#53 | fix | 成立：`ComposeStoreFn` 压平成员类型。按报告改为 `<T extends Record<string, State>>(stores: { [K in keyof T]: BenchmarkStore<T[K]> }) => BenchmarkStore<T>`；实测 `compose({a,b})` 能取到 `composed.getState().a.p`/`.b.q` 且访问未声明键报错（@ts-expect-error）。ComposeStoreFn 全仓仅此定义+再导出，无内部调用点，改动零连带。
#55 | fix | 成立：mergeResults 对同一输入调两次 mergeTimeStats（排序/min/max/stdDev 全做两遍）。提取 `mergedTimeStats` 局部量供 executionTime 与 throughput 共用。
#57 | fix（报告一处依据不准） | 空键/零容量真实存在：实测 `keys[i % keys.length]` 在 keys 为空时得 `NaN → undefined`，`Math.floor(Math.random() * 0)` 恒为 0。但「`??` 覆盖显式 `capacity: 0`」不成立——`0 ?? x` 仍是 0，`??` 只在 null/undefined 时兜底。三处收口：runScenario 进入前对 `stateKeys <= 0`/档位缺失抛错（记入 errors 而非产出假数据）；createTestStore 对显式 capacity 非有限或 <1 抛错、推导值 `Math.max(1, floor(stateKeys/2))`；缓存分支 `keySpaceSize < 1` 早退；helpers.warmupCache 空键早退。
#58 | fix | 成立：删除私有 `buildCacheResult`/`emptyCacheStats`，全部委托模块级 `buildCacheResult`/`emptyCacheResult`（含 mergeCacheStats 的空调用点）；两份算式合一，函数声明提升使类内前向引用合法（typecheck 通过）。
#59 | fix | 用 node 直跑真实 config.ts 实测：改前 `mergeConfig(default, {datasets:{medium:{stateKeys:50}}})` 的 medium 只剩 `{"stateKeys":50}`（actions/getters/subscribers/nestingDepth 被丢）；改后为 `{"stateKeys":50,"actions":20,...}`，逐档位合并。
#61 | fix | 实测改前 `relaxedBenchmarkConfig.datasets === defaultBenchmarkConfig.datasets` 与 `.scenarios ===` 均为 true，且 `mergeConfig(base, undefined) === base`；改副本的 `datasets.medium.stateKeys = 999999` 直接把模块级默认值改成 999999。现 mergeConfig 总是返回逐层副本（datasets 逐档、thresholds 逐组、scenarios 新数组），`relaxedBenchmarkConfig` 改由 mergeConfig 派生（副本与阈值一次到位，实测 relaxed 的 general/thresholds 取值与改前逐项一致）。scenarios 保持整组替换语义，并把 `||` 换成 `??`，让显式空数组按「不跑场景」而不是「回落到默认 7 个场景」解释。
