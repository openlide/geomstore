# G4-types-tests-medium-p2 判定记录

#401 | fix | 属实（`on`/`emit` 与 HookName 无关联、处理器只能写 unknown、`emit('afterDispatch', name, result)` 静默通过）：新增 `HookArgsMap`（逐钩子实参元组）并让 `on`/`emit` 都按 `K extends HookName` 索引它。实测两件事决定了最终形状：①`on<K>(hookName: K, handler: (...args: HookArgsMap[K]) => void)` 直写会让 `HookSystem.on(hookName: HookName, handler: HookHandler)` 报 TS2416（约束实例化后元组联合交叉成 never），逐钩子重载同样失败；②改成 `HookHandlerFor<K> | HookHandler` 联合虽过实现检查，但实测（`X extends string ? true : false` 探针）无标注箭头的形参全部退化为隐式 any，等于丢掉修复目的。故 `HookHandlerFor<K>` 用 `IsUnion` 分支：单一钩子名给精确元组、联合钩子名（组合层桥接）走既有擦除形状，`npx tsc --noEmit -p tsconfig.typecheck.json` 与 `-p tsconfig.tests.json` 均零输出。断言见 tests/types/plugin-hook-args.typecheck.ts。
#402 | fix | 成立：`emit` 返回 `void`、实现层 `handler(...args)` 丢弃返回值，`TResult` 在契约里无处生效（实测 `HookHandler<[], boolean>` 可声明但永远拿不到结果）。按报告第一方案删掉该参数（`HookHandler<TArgs extends unknown[] = unknown[]>`），并在 JSDoc 写明「返回值一律忽略，需否决请走异常通道」；仓库内无人使用第二参数（grep `HookHandler<` 仅 types/plugin.ts 自身定义），新形态由 tests/types/plugin-hook-args.typecheck.ts 的 `@ts-expect-error` 固定。
#407 | FP | 目标文件已不存在：`ls tests/jest.d.ts` → No such file，`git log --diff-filter=D -- tests/jest.d.ts` → 该文件在 1097621（Wave A critical/high 的 #405/#406）里已整体删除（p1 记录同样提到此事）。删除后测试侧只依赖 `types: ["jest","node"]`（tsconfig.json / tsconfig.jest.json），实测 node_modules/@types/jest/index.d.ts 里 `not`(808)/`resolves`(821)/`rejects`(826) 与报告点名的 7 个匹配器（命中 10 处）齐备，`npx tsc --noEmit -p tsconfig.tests.json` 零输出 ⇒ 报告描述的「不一致」前提不成立，无需补任何东西。
#409 | fix（types 侧一次到位 + extras 一行联动待办） | 两点都成立：`combiner` 返回 `unknown` 而 `combine` 用 `combiner(...results) as R` 收口，实测显式 `combine<OrderState, number>` 时 combiner 返回字符串照样编译通过（typer 检查零输出）。已给 `SelectorComposerInput` 增加第 3 个类型参数 `R = unknown` 并让 combiner 返回 `R`（两参数旧写法行为不变，故不破坏 src/extras 现状；`combine` 的入参是 `SelectorComposerInput<S, T>` ⇒ R 取默认 unknown，extras 侧一行改 `SelectorComposerInput<S, T, R>` 后即可删掉 `as R`，见待办）。报告「any[] 形参必须保留」的断言实测为真：临时改成 `unknown[]` 后 `npx tsc --noEmit -p tsconfig.tests.json` 在 tests/unit/extras/selector/selectorComposer.test.ts:25/35、selector-interface-state.test.ts:74 等多处报 TS2322（`(...nums: number[]) => number` 不满足 `(...results: unknown[]) => unknown`），已回退。断言见 tests/types/selector-combiner-result.typecheck.ts。
#414 | fix（报告给的写法不采用） | 死代码 + 注释与实现不符成立：本文件经 jest.config.js `setupFilesAfterEnv` 载入，`jest` 必为已定义，`try { jest.useRealTimers() } catch { 非 jest 环境 }` 永不命中，已删。改为显式 `jest.clearAllTimers()` 再 `jest.useRealTimers()`。但**报告建议的 `try/catch` 包住 clearAllTimers 不成立**：实测（tests/unit/types-interface-state.test.ts #414 用例）jest 30 下未启用 fake timers 时 `jest.clearAllTimers()` 不抛错（`expect(...).not.toThrow()` 通过），再套一层 try/catch 等于复制同一处死防御代码。另：报告「泄漏 handle 使 worker 无法优雅退出」的因果未获证实——修复后整跑 `npx jest --ci --silent tests/unit tests/integration`（101 套件 / 2708 用例全绿）仍打印 "A worker process has failed to exit gracefully"，症状源不在这里（另有 open handle，见待办）。
#415 | fix | 两点实测成立：`process.listeners('unhandledRejection')` 里存在只 `console.error` 的 handler（真实异步缺陷被吞），且 setupFilesAfterEnv 每个测试文件执行一次、worker 内共享 `process` ⇒ 每文件净增 2 个监听器。取报告第一方案「让 Jest 自带处理接管」：两个 handler 直接删除（Node 默认 `--unhandled-rejections=throw`、Jest 会把这类错误报成用例失败），比「自行 throw + afterAll off」少一份与 Jest 重复的逻辑；`npx jest --ci --silent tests/unit tests/integration` 101 套件全绿，说明仓库内没有靠该 handler 静默才通过的用例（tests/unit/extras/action/AsyncActionSupport.test.ts:510-533 自装自摘监听器计数，不受影响）。回归断言：setupHandlers(...) 中不得再出现含 `[Test Setup]` 的 handler 源码。
#416 | fix | 属实：`request/setStorageSync/getStorageSync/removeStorageSync` 原为裸 `jest.fn()`，恒返回 undefined ⇒ 「写入后读回」这类往返断言要么因错误原因失败要么空过。改为内存 Map 支撑的真实读写删（并补 `clearStorageSync`），`request` 默认同步回调 `success({ statusCode: 200, data: {}, errMsg: 'request:ok' })`，两者都仍是 `jest.fn()` 包装、用例可 `mockImplementation(Once)` 覆盖（新用例里就覆盖了 fail 分支）。缺键返回 undefined 与库侧 `plugins/builtin.ts:170-171` 的 `v === undefined || v === null ? null : v` 判定一致（真机返回 `''`，两者都按「无数据」处理）。回归用例 tests/unit/types-interface-state.test.ts #416（往返/无数据/清空/request 默认与覆盖），全套 101 套件 2708 用例全绿，未依赖旧的 undefined 空过行为。
#420 | fix（报告写法之一不可行，已实测） | 两点成立：整调用的 `@ts-expect-error` 会吞掉该表达式里任何其它类型错误；且入参写 `StoreOptions<S>`（state 可选）使 `createTestStore({})` 编译通过，而真实 `createStore` 两个重载都要求 state。已改为 `TestStoreConfig<S> = StoreOptions<S> & { state: S | (() => S) }`（state 必填，实测 158 处既有调用点无一受影响），并删掉 `@ts-expect-error`，改为对函数形状做一次带理由的断言（`createStoreFromOptions`）。实测「按报告的第一个方案——cast 到实现签名参数类型 / 复刻两个 state 必填的重载」都走不通：把入参换成 `StoreConfig` 复刻重载会让 `ThisType` 实参停在未展开的 `ResolveState<S>` 条件类型上，action 内 `this` 退化成 `{}`（实测 store.test.ts 报 17 处 TS2339）；只断言 `getters` 仍剩 `cacheKeys` 同样不可比（TS2769）。返回类型取实现类 `core/store/Store`（类上有 `setStateProtection` 等接口未声明成员，实测改窄会报 8 处 TS2339）。断言见 tests/types/create-test-store-config.typecheck.ts。
#421 | fix | 成立（`ResolveState<S> extends State ? …` 在 store.ts 内实测 3 处而非 4 处，第 4 处在 src/core/store/factory.ts 的注释里）：已提取 `export type ResolvedState<S>` 并把 `StoreConfig` / `StoreOptions` 的 7 个非差异选项收进新的 `StoreOptionsBase<S>`，两接口只保留 `actions` / `getters` / `cacheKeys`。刻意保留 getter 侧 `Record<string, unknown>` 的兜底分支（与 `ResolvedState` 的 `State` 兜底不同：`S = unknown` 的裸配置下 getter 需仍能按键读状态，统一会改变该形状）；`StoreOptionsBase<S>` 的 S 不加 `extends State`（否则 `StoreConfig` 的 `unknown` 默认参数无法满足基接口，实测 TS2312）。两个 tsc 零输出，说明 `Store` 侧与 core/extras/integrations 消费方均未受影响。断言见 tests/types/store-config-base.typecheck.ts（含基座键集合的双向断言）。
#423 | fix（报告的表达式不可直接采用） | 成立：交叉不覆盖，同名 action 会合成重载体。实测：未修复时 `actions: { getState(): number; probe() { const n: number = this.getState() } }` 报 TS2322（`this.getState` 命中基座 `() => S`）。按报告方案一实现，但**表达式必须加守卫**：直接写 `Omit<ActionContextBase<S>, 'dispatch' | keyof A>` 时 `keyof Actions = string | number`，实测把基座整体抹掉，18 处报错并波及禁改文件（src/integrations/enterprise/user-store.ts:138、tests/integration/with-store.test.ts:311 等），故用 `ActionCollisionKeys<A> = string extends keyof A ? never : keyof A`：索引签名（擦除 A）不剔键，具体 action 集合才剔。断言见 tests/types/action-context-collision.typecheck.ts。
#427 | fix | 成立：`grep -rn "onShareTimeline|onAddToFavorites|onSaveExitState|onThemeChange" src tests docs examples` 只命中 `src/types/integration.ts` 的 onResize/onTabItemTap 两行，即这些基础库页面级键仓库内确实无处声明。已补 `onShareTimeline` / `onAddToFavorites` / `onSaveExitState` 与非函数框架键 `options`，并注明「漏收即被 PageOwnMethods 当自定义方法、剥掉 this 后并入 ExtraMethods」。断言：tests/types/integration-types.typecheck.ts 里 `keyof PageOwnMethods<PageCfgShape>` 必须是单元素联合 `'customMethod'`（漏收任一框架键即报错）。
#428 | fix（文档项）+ 联动待办 | 死代码与文档失实都成立：`PageOwnMethods` 仅被 `src/core/index.ts:61` 重导出，无任何实现层引用（grep 结果：with-store.ts 只用 `ComponentOwnMethods`，页面侧是 `PageThis<S, A, G, O>`，未传第 5 泛型）；`PageThis` 注释却写「用户自定义方法也作为 ExtraMethods 注入 this」。已就地改掉两处失实注释（`PageThis` 头注释 + `PageOwnMethods` 说明），并新增 `PageThis<UserState, UserActions, Getters, {mapState,mapActions}>` 的断言把现状钉住（自定义方法确实不在 this 上）。不删导出：`PageOwnMethods` / `PageReservedKeys` 已随 dist 发布，删除属破坏性变更；把 `PageOwnMethods<C>` 接到 `withPageStore` 是集成层一行改动，越界，见待办。

## 主控需知（联动待办）
- #401 契约面生效、实现面还差一步：`src/core/store/Store.ts:169` 的 `public readonly hooks: HookSystem` 把接口成员窄化成了实现类，`createStore(...).hooks.on('beforeDispatch', (name: string, args) => …)` 走的是类上的擦除签名（实测精确形参在这一点上被判 TS2345）；改成 `IHookSystem`（或让 `HookSystem.on/emit` 也按 `K extends HookName` 泛型化）后，精确参数才对直连调用方可见。插件侧（`install(store)` 的 `Store` 接口）已实测生效。
- #401 新类型未导出：`HookArgsMap` / `HookHandlerFor` 需要加进 `src/core/hooks/index.ts:6`、`src/core/index.ts:47`、`src/plugins/index.ts:13` 与根入口的类型重导出，否则外部只能按 `@openlide/geomstore/...` 的深路径引用；`docs/API.md:144-145` 的 `on(name, handler: HookHandler)` 描述同步（Wave D）。
- #409 一行联动：`src/extras/selector/selectorComposer.ts:64` 的入参 `SelectorComposerInput<S, T>` 改成 `SelectorComposerInput<S, T, R>`，随后删掉第 72 行 `combiner(...results) as R` 的 `as R`；types 侧已把 `R` 通到 combiner 返回位，不改这一行则 `R` 取默认 `unknown`、断言仍在（本轮刻意保持现状不破坏 extras）。
- #428 一行联动：`src/integrations/with-store.ts:154` 的两处 `PageThis<S, A, G, O>` 补第 5 泛型 `PageOwnMethods<C>`（与 `with-store.ts:286/304/410` 的 Component、`with-app-store.ts:143` 的 App 对齐）。接线完成后请删掉 `src/types/integration.ts` 里 `PageOwnMethods` / `PageThis` 两段「当前未注入」的现状说明。
- #414 遗留症状：整跑 `npx jest --ci --silent tests/unit tests/integration` 仍打印 “A worker process has failed to exit gracefully”（修复前后都在），与本条无关，需 `--detectOpenHandles` 另案定位（大概率落在 tests/unit 各用例文件，属我的禁改目录）。
- Wave D 同步：公开类型面本轮变化 = 新增 `ResolvedState` / `StoreOptionsBase` / `HookArgsMap` / `HookHandlerFor`；`HookHandler` 由 2 个类型参数变 1 个（**破坏性**，按 minor + 迁移说明写 CHANGELOG）；`ActionContext` 在 action 名与基座成员同名时的解析结果改变（#423）；`IHookSystem.on/emit` 签名收紧（#401）；`SelectorComposerInput` 多一个可选类型参数（#409）；`PageReservedKeys` 扩列（#427，纯类型，无运行时影响）。docs/API.md 的 hooks 章与 CHANGELOG 需一并更新。
- 说明：本轮未创建/未删除任何 `.py` 文件（唯一的 python3 调用被权限拦截、未执行，`ls *.py` 为空）；`.ocr-fix/scripts/*.py` 是其他 agent 的产物，未触碰。
